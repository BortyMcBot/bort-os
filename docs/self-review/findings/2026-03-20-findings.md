# Self-Review Findings — 2026-03-20

Generated by `self-review-job.mjs` on 2026-03-20T07:43:22.041Z

## Findings

### scripts/autonomous_guard.mjs
- **Severity:** HIGH
- **Finding:** The script uses `require('child_process')` inside an ES module, which will throw at runtime because `require` is not defined in `.mjs`.
- **Suggestion:** Replace the CommonJS `require` call with a top-level ESM import from `child_process` and reuse it in `stopAutonomous()`.
- **Disposition:** PR opened

### scripts/autonomous_report.mjs
- **Severity:** MEDIUM
- **Finding:** PR collection ignores `gh` command failures and silently reports empty arrays, which can mask auth, network, or repo-path errors as “no PRs”.
- **Suggestion:** Check the exit code from both `gh pr list` calls and surface/log failures instead of defaulting to `'[]'`.
- **Disposition:** PR opened

### scripts/autonomous_runner.mjs
- **Severity:** LOW
- **Finding:** `RUN_QUEUE` points to `memory/autonomous_queue.md` while the generator/executor use `memory/autonomous_queue.json`, leaving the seeded markdown queue unused.
- **Suggestion:** Remove the dead markdown queue path/seed logic or consolidate everything onto one queue format.
- **Disposition:** Logged only

### scripts/autonomous_execute_queue.mjs
- **Severity:** MEDIUM
- **Finding:** All git/gh command exit codes are ignored, so branch creation, commit, push, or PR failures can cascade and leave partial state without stopping execution.
- **Suggestion:** Make `run()` throw on non-zero exit codes and handle failures explicitly per task before continuing.
- **Disposition:** PR opened

### scripts/autonomous_execute_queue.mjs
- **Severity:** LOW
- **Finding:** `task` is accepted by `execBortOsTask()` and `execPersonalWebsiteTask()` but only a single hardcoded task ID is handled in each function, making the queue format more general than the implementation.
- **Suggestion:** Add an explicit default case that logs unknown task IDs or refactor to a dispatch table keyed by task ID.
- **Disposition:** Logged only

### scripts/autonomous_start.mjs
- **Severity:** MEDIUM
- **Finding:** The `until` argument is not validated beyond presence, so malformed values can be written to state and produce a zero-second hard-stop timer with confusing behavior.
- **Suggestion:** Validate `until` against `HH:MM` before writing state or spawning background processes.
- **Disposition:** PR opened

### scripts/deploy.mjs
- **Severity:** HIGH
- **Finding:** Deployment decides whether to restart by diffing only `HEAD~1..HEAD` after `git pull`, which misses multi-commit pulls and can skip required restarts for earlier commits in the fetched range.
- **Suggestion:** Capture the pre-pull commit and diff the full pulled range (`prevHead..HEAD`) before deciding whether a restart is required.
- **Disposition:** PR opened

### scripts/deploy.mjs
- **Severity:** MEDIUM
- **Finding:** `runReadOnly()` still executes commands during `--dry-run`, so supposedly read-only simulation mode mutates nothing but still depends on live git state and can fail noisily.
- **Suggestion:** Make dry-run explicitly stub or guard all external commands consistently, or rename the mode to reflect that some real commands still run.
- **Disposition:** PR opened

### scripts/export-project-source.mjs
- **Severity:** LOW
- **Finding:** `buildTgzBundle()` discards tar stderr and its boolean result is ignored, so bundle creation failures are silently swallowed while the export continues.
- **Suggestion:** Check the tar exit status in `main()`, log stderr, and fail or warn explicitly when bundle generation does not succeed.
- **Disposition:** Logged only

### scripts/generate-bort-capabilities-md.mjs
- **Severity:** LOW
- **Finding:** Input and output paths are hardcoded to `/root/.openclaw/workspace/source/...`, which breaks the workspace/path pattern used by the rest of the scripts.
- **Suggestion:** Resolve paths from a shared workspace constant or environment variable instead of embedding absolute paths.
- **Disposition:** Logged only

### scripts/generate-state-of-bort.mjs
- **Severity:** MEDIUM
- **Finding:** `countFiles(WORKSPACE)` recursively walks the entire workspace every run, which is unnecessarily expensive for a generated status document and will scale poorly as the repo grows.
- **Suggestion:** Drop the recursive file count or scope it to a smaller set of relevant directories/cached metadata.
- **Disposition:** PR opened

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** `ghJson()` is dead code and is never called anywhere in the script.
- **Suggestion:** Remove the unused helper or replace current JSON parsing call sites with it consistently.
- **Disposition:** Logged only

### scripts/pr-review-job.mjs
- **Severity:** MEDIUM
- **Finding:** `runDeploy()` executes during `--dry-run` after an APPROVE decision, which makes dry-run mode perform extra side effects/logging instead of remaining review-only.
- **Suggestion:** Skip deploy entirely in dry-run mode and reserve deployment for successful real merges only.
- **Disposition:** PR opened

### scripts/repo_hygiene_check.sh
- **Severity:** HIGH
- **Finding:** The script auto-adds and commits all workspace changes with `git add -A`, which can accidentally publish unrelated or sensitive local edits instead of only hygiene drift.
- **Suggestion:** Restrict staging to known hygiene/generated paths or switch to alert-only mode unless an allowlisted file set is dirty.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** HIGH
- **Finding:** `STALENESS_STATE_PATH` is hardcoded to `/tmp/pr-staleness-state.json`, so notification history is non-durable and can be lost across reboots or collisions with other runs.
- **Suggestion:** Store staleness state under the workspace (for example in `memory/` or `docs/self-review/`) instead of `/tmp`.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** MEDIUM
- **Finding:** The code writes temp prompt files in `/tmp` (`tmpPrompt`, `tmpFixPrompt`) but then passes the in-memory prompt string directly to `openclaw`, making those files redundant.
- **Suggestion:** Remove the unused temp-file writes or actually use the files as the source of truth for agent invocation/debugging.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** MEDIUM
- **Finding:** `createFixPRs()` shells out to the agent and then relies on `git status --porcelain` cleanup heuristics, but it does not fail fast if the target file itself is deleted/renamed or if branch creation/push partially succeeds.
- **Suggestion:** Add explicit validation that the target file still exists and wrap branch lifecycle steps with stronger rollback/error handling.
- **Disposition:** PR opened

### scripts/site-improvement-job.mjs
- **Severity:** LOW
- **Finding:** `PINCHTAB_PID` and `PINCHTAB_BASE` are defined but never used.
- **Suggestion:** Remove the unused constants or wire them into the Pinchtab start/health flow consistently.
- **Disposition:** Logged only

### scripts/site-improvement-job.mjs
- **Severity:** MEDIUM
- **Finding:** `writeTodo()` emits Markdown tables containing unescaped model-provided fields like titles and descriptions, which can break rendering if they include `|` or newlines.
- **Suggestion:** Escape or sanitize table cell content before interpolating analysis output into Markdown tables.
- **Disposition:** PR opened

### scripts/update-bort-bundle.sh
- **Severity:** MEDIUM
- **Finding:** The script copies a hardcoded file list into the public bundle without existence checks, so adding/removing canonical export files can break the update flow unexpectedly.
- **Suggestion:** Derive the bundle file list from the canonical source/export script or validate each file before copying with a clear error.
- **Disposition:** PR opened

### integrations/gmail/backlog-sweep.log
- **Severity:** HIGH
- **Finding:** A large runtime log with sender-level inbox data and operational output is committed in-repo, which creates unnecessary churn and exposes sensitive mailbox metadata.
- **Suggestion:** Stop versioning the log file and write it to an ignored runtime/logs path outside the tracked integration folder.
- **Disposition:** PR attempted

### integrations/gmail/backlog-state-gobuffs10.json
- **Severity:** HIGH
- **Finding:** Runtime sweep state including detailed offender maps is stored as a tracked repo file, leaking account-specific operational data and causing high-churn commits.
- **Suggestion:** Move backlog state to an ignored per-account runtime state directory and keep only a template/example in the repo.
- **Disposition:** PR opened

### integrations/gmail/other-prune-state-gobuffs10.json
- **Severity:** HIGH
- **Finding:** The tracked state file contains thread IDs, sender mappings, and account-specific counts, which is sensitive runtime data and not source code.
- **Suggestion:** Relocate prune state to an ignored runtime path and exclude account-specific state snapshots from version control.
- **Disposition:** PR opened

### integrations/gmail/progress-gobuffs10.json
- **Severity:** MEDIUM
- **Finding:** Account-specific progress state is committed to the repo even though it is mutable runtime data rather than reusable code or config.
- **Suggestion:** Store progress markers under an ignored runtime/state directory and keep only a generic schema or template in-repo.
- **Disposition:** PR opened

### integrations/gmail/prefs-gobuffs10.json
- **Severity:** MEDIUM
- **Finding:** The integration hardcodes a single account-specific prefs file throughout multiple scripts, which breaks reuse and diverges from config-driven patterns.
- **Suggestion:** Accept an account slug or prefs path consistently and treat the checked-in file as user data rather than the code default.
- **Disposition:** PR opened

### integrations/gmail/lib.js
- **Severity:** MEDIUM
- **Finding:** `loadOAuthClient()` does not validate parsed credential structure before dereferencing `redirect_uris[0]`, so malformed creds fail with opaque runtime errors.
- **Suggestion:** Add explicit validation for `client_id`, `client_secret`, and `redirect_uris` and throw a clear configuration error early.
- **Disposition:** PR opened

### integrations/gmail/lib.js
- **Severity:** LOW
- **Finding:** `getOrCreateLabel()` lists all labels on every call, so scripts creating five labels perform five redundant label-list API requests.
- **Suggestion:** Cache label lists per run or add a batch helper that fetches labels once and resolves all required names.
- **Disposition:** Logged only

### integrations/gmail/classify.js
- **Severity:** LOW
- **Finding:** `looksImportant()` rebuilds lowercased allow/block/keyword arrays on every message classification, creating avoidable repeated work in large sweeps.
- **Suggestion:** Normalize prefs once per run and pass precomputed sets/arrays into the classifier.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.js
- **Severity:** MEDIUM
- **Finding:** The code computes `bucket` but never uses it after classification, leaving dead state that adds noise without affecting behavior.
- **Suggestion:** Remove the unused `bucket` variable or use it consistently in output/state to justify its presence.
- **Disposition:** PR opened

### integrations/gmail/backlog-sweep.js
- **Severity:** MEDIUM
- **Finding:** Per-thread processing failures abort the entire sweep, so one malformed thread or transient metadata error can stop backlog progress for the whole batch.
- **Suggestion:** Catch and record errors per thread, then continue with the remaining threads while preserving failure details in state/output.
- **Disposition:** PR opened

### integrations/gmail/backlog-sweep.js
- **Severity:** LOW
- **Finding:** The default prefs and state filenames are hardcoded to the gobuffs10 account, reducing portability despite the script already taking creds and token paths.
- **Suggestion:** Derive defaults from an account slug or require explicit prefs/state inputs for multi-account consistency.
- **Disposition:** Logged only

### integrations/gmail/count-threads.js
- **Severity:** MEDIUM
- **Finding:** This script duplicates OAuth loading and backoff logic that already exists in `lib.js`, creating inconsistent patterns and extra maintenance surface.
- **Suggestion:** Reuse shared helpers from `lib.js` for auth and retry handling instead of re-implementing them locally.
- **Disposition:** PR opened

### integrations/gmail/count-unread.js
- **Severity:** LOW
- **Finding:** The script imports `googleapis` only to instantiate Gmail after `loadOAuthClient()`, while every other Gmail utility follows a mixed but more centralized helper approach.
- **Suggestion:** Consolidate Gmail client creation into a shared helper to reduce boilerplate and keep script patterns consistent.
- **Disposition:** Logged only

### integrations/gmail/daily-review.js
- **Severity:** HIGH
- **Finding:** The implementation removes `UNREAD` for `spamReview` even though the runbook says daily review should not change read/unread state.
- **Suggestion:** Remove the spamReview mark-read behavior or update the documented policy so code and runbook agree.
- **Disposition:** PR opened

### integrations/gmail/daily-review.js
- **Severity:** MEDIUM
- **Finding:** Archiving subscribed senders performs a second `threads.modify` call per thread instead of combining the `INBOX` removal into the primary modification request.
- **Suggestion:** Fold archive-by-rule into the first modify request to match the lower-call pattern already used by `backlog-sweep.js`.
- **Disposition:** PR opened

### integrations/gmail/daily-summary.sh
- **Severity:** HIGH
- **Finding:** The script hardcodes `/tmp/gmail-daily.json` inside the embedded Node snippet instead of using the configurable `OUT` path defined at the top.
- **Suggestion:** Pass `$OUT` into both Node snippets so the script respects its own output-path configuration.
- **Disposition:** PR opened

### integrations/gmail/daily-summary.sh
- **Severity:** MEDIUM
- **Finding:** The Telegram chat ID is pulled from a hardcoded workspace module path rather than from an explicit integration config/env source.
- **Suggestion:** Make the Telegram target an environment/config input and keep the shell script independent of workspace-internal JS constants.
- **Disposition:** PR opened

### integrations/gmail/inbox-audit.js
- **Severity:** MEDIUM
- **Finding:** `scanTopSenders()` makes one `threads.get` call per thread for up to 10,000 threads by default, which is expensive and likely to be slow or rate-limited.
- **Suggestion:** Reduce the default scan size, make sampling explicit, or add a cheaper sender aggregation strategy with stronger batching/checkpoint controls.
- **Disposition:** PR opened

### integrations/gmail/other-prune.js
- **Severity:** MEDIUM
- **Finding:** The prune flow does a full two-pass scan with one metadata fetch per thread and no checkpointing, making it inefficient and fragile for larger labels.
- **Suggestion:** Prefer the resumable checkpointed implementation and retire or clearly limit this non-resumable variant.
- **Disposition:** PR opened

### integrations/gmail/other-prune2.js
- **Severity:** MEDIUM
- **Finding:** Gmail API calls in `listThreadsPage()`, `threads.get`, and `threads.modify` bypass the shared `withBackoff()` helper, so transient 429/5xx failures are not retried.
- **Suggestion:** Wrap all Gmail API operations in the shared backoff helper for consistency with the rest of the integration.
- **Disposition:** PR opened

### integrations/gmail/status-ping.sh
- **Severity:** HIGH
- **Finding:** The script parses JSON with `console.log(...)` inside `node -pe`, which emits `undefined` after the value and can corrupt `PROCESSED` and `UPDATED`.
- **Suggestion:** Use `process.stdout.write(...)` or a plain `node -e` invocation when extracting scalar values from JSON.
- **Disposition:** PR opened

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** Telegram chat ID discovery hardcodes `/root/.openclaw/openclaw.json` instead of using the workspace/env configuration pattern already started by `WORKSPACE` and `GMAIL_DIR`.
- **Suggestion:** Read the chat ID from an explicit env/config variable and avoid direct dependency on a fixed host config path.
- **Disposition:** PR opened

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** The `--email` argument is required only for OAuth account selection messaging but is never validated against the loaded token or used in API calls.
- **Suggestion:** Remove the unused required flag or actually verify that the authenticated Gmail profile matches the requested email.
- **Disposition:** Logged only

### integrations/gmail/unsubscribe.js
- **Severity:** MEDIUM
- **Finding:** `parseMailto()` uses `new URL(mailtoUrl)` without guarding parse failures, so malformed `List-Unsubscribe` mailto headers can crash the sender loop.
- **Suggestion:** Catch URL parsing errors per target and record a failed attempt instead of aborting the whole sender flow.
- **Disposition:** PR opened

### integrations/gmail/unsubscribe.js
- **Severity:** MEDIUM
- **Finding:** The script searches `from:<sender> newer_than:90d` for every sender independently, which scales poorly when unsubscribing many senders in one run.
- **Suggestion:** Batch or cache message lookups where possible, or cap sender volume more aggressively with clearer operational limits.
- **Disposition:** PR opened

### os/models_inventory.js
- **Severity:** HIGH
- **Finding:** The script iterates `inv.openaiFallbackChain`, but `inventory()` in `os/model-routing.js` never returns that property, causing a runtime TypeError.
- **Suggestion:** Add the fallback chain to `inventory()` or remove that loop and print only fields the inventory actually exposes.
- **Disposition:** PR opened

### os/x_daily_post.js
- **Severity:** HIGH
- **Finding:** `latestCommitSubjects()` calls `execSync(...)` but only `execFileSync` is imported, so the fallback tweet path will throw at runtime.
- **Suggestion:** Import `execSync` from `child_process` or rewrite the helper to use the already imported `execFileSync`.
- **Disposition:** PR opened

### os/x_auth.js
- **Severity:** MEDIUM
- **Finding:** The post-auth `if (args.dryRun)` branch is unreachable because the script exits early in the initial dry-run path before starting OAuth.
- **Suggestion:** Remove the dead branch after token storage or replace it with a separate explicit post-auth verification flag.
- **Disposition:** PR opened

### os/x_auth.js
- **Severity:** MEDIUM
- **Finding:** `updateOpenClawEnvVars()` uses a hand-rolled busy-wait lock in `/tmp`, which wastes CPU and is brittle under contention.
- **Suggestion:** Replace the spin loop with a non-busy retry/backoff strategy or reuse a shared atomic config update helper.
- **Disposition:** PR opened

### os/x_call.js
- **Severity:** HIGH
- **Finding:** `updateOpenClawEnvVars()` rewrites `/root/.openclaw/openclaw.json` without any locking, so token refresh can race with other config writers and corrupt or lose updates.
- **Suggestion:** Use the same atomic lock/update pattern as other config writers or route token persistence through a single shared config helper.
- **Disposition:** PR opened

### os/x_call.js
- **Severity:** MEDIUM
- **Finding:** Budget preflight and actual ledger recording can diverge because `guardOrQueue()` uses `x_budget`'s estimator while `xCall()` may use `x_pricing.json` or `costUsdOverride`.
- **Suggestion:** Pass one canonical computed estimate into both the guard and the ledger so blocking and spend recording use identical pricing.
- **Disposition:** PR opened

### os/x_call.js
- **Severity:** MEDIUM
- **Finding:** A 401-triggered token refresh retries the same action after performing a second budget check, which can queue or charge retries inconsistently relative to the original call.
- **Suggestion:** Treat refresh retry as part of the original action budget decision or explicitly reserve retry budget up front.
- **Disposition:** PR opened

### os/model-routing.js
- **Severity:** MEDIUM
- **Finding:** `loadHatProfiles()` synchronously reads and parses `os/hat-profiles.json` on every routing call, adding avoidable repeated I/O to a hot path.
- **Suggestion:** Cache the parsed hat profiles in-process and reload only when explicitly needed.
- **Disposition:** PR opened

### os/model-routing.js
- **Severity:** MEDIUM
- **Finding:** The fallback return value `'openai/gpt-5.2-chat-latest'` is not present in the verified allowlist or route tables, breaking the module’s explicit availability-gating pattern.
- **Suggestion:** Return `null`/error when no allowlisted model is available or add the fallback model to the same verified inventory used everywhere else.
- **Disposition:** PR opened

### os/model-routing_selftest.js
- **Severity:** MEDIUM
- **Finding:** Several test comments and expectations describe models like `gemini flash-lite`, `gpt-5.2-pro`, and explicit OpenRouter OpenAI routing, but the assertions actually expect Codex outputs, making the self-test inconsistent and misleading.
- **Suggestion:** Update the comments/expectations to match current routing policy or adjust the routing table so the tests reflect intended behavior.
- **Disposition:** PR opened

### os/preflight.js
- **Severity:** MEDIUM
- **Finding:** `runProjectSourceCheck()` ignores the child process exit status entirely, so failures in the source check are silently dropped even though `spawnSync` does not throw for non-zero exits.
- **Suggestion:** Inspect the returned `status`/`error` from `spawnSync()` and log a compact warning instead of swallowing all failures.
- **Disposition:** PR opened

### os/preflight.js
- **Severity:** LOW
- **Finding:** The fallback hat profile embedded in `loadHatProfiles()` duplicates config already stored in `os/hat-profiles.json`, creating two sources of truth that can drift.
- **Suggestion:** Move the fallback to a minimal bootstrap/default file or centralize the shared profile data in one module.
- **Disposition:** Logged only

### os/preflight.test.js
- **Severity:** LOW
- **Finding:** `path` is imported but never used.
- **Suggestion:** Remove the unused `path` import to keep the test harness clean.
- **Disposition:** Logged only

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** The `BIO` constant is dead code because bio updates are always skipped and the value is never read.
- **Suggestion:** Remove the unused constant or implement bio-update support behind an explicit capability check.
- **Disposition:** Logged only

### os/state_report.js
- **Severity:** MEDIUM
- **Finding:** The report hardcodes `preflight tests: PASS (14 passed / 0 failed)` instead of reading actual test results, so it can drift from reality.
- **Suggestion:** Run the test harness or consume a generated status artifact instead of embedding a fixed PASS line.
- **Disposition:** PR opened

### os/state_report.js
- **Severity:** LOW
- **Finding:** Per-task routing fallback labels mention models like `openai/gpt-5.2-pro`, which are not part of the current routing allowlist and may not match real fallback behavior.
- **Suggestion:** Derive fallback text from the same routing inventory/config used by `model-routing.js` rather than hardcoding model names in the report.
- **Disposition:** Logged only

### os/constants.js
- **Severity:** LOW
- **Finding:** `resolveTelegramChatId()` hardcodes `/root/.openclaw/openclaw.json`, which makes the helper host-specific despite already exposing `BORT_WORKSPACE` as configurable.
- **Suggestion:** Resolve the config path from env/shared config settings instead of embedding an absolute host path.
- **Disposition:** Logged only

### os/x_budget_checks.js
- **Severity:** LOW
- **Finding:** The “blocked” check is not deterministic because `guardOrQueue()` usually tests a small real endpoint cost and may never hit the cap, leaving the test to fake success by appending a synthetic queue entry.
- **Suggestion:** Add an injectable cap/estimate test hook so blocked-path behavior can be asserted directly instead of approximated.
- **Disposition:** Logged only

### os/x_daily_watchdog.js
- **Severity:** MEDIUM
- **Finding:** `notify()` shells out via `execSync` and interpolates `TELEGRAM_CHAT_ID` directly into a shell command, which is inconsistent with safer `execFileSync` usage elsewhere.
- **Suggestion:** Switch to `execFileSync('openclaw', [...])` with argv separation to avoid shell interpolation and match existing patterns.
- **Disposition:** PR opened

### os/x_post_job.js
- **Severity:** MEDIUM
- **Finding:** `lastDigestEntry()` relies on brittle string slicing around the last `- ts_utc:` marker, which can misparse if that token appears elsewhere in the log format.
- **Suggestion:** Parse digest entries by explicit heading blocks or structured frontmatter instead of substring heuristics.
- **Disposition:** PR opened

### os/x_pricing.json
- **Severity:** LOW
- **Finding:** The file is treated as pricing input by `os/x_call.js`, but it currently contains only a manual-required note and no usable pricing schema, making its role ambiguous.
- **Suggestion:** Separate pricing metadata from live route-cost config or add schema/version fields so callers can distinguish “informational placeholder” from actionable pricing data.
- **Disposition:** Logged only

### os/memory/x_budget_ledger.json
- **Severity:** MEDIUM
- **Finding:** This is mutable runtime spend state checked into the repo, which creates churn and mixes operational data with source-controlled code.
- **Suggestion:** Move the budget ledger to an ignored runtime/state path and keep only a schema/example file in version control.
- **Disposition:** PR attempted

### os/memory/x_digest.log.md
- **Severity:** MEDIUM
- **Finding:** The append-only digest log is committed as workspace data even though it is runtime output that will grow indefinitely and create noisy diffs.
- **Suggestion:** Store digest logs in an ignored runtime logs directory and keep the repo limited to code plus optional sample output.
- **Disposition:** PR attempted

### os/memory/x_digest_state.json
- **Severity:** LOW
- **Finding:** The file contains only a mutable cursor (`nextIndex`) and is runtime state rather than reusable project source.
- **Suggestion:** Relocate digest cursor state to an ignored runtime/state directory instead of versioning it.
- **Disposition:** Logged only

### os/memory/x_engagement.log.md
- **Severity:** MEDIUM
- **Finding:** The engagement log is runtime-generated historical data and does not belong in the tracked source tree alongside code.
- **Suggestion:** Write engagement snapshots to an ignored runtime/output path and commit only format documentation or fixtures if needed.
- **Disposition:** PR attempted

### os/memory/x_queue.md
- **Severity:** MEDIUM
- **Finding:** The action queue is operational append-only state stored in-repo, which is inconsistent with keeping queues and ledgers as ephemeral runtime data.
- **Suggestion:** Move the queue file to an ignored runtime/state directory and leave only a documented schema/template in the repo.
- **Disposition:** PR attempted

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** This file duplicates the same preflight/header/logging skeleton behavior already implemented by `skills/inbox_daily_triage_wrapper.js`, leaving two near-identical no-op entrypoints to maintain.
- **Suggestion:** Consolidate the inbox skeleton into a single script or make one file the canonical wrapper and delete the duplicate.
- **Disposition:** Logged only

### skills/inbox_daily_triage_wrapper.js
- **Severity:** LOW
- **Finding:** The wrapper is effectively dead code because it never invokes any underlying triage logic and only repeats the common skeleton pattern.
- **Suggestion:** Either wire it to the real triage entrypoint behind an explicit flag/approval gate or remove it to avoid a permanent no-op wrapper.
- **Disposition:** Logged only

### skills/web_repo_audit.js
- **Severity:** MEDIUM
- **Finding:** The script exits early on missing/unsafe `WEB_REPO_PATH` before writing the required memory log entry, which is inconsistent with the local skill contract in `skills/README.md`.
- **Suggestion:** Log the validation failure before exiting or refactor to a shared wrapper that always records a 1–3 line run result.
- **Disposition:** PR opened

### skills/web_repo_audit.js
- **Severity:** LOW
- **Finding:** `repoPath` validation is handled after preflight/output setup instead of through the same envelope/config validation pattern used elsewhere, creating split validation paths.
- **Suggestion:** Move repo-path validation into a shared helper or config stage so runtime requirements are checked consistently before execution starts.
- **Disposition:** Logged only

### skills/weekly_healthcheck.js
- **Severity:** LOW
- **Finding:** The healthcheck shells out with hardcoded commands (`free -h`, `df -h /`, `uptime`) rather than using config-driven targets or a shared command wrapper, which limits portability.
- **Suggestion:** Make the probed commands/paths configurable or centralize them in a reusable helper for host diagnostics.
- **Disposition:** Logged only

### skills/pr-review/SKILL.md
- **Severity:** LOW
- **Finding:** The command path is hardcoded to `/root/.openclaw/workspace/scripts/pr-review-job.mjs`, which makes the skill less portable across workspaces and hosts.
- **Suggestion:** Resolve the script path from the workspace root or a relative path anchored to the skill/workspace instead of embedding an absolute host path.
- **Disposition:** Logged only
