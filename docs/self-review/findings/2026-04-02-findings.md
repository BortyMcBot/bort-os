# Self-Review Findings — 2026-04-02

Generated by `self-review-job.mjs` on 2026-04-02T07:11:16.216Z

## Findings

### scripts/arch-drift-check.mjs
- **Severity:** LOW
- **Finding:** WATCH_FILES is hardcoded to only os/preflight.js and os/model-routing.js, so other architecture-relevant enforcement files can drift without being classified by this check.
- **Suggestion:** Move the watched file list into config or expand it to cover the full set of architectural source-of-truth files.
- **Disposition:** Logged only

### scripts/autonomous_execute_queue.mjs
- **Severity:** LOW
- **Finding:** execBortOsTask() and execPersonalWebsiteTask() duplicate large chunks of branch/commit/push/PR workflow logic, which is redundant and will get harder to maintain as more task types are added.
- **Suggestion:** Extract the shared branch/commit/push/PR flow into a helper and keep each task handler focused on repo-specific edits only.
- **Disposition:** Logged only

### scripts/autonomous_generate_queue.mjs
- **Severity:** LOW
- **Finding:** seedQueue() is hardcoded to two specific repos and tasks, so changing autonomous work selection still requires editing code instead of adjusting config or data.
- **Suggestion:** Move the seed task list into a config or data file and keep the script focused on queue generation mechanics.
- **Disposition:** Logged only

### scripts/autonomous_report.mjs
- **Severity:** LOW
- **Finding:** autonomous_report.mjs always attempts a Telegram send even when TELEGRAM_CHAT_ID is unset, which is inconsistent with the guarded notification pattern used elsewhere.
- **Suggestion:** Skip the openclaw message send call entirely when TELEGRAM_CHAT_ID is empty.
- **Disposition:** Logged only

### scripts/autonomous_runner.mjs
- **Severity:** LOW
- **Finding:** autonomous_runner.mjs runs immediately and then every 10 minutes without any lock or singleton guard, so duplicate starts can cause overlapping queue executions.
- **Suggestion:** Add a PID or lockfile mechanism so only one autonomous runner instance can execute at a time.
- **Disposition:** Logged only

### scripts/autonomous_start.mjs
- **Severity:** LOW
- **Finding:** autonomous_start.mjs still hardcodes /root/.openclaw/workspace and absolute child script paths instead of using the shared workspace constant pattern used by other scripts.
- **Suggestion:** Resolve STATE_PATH, LOG_PATH, and child script paths from BORT_WORKSPACE or a shared constants module.
- **Disposition:** Logged only

### scripts/autonomous_stop.mjs
- **Severity:** LOW
- **Finding:** autonomous_stop.mjs also hardcodes /root/.openclaw/workspace and the autonomous_report.mjs path, which is inconsistent with the codebase’s newer workspace-aware pattern.
- **Suggestion:** Build paths from BORT_WORKSPACE or a shared constants module instead of embedding absolute paths.
- **Disposition:** Logged only

### scripts/deploy.mjs
- **Severity:** LOW
- **Finding:** run() and runReadOnly() are functionally identical execSync wrappers, so the second helper is redundant and adds maintenance noise without changing behavior.
- **Suggestion:** Collapse both call paths onto one helper or give the read-only variant distinct enforcement behavior.
- **Disposition:** Logged only

### scripts/export-bort-source.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace for invoked scripts and output paths, which is inconsistent with workspace-aware scripts that honor BORT_WORKSPACE or shared constants.
- **Suggestion:** Resolve the workspace root from BORT_WORKSPACE or a shared constant instead of embedding absolute paths.
- **Disposition:** Logged only

### scripts/export-project-source.mjs
- **Severity:** LOW
- **Finding:** export-project-source.mjs hardcodes workspace, dist, and upload paths instead of following the shared constants/workspace-aware pattern used elsewhere.
- **Suggestion:** Replace embedded absolute paths with BORT_WORKSPACE-derived paths or a shared constants import.
- **Disposition:** Logged only

### scripts/generate-bort-capabilities-md.mjs
- **Severity:** LOW
- **Finding:** generate-bort-capabilities-md.mjs hardcodes snapshot and output paths under /root/.openclaw/workspace, which is inconsistent with workspace-aware script patterns elsewhere.
- **Suggestion:** Resolve source and output paths from BORT_WORKSPACE or a shared constants module.
- **Disposition:** Logged only

### scripts/generate-project-source-core.mjs
- **Severity:** LOW
- **Finding:** generate-project-source-core.mjs hardcodes workspace and config file locations, reducing portability relative to scripts that import shared constants.
- **Suggestion:** Build workspace and config paths from shared configuration instead of absolute literals.
- **Disposition:** Logged only

### scripts/generate-state-of-bort.mjs
- **Severity:** LOW
- **Finding:** The installed-skill section arbitrarily truncates output to 30 entries, which can silently omit available skills from the generated state snapshot.
- **Suggestion:** Make the limit configurable or include all detected skills so the exported state remains complete.
- **Disposition:** Logged only

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** ghJson() is defined but never used, leaving dead helper code in the PR review job.
- **Suggestion:** Remove ghJson() or use it consistently where JSON gh calls are needed.
- **Disposition:** Logged only

### scripts/refresh-project-source.mjs
- **Severity:** LOW
- **Finding:** refresh-project-source.mjs hardcodes absolute child script paths instead of using a shared workspace constant, making it inconsistent with workspace-aware patterns elsewhere.
- **Suggestion:** Resolve child script paths from BORT_WORKSPACE or a shared constants module.
- **Disposition:** Logged only

### scripts/repo_hygiene_check.sh
- **Severity:** LOW
- **Finding:** The dirty-worktree alert path still unconditionally calls openclaw message send even when TELEGRAM_CHAT_ID is empty, which is inconsistent with the guarded notification behavior elsewhere in the script.
- **Suggestion:** Gate that notification behind a non-empty TELEGRAM_CHAT_ID check just like the later branches.
- **Disposition:** Logged only

### scripts/self-review-job.mjs
- **Severity:** LOW
- **Finding:** runWebResearch() still writes /tmp/self-review-research-*.txt for every topic but never uses those files in the downstream call, leaving dead temp-file work in the flow.
- **Suggestion:** Remove the tmpQuery write/unlink path or pass the file to a consumer that actually uses it.
- **Disposition:** Logged only

### scripts/site-improvement-job.mjs
- **Severity:** LOW
- **Finding:** PINCHTAB_PID and PINCHTAB_BASE are declared but never used, leaving dead configuration constants in the site improvement job.
- **Suggestion:** Remove the unused constants or wire them into the Pinchtab control flow.
- **Disposition:** Logged only

### scripts/update-bort-bundle.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace and public/bort-bundle paths, reducing portability relative to the rest of the workspace-rooted tooling.
- **Suggestion:** Read the workspace root and bundle destination from environment or shared constants with safe defaults.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.js
- **Severity:** LOW
- **Finding:** backlog-sweep.js contains its own copy of mapLimit(), which duplicates the same bounded-concurrency helper logic also present in daily-review.js, inbox-audit.js, and unsubscribe.js.
- **Suggestion:** Move mapLimit() into integrations/gmail/lib.js and reuse it across the Gmail scripts that need bounded parallelism.
- **Disposition:** Logged only

### integrations/gmail/daily-review.js
- **Severity:** LOW
- **Finding:** daily-review.js also carries a local mapLimit() implementation, creating redundant concurrency utility code instead of following a shared helper pattern.
- **Suggestion:** Extract mapLimit() into lib.js and import it from daily-review.js, backlog-sweep.js, inbox-audit.js, and unsubscribe.js.
- **Disposition:** Logged only

### integrations/gmail/inbox-audit.js
- **Severity:** LOW
- **Finding:** inbox-audit.js includes another local copy of mapLimit(), so the same worker-pool logic is repeated across multiple Gmail scripts.
- **Suggestion:** Centralize the bounded concurrency helper in lib.js and reuse it from all Gmail scripts that need it.
- **Disposition:** Logged only

### integrations/gmail/unsubscribe.js
- **Severity:** LOW
- **Finding:** unsubscribe.js defines yet another local mapLimit() helper instead of using a shared concurrency utility, adding more duplicate support code to maintain.
- **Suggestion:** Reuse a single shared mapLimit() helper from lib.js across the Gmail integration scripts.
- **Disposition:** Logged only

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** test.js manually reimplements OAuth credential and token loading instead of using the shared loadOAuthClient() helper from lib.js, creating redundant auth logic.
- **Suggestion:** Replace the inline OAuth setup in test.js with loadOAuthClient() so OAuth parsing and validation live in one shared path.
- **Disposition:** Logged only

### integrations/gmail/other-prune.js
- **Severity:** LOW
- **Finding:** other-prune.js is now only a legacy passthrough wrapper around other-prune2.js, leaving a compatibility shim that adds another entrypoint without functional value.
- **Suggestion:** Remove the legacy wrapper after callers are migrated or clearly deprecate it in favor of invoking other-prune2.js directly.
- **Disposition:** Logged only

### integrations/gmail/backlog-state-gobuffs10.json
- **Severity:** LOW
- **Finding:** A runtime backlog state template is still committed in-repo even though the file says real per-account state should live outside version control, which is inconsistent with the runtime-state pattern.
- **Suggestion:** Stop tracking the runtime-state artifact and keep only a documented schema or example outside the live integration path.
- **Disposition:** Logged only

### integrations/gmail/other-prune-state-gobuffs10.json
- **Severity:** LOW
- **Finding:** This committed prune state file is another runtime-state artifact checked into the repo despite its own note saying real state should live outside version control.
- **Suggestion:** Replace the tracked state file with documentation or a non-runtime example template.
- **Disposition:** Logged only

### integrations/gmail/progress-gobuffs10.json
- **Severity:** LOW
- **Finding:** progress-gobuffs10.json is tracked in-repo even though it is labeled as template-only runtime progress state, which conflicts with the intended ignored runtime-state layout.
- **Suggestion:** Remove the tracked runtime progress artifact and keep only a schema or example file if needed.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.log
- **Severity:** LOW
- **Finding:** A runtime log placeholder is committed in-repo even though it explicitly says active logs should live outside version control, which is inconsistent with the runtime-path guidance elsewhere.
- **Suggestion:** Remove the tracked log artifact and document the external runtime log path instead.
- **Disposition:** Logged only

### integrations/gmail/prefs-gobuffs10.json
- **Severity:** LOW
- **Finding:** prefs-gobuffs10.json is committed in-repo despite its own note saying live account prefs should be provided via an explicit runtime prefs path instead of being stored in version control.
- **Suggestion:** Keep only prefs-template.json in the repo and move real account prefs to the ignored runtime state directory.
- **Disposition:** Logged only

### integrations/gmail/daily-summary.sh
- **Severity:** LOW
- **Finding:** daily-summary.sh hardcodes gobuffs10-specific defaults for account slug and secret or prefs paths, making reuse for another inbox depend on environment overrides instead of shared config.
- **Suggestion:** Move per-account defaults into a small config file or account-resolution helper rather than embedding gobuffs10-specific defaults in the script.
- **Disposition:** Logged only

### integrations/gmail/status-ping.sh
- **Severity:** LOW
- **Finding:** status-ping.sh also hardcodes gobuffs10-specific credential, token, and state defaults, which makes the operational status flow less portable across Gmail accounts.
- **Suggestion:** Centralize per-account defaults in config and have the script resolve paths from the selected account slug.
- **Disposition:** Logged only

### integrations/gmail/sweep-runner.sh
- **Severity:** LOW
- **Finding:** sweep-runner.sh hardcodes gobuffs10-specific credential, token, prefs, and state defaults, which is inconsistent with a config-driven multi-account integration pattern.
- **Suggestion:** Resolve account-specific paths from a shared config or account slug instead of embedding gobuffs10 defaults directly in the script.
- **Disposition:** Logged only

### integrations/gmail/README.md
- **Severity:** LOW
- **Finding:** README.md still centers a local credentials.json or token.json workflow inside integrations/gmail, while RUNBOOK.md and the newer scripts assume secrets and runtime state live under ~/.openclaw paths.
- **Suggestion:** Update the README so the ~/.openclaw secret and state layout is the primary documented workflow and local files are framed as bootstrap-only.
- **Disposition:** Logged only

### os/x_auth.js
- **Severity:** MEDIUM
- **Finding:** x_auth.js references OPENCLAW_CONFIG_PATH in its success log line but never defines or imports that constant, which can crash the script after a successful token exchange.
- **Suggestion:** Import OPENCLAW_CONFIG_PATH from os/constants.js or log the literal config path from the same shared source used elsewhere.
- **Disposition:** PR attempted

### os/x_call.js
- **Severity:** LOW
- **Finding:** x_call.js duplicates the same OpenClaw env-var load/update file-lock logic found in x_auth.js instead of sharing one credential/config helper.
- **Suggestion:** Extract the OpenClaw config read/write helpers into a shared module and reuse them from both x_call.js and x_auth.js.
- **Disposition:** Logged only

### os/model-routing.js
- **Severity:** LOW
- **Finding:** ROUTES repeats nearly the same Codex-first fallback chain in every category, creating redundant routing tables that are harder to keep aligned.
- **Suggestion:** Factor the shared model chain into a base constant and only override categories that truly differ.
- **Disposition:** Logged only

### os/model-routing_selftest.js
- **Severity:** LOW
- **Finding:** model-routing_selftest.js reports expectedModel from inventory().routes.default[0] for every case even when category-specific routes are being tested, which weakens the value of the self-test output.
- **Suggestion:** Derive the expected model per test case from the same category routing logic being exercised instead of always labeling the default route as expected.
- **Disposition:** Logged only

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** social_ops_execute_x.js loads a configurable bio value but then always skips the bio-update step, leaving dead config and unused data in the flow.
- **Suggestion:** Remove the unused bio configuration or implement the supported bio-update path so the config is not dead weight.
- **Disposition:** Logged only

### os/social_ops_follow_remaining.js
- **Severity:** LOW
- **Finding:** social_ops_follow_remaining.js hardcodes a default target list in code, which makes follow policy changes require edits instead of configuration.
- **Suggestion:** Move the default targets into a config file or shared policy source and keep the script focused on execution.
- **Disposition:** Logged only

### os/state_report.js
- **Severity:** LOW
- **Finding:** state_report.js still falls back to parsing legacy memory/hats.md even though preflight.js and hat-profiles.json are now the clearer source-of-truth path.
- **Suggestion:** Drop the legacy markdown fallback or clearly isolate it behind a compatibility-only path to keep the source of truth consistent.
- **Disposition:** Logged only

### os/help.js
- **Severity:** LOW
- **Finding:** help.js also includes a legacy memory/hats.md parsing fallback, which is inconsistent with the newer hat-profiles.json and preflight-driven source-of-truth pattern.
- **Suggestion:** Prefer hat-profiles.json and exported preflight metadata exclusively, or gate the markdown parsing path as explicit backward compatibility.
- **Disposition:** Logged only

### os/preflight.js
- **Severity:** LOW
- **Finding:** loadHatProfiles() embeds a full hardcoded fallback copy of the hat definitions, duplicating policy data that already lives in os/hat-profiles.json.
- **Suggestion:** Move the fallback into a single shared default profile source or fail with a clearer error instead of maintaining two policy copies.
- **Disposition:** Logged only

### os/memory-log.js
- **Severity:** LOW
- **Finding:** hatFilePath() hardcodes a partial hat-to-file map and throws on unknown hats, which will require code edits whenever a new hat is added in hat-profiles.json.
- **Suggestion:** Derive log file naming from hat metadata or use a deterministic fallback filename so new hats do not require code changes.
- **Disposition:** Logged only

### os/models_check.js
- **Severity:** LOW
- **Finding:** recommendRouting() defines recResearch using openrouter/openai/o3-mini-high, but model-routing.js intentionally filters OpenRouter OpenAI models unless explicitly requested, making this recommendation inconsistent with actual routing policy.
- **Suggestion:** Limit recommendations to models the current routing policy can really select or annotate the recommendation as requiring an explicit policy change.
- **Disposition:** Logged only

### os/x_daily_post.js
- **Severity:** LOW
- **Finding:** x_daily_post.js imports fs, execSync, and RESULTS_PATH but only uses fs and RESULTS_PATH, leaving at least one unused child_process import in the script.
- **Suggestion:** Remove the unused imports to keep the job implementation smaller and easier to audit.
- **Disposition:** Logged only

### os/x_digest_job.js
- **Severity:** LOW
- **Finding:** x_digest_job.js hardcodes the curated allowlist in code and separately hardcodes the same list again in digest output text, creating duplicated policy data that can drift.
- **Suggestion:** Store the curated account list once and render both execution and logging from that single source.
- **Disposition:** Logged only

### os/x_pricing.json
- **Severity:** LOW
- **Finding:** x_pricing.json is a manual-placeholder status file rather than machine-usable pricing data, so x_call.js falls back to defaults instead of having a real config-driven pricing source.
- **Suggestion:** Replace the placeholder document with an actual structured pricing table or rename it to make clear that it is informational only and not active config.
- **Disposition:** Logged only

### os/memory/x_budget_ledger.json
- **Severity:** LOW
- **Finding:** This committed ledger file is explicitly marked template-only runtime state, which is inconsistent with the intended pattern of keeping live X budget state outside version control.
- **Suggestion:** Remove the tracked runtime ledger artifact and keep only a documented schema or example in a non-runtime location.
- **Disposition:** Logged only

### os/memory/x_digest.log.md
- **Severity:** LOW
- **Finding:** The digest log placeholder is committed in-repo even though it says active runtime digest logs should live outside version control, which conflicts with the runtime-state pattern.
- **Suggestion:** Stop tracking the runtime log artifact and document the ignored external log path instead.
- **Disposition:** Logged only

### os/memory/x_digest_state.json
- **Severity:** LOW
- **Finding:** This digest cursor file is another tracked template-only runtime state artifact despite its own note saying real state should live outside version control.
- **Suggestion:** Replace the tracked state file with documentation or a clearly non-runtime example template.
- **Disposition:** Logged only

### os/memory/x_engagement.log.md
- **Severity:** LOW
- **Finding:** The engagement log placeholder is committed in-repo even though the file says active runtime output should live in an ignored runtime path, which is inconsistent with the codebase’s runtime-state guidance.
- **Suggestion:** Remove the tracked runtime log placeholder and keep only documentation of the external runtime output path.
- **Disposition:** Logged only

### os/memory/x_queue.md
- **Severity:** LOW
- **Finding:** x_queue.md is tracked in-repo even though it is labeled as a schema/template placeholder and says active runtime queue state should live outside version control.
- **Suggestion:** Keep only a schema/example in documentation and move live queue state to the ignored runtime state directory.
- **Disposition:** Logged only

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** inbox_daily_triage.js is a placeholder that hardcodes a fixed skeleton envelope and never calls the underlying Gmail review flow, leaving dead-end skill logic in place.
- **Suggestion:** Either wire the skill to the real triage implementation behind the intended approval guard or remove the placeholder entrypoint until it does useful work.
- **Disposition:** Logged only

### skills/inbox_daily_triage_wrapper.js
- **Severity:** LOW
- **Finding:** inbox_daily_triage_wrapper.js is another no-op placeholder that duplicates the same envelope, logging, and messaging pattern as inbox_daily_triage.js without adding functional behavior.
- **Suggestion:** Consolidate the wrapper and skeleton into a single entrypoint or implement the wrapper’s intended delegation path so the duplication serves a purpose.
- **Disposition:** Logged only

### skills/web_repo_audit.js
- **Severity:** LOW
- **Finding:** web_repo_audit.js manually duplicates the same preflight, executionHeader, appendLog, appendHatLog, and timestamp scaffolding pattern used across the local skill scripts.
- **Suggestion:** Extract the shared skill bootstrap/logging flow into a small helper so each skill only defines its envelope and task-specific logic.
- **Disposition:** Logged only

### skills/weekly_healthcheck.js
- **Severity:** LOW
- **Finding:** weekly_healthcheck.js repeats the same preflight and memory-log scaffolding used by the other local skills, which creates redundant boilerplate across the skill directory.
- **Suggestion:** Move the common envelope validation and logging wrapper into a shared helper module and reuse it across the local skills.
- **Disposition:** Logged only

### skills/README.md
- **Severity:** LOW
- **Finding:** README.md states these scripts must be read-only by default, but the directory still contains multiple skeleton/no-op entrypoints that are documented like real skills, which is inconsistent with a production-ready skill catalog.
- **Suggestion:** Mark placeholder skills explicitly as stubs in the README or remove them from the advertised skill set until they provide real functionality.
- **Disposition:** Logged only

### skills/pr-review/SKILL.md
- **Severity:** LOW
- **Finding:** SKILL.md hardcodes the absolute workspace path to scripts/pr-review-job.mjs instead of following the codebase’s newer workspace-aware or relative-path pattern.
- **Suggestion:** Resolve the script path from BORT_WORKSPACE or document it relative to the skill/workspace root instead of embedding /root/.openclaw/workspace.
- **Disposition:** Logged only
