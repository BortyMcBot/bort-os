# Self-Review Findings — 2026-03-28

Generated by `self-review-job.mjs` on 2026-03-28T13:10:45.172Z

## Findings

### scripts/self-review-job.mjs
- **Severity:** LOW
- **Finding:** runWebResearch() still writes /tmp/self-review-research-*.txt for every topic but never uses those files in the downstream call, leaving dead temp-file work in the flow.
- **Suggestion:** Remove the tmpQuery write/unlink path or pass the file to a consumer that actually uses it.
- **Disposition:** Logged only

### scripts/autonomous_execute_queue.mjs
- **Severity:** LOW
- **Finding:** execBortOsTask() and execPersonalWebsiteTask() still duplicate large chunks of branch/commit/push/PR workflow logic, which is redundant and will get harder to maintain as more task types are added.
- **Suggestion:** Extract the shared branch/commit/push/PR flow into a helper and keep each task handler focused on repo-specific edits only.
- **Disposition:** Logged only

### scripts/autonomous_generate_queue.mjs
- **Severity:** LOW
- **Finding:** seedQueue() is hardcoded to two specific repos and tasks, so changing autonomous work selection still requires editing code instead of adjusting config or data.
- **Suggestion:** Move the seed task list into a config or data file and keep the script focused on queue generation mechanics.
- **Disposition:** Logged only

### scripts/deploy.mjs
- **Severity:** LOW
- **Finding:** run() and runReadOnly() are functionally identical execSync wrappers, so the second helper is redundant and adds maintenance noise without changing behavior.
- **Suggestion:** Collapse both call paths onto one helper or give the read-only variant distinct enforcement behavior.
- **Disposition:** Logged only

### scripts/export-bort-source.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace for invoked scripts and output paths, which is inconsistent with workspace-aware scripts that honor BORT_WORKSPACE or shared constants.
- **Suggestion:** Resolve the workspace root from BORT_WORKSPACE or a shared constant instead of embedding absolute paths.
- **Disposition:** Logged only

### scripts/update-bort-bundle.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace and public/bort-bundle paths, reducing portability relative to the rest of the workspace-rooted tooling.
- **Suggestion:** Read the workspace root and bundle destination from environment or shared constants with safe defaults.
- **Disposition:** Logged only

### scripts/generate-state-of-bort.mjs
- **Severity:** LOW
- **Finding:** The installed-skill section arbitrarily truncates output to 30 entries, which can silently omit available skills from the generated state snapshot.
- **Suggestion:** Make the limit configurable or include all detected skills so the exported state remains complete.
- **Disposition:** Logged only

### scripts/arch-drift-check.mjs
- **Severity:** LOW
- **Finding:** WATCH_FILES is hardcoded to only os/preflight.js and os/model-routing.js, so other architecture-relevant enforcement files can drift without being classified by this check.
- **Suggestion:** Move the watched file list into config or expand it to cover the full set of architectural source-of-truth files.
- **Disposition:** Logged only

### scripts/autonomous_start.mjs
- **Severity:** LOW
- **Finding:** autonomous_start.mjs still hardcodes /root/.openclaw/workspace and absolute child script paths instead of using the shared workspace constant pattern used by other updated scripts.
- **Suggestion:** Resolve STATE_PATH/LOG_PATH and child script paths from BORT_WORKSPACE or a shared constants module.
- **Disposition:** Logged only

### scripts/autonomous_stop.mjs
- **Severity:** LOW
- **Finding:** autonomous_stop.mjs also hardcodes /root/.openclaw/workspace and the autonomous_report.mjs path, which is inconsistent with the codebase’s newer workspace-aware pattern.
- **Suggestion:** Build paths from BORT_WORKSPACE or a shared constants module instead of embedding absolute paths.
- **Disposition:** Logged only

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** test.js manually reimplements OAuth credential and token loading instead of using the shared loadOAuthClient() helper from lib.js, creating redundant auth logic to maintain.
- **Suggestion:** Replace the inline OAuth setup in test.js with loadOAuthClient() so auth parsing and validation live in one shared path.
- **Disposition:** Logged only

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** status-ping.sh now reports processed/updated as state-missing when the state file is absent, but it still unconditionally runs a Node require() against $STATE for TOP, which can fail and mask the intended clearer missing-state behavior.
- **Suggestion:** Guard the TOP calculation behind the same state-file existence check and emit an explicit placeholder when no state file exists yet.
- **Disposition:** PR attempted

### integrations/gmail/backlog-sweep.js
- **Severity:** LOW
- **Finding:** mapLimit() uses a shared next++ counter across concurrent workers, which is safe in practice here but duplicates the same ad hoc bounded-concurrency helper already copied into daily-review.js and inbox-audit.js.
- **Suggestion:** Move the bounded worker-pool helper into lib.js and reuse it across the Gmail scripts instead of duplicating it per file.
- **Disposition:** Logged only

### integrations/gmail/daily-review.js
- **Severity:** LOW
- **Finding:** daily-review.js contains its own copy of the same mapLimit() helper now also present in backlog-sweep.js and inbox-audit.js, creating redundant concurrency utility logic.
- **Suggestion:** Extract mapLimit() into lib.js and have the Gmail scripts share the same helper.
- **Disposition:** Logged only

### integrations/gmail/inbox-audit.js
- **Severity:** LOW
- **Finding:** inbox-audit.js also carries a local copy of mapLimit(), so the new concurrency improvement introduced another duplicated helper instead of following a shared utility pattern.
- **Suggestion:** Centralize the bounded-concurrency helper in lib.js and reuse it from backlog-sweep.js, daily-review.js, and inbox-audit.js.
- **Disposition:** Logged only

### integrations/gmail/backlog-state-gobuffs10.json
- **Severity:** LOW
- **Finding:** A runtime backlog state file template is still committed in-repo even though the file itself says real per-account state should live outside version control, which is inconsistent with the runtime-state pattern.
- **Suggestion:** Remove the tracked runtime-state artifact and keep only a documented example or template outside the live integration file set.
- **Disposition:** Logged only

### integrations/gmail/other-prune-state-gobuffs10.json
- **Severity:** LOW
- **Finding:** This committed prune state file is another runtime-state artifact checked into the repo despite its own note saying real state should live outside version control.
- **Suggestion:** Replace the tracked state file with documentation or a clearly non-runtime example/template.
- **Disposition:** Logged only

### integrations/gmail/progress-gobuffs10.json
- **Severity:** LOW
- **Finding:** progress-gobuffs10.json is tracked in-repo even though it is labeled as template-only runtime progress state, which conflicts with the intended ignored runtime-state layout.
- **Suggestion:** Stop tracking the runtime progress artifact and keep only a non-runtime schema/example file.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.log
- **Severity:** LOW
- **Finding:** A runtime log placeholder is committed in-repo even though it explicitly says active logs should live outside version control, which is inconsistent with the rest of the runtime-path guidance.
- **Suggestion:** Remove the tracked log artifact and document the external runtime log path instead.
- **Disposition:** Logged only

### integrations/gmail/daily-summary.sh
- **Severity:** LOW
- **Finding:** daily-summary.sh hardcodes gobuffs10-specific defaults for account slug and secret/prefs paths, making reuse for another inbox dependent on environment overrides instead of config.
- **Suggestion:** Move per-account defaults into a small shared config or account-selection helper rather than embedding gobuffs10-specific defaults in the script.
- **Disposition:** Logged only

### integrations/gmail/sweep-runner.sh
- **Severity:** LOW
- **Finding:** sweep-runner.sh also hardcodes gobuffs10-specific credential, token, prefs, and state defaults, which makes the runner less portable across Gmail accounts.
- **Suggestion:** Centralize per-account defaults in config and have the runner resolve paths from the selected account slug.
- **Disposition:** Logged only

### integrations/gmail/README.md
- **Severity:** LOW
- **Finding:** README.md still centers a local token.json/credentials.json workflow inside integrations/gmail, while RUNBOOK.md and the newer scripts assume secrets and runtime state live under ~/.openclaw paths.
- **Suggestion:** Update the README so the runtime secret/state paths are the primary documented workflow and the local-file path is clearly secondary/bootstrap-only.
- **Disposition:** Logged only

### os/x_auth.js
- **Severity:** HIGH
- **Finding:** x_auth.js now inlines openclaw.json env-var read/write helpers while os/x_call.js still maintains its own separate config persistence path, leaving duplicated high-risk config mutation logic that can drift.
- **Suggestion:** Consolidate OpenClaw config env-var loading/updating into one shared helper used by both x_auth.js and x_call.js.
- **Disposition:** PR attempted

### os/help.js
- **Severity:** MEDIUM
- **Finding:** deriveSkills() only scans /usr/lib/node_modules/openclaw/skills and ignores workspace-local skills, which is inconsistent with the environment’s mixed global-plus-workspace skill model.
- **Suggestion:** Include workspace skills alongside installed global skills so help output matches the actual available skill set.
- **Disposition:** PR attempted

### os/preflight.js
- **Severity:** LOW
- **Finding:** loadHatProfiles() still embeds a large fallback hat-policy object that duplicates hat-profiles.json, creating redundant policy definitions that can drift from the JSON source of truth.
- **Suggestion:** Keep the fallback minimal or fail clearly when hat-profiles.json is invalid instead of duplicating the full hat table in code.
- **Disposition:** Logged only

### os/model-routing.js
- **Severity:** LOW
- **Finding:** the lightweight, research_web, social_drafting, spec_large, and default route arrays are currently identical, so the routing table carries redundant categories without behavior differences.
- **Suggestion:** Collapse identical route chains into shared constants or make the category-specific differences explicit.
- **Disposition:** Logged only

### os/models_inventory.js
- **Severity:** LOW
- **Finding:** The heading "OpenAI fallback chain" is misleading because inv.routes.default includes non-OpenAI entries like OpenRouter Claude, which can misstate the actual routing chain.
- **Suggestion:** Rename the section to something like "default routing chain" or print provider-specific chains separately.
- **Disposition:** Logged only

### os/state_report.js
- **Severity:** MEDIUM
- **Finding:** state_report.js still derives hats from memory/hats.md instead of hat-profiles.json/preflight exports, so its hat list can drift from the enforced runtime source of truth.
- **Suggestion:** Load hats from hat-profiles.json or preflight exports so the report matches current enforcement.
- **Disposition:** PR attempted

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** DEFAULT_CONFIG.bio is still loaded and returned by loadConfig() but never used because the script always skips the bio update step, leaving dead configuration data in the flow.
- **Suggestion:** Remove the unused bio config or implement the bio-update path behind an explicit supported flow.
- **Disposition:** Logged only

### os/x_budget_checks.js
- **Severity:** LOW
- **Finding:** The blocked-action check depends on a fallback path that records DAILY_CAP_USD only when the first guardOrQueue() unexpectedly succeeds, making the test less deterministic than the rest of the harness.
- **Suggestion:** Fill the ledger to the cap explicitly before the blocked assertion so the test does not rely on branchy setup behavior.
- **Disposition:** Logged only

### os/x_pricing.json
- **Severity:** LOW
- **Finding:** x_pricing.json still contains only manual_required metadata and source URLs, so pricing remains effectively hardcoded in code instead of being truly config-driven.
- **Suggestion:** Populate this file with the normalized route pricing data expected by x_call.js or remove the partial config until authoritative pricing data is available.
- **Disposition:** Logged only

### os/memory/x_budget_ledger.json
- **Severity:** LOW
- **Finding:** This committed ledger file is explicitly marked template-only runtime state, which is inconsistent with the stated pattern that live X budget state should live outside version control.
- **Suggestion:** Remove the tracked runtime-state artifact and keep only a documented template/example in a non-runtime location.
- **Disposition:** Logged only

### os/memory/x_digest.log.md
- **Severity:** LOW
- **Finding:** A runtime digest log placeholder is committed in-repo even though the file says active digest logs should live in an ignored runtime path outside version control.
- **Suggestion:** Stop tracking the runtime log artifact and replace it with documentation of the external runtime log location.
- **Disposition:** Logged only

### os/memory/x_digest_state.json
- **Severity:** LOW
- **Finding:** This tracked digest cursor file is another template-only runtime state artifact that conflicts with the documented runtime-path convention.
- **Suggestion:** Remove the committed state file and keep only a non-runtime template or schema reference.
- **Disposition:** Logged only

### os/memory/x_engagement.log.md
- **Severity:** LOW
- **Finding:** The engagement log placeholder is stored in-repo despite stating that real engagement output belongs in an ignored runtime path, which is inconsistent with the intended storage model.
- **Suggestion:** Keep only documentation or a sample format in-repo and move real engagement logs fully to runtime storage.
- **Disposition:** Logged only

### os/memory/x_queue.md
- **Severity:** LOW
- **Finding:** x_queue.md is checked in as a placeholder even though it represents active append-only queue state in the running system, making it inconsistent with the external runtime-state guidance used by the neighboring files.
- **Suggestion:** Replace the tracked queue file with schema documentation and write the live queue to an ignored runtime state path instead.
- **Disposition:** Logged only

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** This file is still a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **Suggestion:** Either implement the promised dry-run integration path or remove/replace the stub with documentation until real behavior exists.
- **Disposition:** Logged only

### skills/inbox_daily_triage_wrapper.js
- **Severity:** LOW
- **Finding:** This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying work, creating redundant no-op logic.
- **Suggestion:** Consolidate the wrapper and the triage skeleton into a single entrypoint or make the wrapper actually invoke the target path behind an explicit flag.
- **Disposition:** Logged only

### skills/web_repo_audit.js
- **Severity:** LOW
- **Finding:** Repo audit failures print stderr and exit without appending a failure log entry, which is inconsistent with the other skeletons’ memory-log pattern.
- **Suggestion:** Log the failure outcome to memory/logs.md and the hat log before exiting on command errors.
- **Disposition:** Logged only

### skills/weekly_healthcheck.js
- **Severity:** LOW
- **Finding:** The script shells out to hardcoded commands free, df, and uptime directly, which makes the health snapshot behavior less configurable than the rest of the workspace tooling.
- **Suggestion:** Move the command set into config/constants or a small helper so the snapshot inputs can be adjusted without code edits.
- **Disposition:** Logged only

### skills/README.md
- **Severity:** LOW
- **Finding:** The README promises lightweight read-only skill scripts in general terms, but the current directory mostly contains placeholders/no-op skeletons, which can mislead operators about implementation maturity.
- **Suggestion:** Clarify in the README which entries are skeletons/placeholders versus actually implemented skills.
- **Disposition:** Logged only

### skills/pr-review/SKILL.md
- **Severity:** LOW
- **Finding:** The command path is hardcoded to /root/.openclaw/workspace/scripts/pr-review-job.mjs, which is inconsistent with more portable workspace-relative patterns used elsewhere.
- **Suggestion:** Resolve the script path from the workspace root or a shared constant instead of embedding an absolute path in the skill.
- **Disposition:** Logged only
