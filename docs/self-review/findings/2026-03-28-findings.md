# Self-Review Findings — 2026-03-28

Generated by `self-review-job.mjs` on 2026-03-28T07:19:12.785Z

## Findings

### scripts/site-improvement-job.mjs
- **Severity:** HIGH
- **Finding:** createPRs() validates changed paths against expectedPaths but expectedPaths is never defined, so any PR-ready finding will throw at runtime before staging.
- **Suggestion:** Reintroduce explicit expected-path extraction from suggested_change and fail early with a clear message when no expected path can be derived.
- **Disposition:** PR attempted

### scripts/self-review-job.mjs
- **Severity:** MEDIUM
- **Finding:** createFixPRs() writes PR body files into unique temp directories and unlinks only body.md, leaving the temp directories orphaned after each run.
- **Suggestion:** Remove path.dirname(bodyPath) after PR creation or keep a single per-run temp workspace and clean it once.
- **Disposition:** PR attempted

### scripts/self-review-job.mjs
- **Severity:** LOW
- **Finding:** runWebResearch() writes /tmp/self-review-research-*.txt for every topic but never uses those files for prompting or later processing, making the temp-file work dead code.
- **Suggestion:** Remove the tmpQuery write/unlink path or actually consume the file in the downstream call.
- **Disposition:** Logged only

### scripts/autonomous_report.mjs
- **Severity:** MEDIUM
- **Finding:** collectPrs() checks personal-site PRs under WORKSPACE/tmp/site-review/repo while site-improvement-job.mjs clones into /tmp/site-review/repo by default, so site PR reporting will usually be skipped even when PRs exist.
- **Suggestion:** Read the site review repo path from the same environment/config used by site-improvement-job.mjs instead of hardcoding a different default.
- **Disposition:** PR attempted

### scripts/autonomous_start.mjs
- **Severity:** MEDIUM
- **Finding:** autonomous_start.mjs always spawns detached guard/runner/stop processes without checking existing state or active worker processes, so repeated starts can create duplicate autonomous loops.
- **Suggestion:** Refuse to start when autonomous_state.json is already active or track child PIDs and avoid spawning duplicates.
- **Disposition:** PR attempted

### scripts/deploy.mjs
- **Severity:** LOW
- **Finding:** run() and runReadOnly() are functionally identical wrappers around execSync, which duplicates logic without changing behavior.
- **Suggestion:** Consolidate both call sites onto a single helper or give the read-only variant distinct safety behavior.
- **Disposition:** Logged only

### scripts/repo_hygiene_check.sh
- **Severity:** LOW
- **Finding:** The script unconditionally calls openclaw message send in the dirty/non-allowlist branch even when TELEGRAM_CHAT_ID is empty, unlike its guarded notification behavior elsewhere.
- **Suggestion:** Gate that send behind a non-empty TELEGRAM_CHAT_ID check for consistency with the other notification paths.
- **Disposition:** Logged only

### scripts/export-bort-source.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace for both invoked commands and output paths, which is inconsistent with workspace-aware scripts that honor BORT_WORKSPACE/constants.
- **Suggestion:** Resolve the workspace root from BORT_WORKSPACE or a shared constant instead of embedding absolute paths.
- **Disposition:** Logged only

### scripts/update-bort-bundle.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace and public/bort-bundle paths, making it less portable than other workspace-rooted tooling.
- **Suggestion:** Read the workspace root and public bundle destination from environment/config with safe defaults.
- **Disposition:** Logged only

### scripts/autonomous_generate_queue.mjs
- **Severity:** LOW
- **Finding:** seedQueue() is entirely hardcoded to two specific tasks and repos, so queue contents require code edits instead of policy/config updates.
- **Suggestion:** Move the seed task list into a config or data file and keep the script focused on queue generation mechanics.
- **Disposition:** Logged only

### scripts/autonomous_execute_queue.mjs
- **Severity:** LOW
- **Finding:** execBortOsTask() and execPersonalWebsiteTask() contain large inlined task-specific implementations, which duplicates branch/commit/PR flow and does not scale as more autonomous tasks are added.
- **Suggestion:** Extract the shared branch-commit-push-PR workflow into a helper and keep task handlers limited to repo-specific edits.
- **Disposition:** Logged only

### scripts/generate-state-of-bort.mjs
- **Severity:** LOW
- **Finding:** The installed-skill listing arbitrarily truncates to 30 entries, which can silently omit available skills from the generated snapshot.
- **Suggestion:** Make the listing limit configurable or include all skills so the exported state stays complete.
- **Disposition:** Logged only

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** test.js manually reimplements OAuth credential loading instead of using the shared loadOAuthClient() helper from lib.js, creating redundant logic and a second parsing path to maintain.
- **Suggestion:** Replace the inline credential/token loading with loadOAuthClient() so OAuth setup is validated consistently in one place.
- **Disposition:** Logged only

### integrations/gmail/inbox-audit.js
- **Severity:** MEDIUM
- **Finding:** scanTopSenders() fetches every thread's metadata sequentially, which makes large sender scans unnecessarily slow despite already having retry/backoff infrastructure.
- **Suggestion:** Batch thread metadata fetches with a small concurrency limit to reduce audit runtime without spiking API pressure.
- **Disposition:** PR attempted

### integrations/gmail/inbox-audit.js
- **Severity:** LOW
- **Finding:** countThreads() and scanTopSenders() pass a string as the second argument to withBackoff(), but lib.js expects an options object, so the extra context is silently ignored.
- **Suggestion:** Remove the unused string argument or extend withBackoff() to accept structured context explicitly for logging/debugging.
- **Disposition:** Logged only

### integrations/gmail/daily-review.js
- **Severity:** MEDIUM
- **Finding:** The script processes each unread thread sequentially and performs one threads.get plus one threads.modify per item with no concurrency, which can make daily triage slower than necessary on larger inboxes.
- **Suggestion:** Use a small bounded concurrency worker pool for per-thread operations while preserving backoff behavior.
- **Disposition:** PR attempted

### integrations/gmail/backlog-sweep.js
- **Severity:** MEDIUM
- **Finding:** The backlog sweep also handles threads strictly one-by-one, so long backlog runs can be much slower than necessary even though each thread is independent work.
- **Suggestion:** Add a conservative concurrency limit for per-thread get/modify operations to improve throughput without overwhelming the Gmail API.
- **Disposition:** PR attempted

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** status-ping.sh assumes the state file can be require()'d from Node, but when the state file does not exist the inline Node command exits non-zero and the script quietly falls back to 0/empty values, masking missing-state errors as normal status.
- **Suggestion:** Check for the state file explicitly and surface a clearer status when no sweep state exists yet instead of silently treating it as zero progress.
- **Disposition:** PR attempted

### integrations/gmail/backlog-state-gobuffs10.json
- **Severity:** LOW
- **Finding:** A runtime backlog state file template is committed in-repo even though the file itself says real per-account state should live outside version control, which is inconsistent with the stated runtime-state pattern.
- **Suggestion:** Remove the committed runtime-state artifact and keep only a documented template or example path outside tracked integration files.
- **Disposition:** Logged only

### integrations/gmail/other-prune-state-gobuffs10.json
- **Severity:** LOW
- **Finding:** This committed prune state file is another runtime-state artifact checked into the repo despite its own note saying real state should live outside version control.
- **Suggestion:** Replace the tracked state file with documentation or an example/template file that is clearly non-runtime.
- **Disposition:** Logged only

### integrations/gmail/progress-gobuffs10.json
- **Severity:** LOW
- **Finding:** progress-gobuffs10.json is tracked in-repo even though it is labeled as template-only runtime progress state, which conflicts with the intended ignored runtime-state layout.
- **Suggestion:** Stop tracking the runtime progress artifact and keep only a non-runtime template/example.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.log
- **Severity:** LOW
- **Finding:** A runtime log placeholder is committed in-repo even though it explicitly says active logs should live outside version control, which is inconsistent with the rest of the runtime-path guidance.
- **Suggestion:** Remove the tracked log artifact and document the external runtime log path instead.
- **Disposition:** Logged only

### integrations/gmail/daily-summary.sh
- **Severity:** LOW
- **Finding:** daily-summary.sh hardcodes the default account slug gobuffs10 and matching secret/prefs paths, so reusing it for another inbox requires script-level environment overrides everywhere.
- **Suggestion:** Move the account defaults into a small config file or shared account-selection helper instead of embedding gobuffs10-specific defaults in the script.
- **Disposition:** Logged only

### integrations/gmail/sweep-runner.sh
- **Severity:** LOW
- **Finding:** sweep-runner.sh also hardcodes gobuffs10-specific credential, token, prefs, and state defaults, which makes the runner less portable across Gmail accounts.
- **Suggestion:** Centralize per-account defaults in config and have the runner resolve paths from the selected account slug.
- **Disposition:** Logged only

### integrations/gmail/README.md
- **Severity:** LOW
- **Finding:** README.md still instructs users to create token.json and credentials.json directly inside integrations/gmail, while RUNBOOK.md and the newer scripts assume secrets and runtime state live under ~/.openclaw paths.
- **Suggestion:** Update the README to make the runtime secret/state paths the primary workflow so documentation matches the active script behavior.
- **Disposition:** Logged only

### os/model-routing_selftest.js
- **Severity:** HIGH
- **Finding:** model-routing_selftest.js calls inventory() to compute defaultExpectedModel but never imports inventory from os/model-routing.js, so the self-test crashes immediately with a ReferenceError.
- **Suggestion:** Import inventory alongside routeModel or stop referencing inventory in the self-test output.
- **Disposition:** PR attempted

### os/x_budget_checks.js
- **Severity:** HIGH
- **Finding:** x_budget_checks.js calls dailySpendUsd() and enforceBudgetOrQueue(), but os/x_budget.js exports neither function, making the test harness unusable as written.
- **Suggestion:** Update the checks to use the current x_budget API or restore the expected exported functions with matching names.
- **Disposition:** PR attempted

### os/help.js
- **Severity:** MEDIUM
- **Finding:** help.js prefers deriving hats from memory/hats.md and only partially from hat-profiles.json, which is inconsistent with the codebase’s newer hat-profiles.json source of truth and can show stale help output.
- **Suggestion:** Read hats and required fields directly from hat-profiles.json/preflight exports instead of falling back to memory/hats.md first.
- **Disposition:** PR attempted

### os/preflight.js
- **Severity:** MEDIUM
- **Finding:** runProjectSourceCheck() hardcodes /root/.openclaw/workspace/scripts/run-project-source-check.mjs instead of resolving from BORT_WORKSPACE or __dirname, reducing portability relative to other workspace-aware modules.
- **Suggestion:** Build the script path from a shared workspace constant or a path relative to the current file.
- **Disposition:** PR attempted

### os/preflight.js
- **Severity:** LOW
- **Finding:** loadHatProfiles() embeds a full fallback hat-policy object that duplicates hat-profiles.json, creating redundant policy definitions that can drift from the JSON source.
- **Suggestion:** Keep a minimal fallback or fail clearly when hat-profiles.json is invalid instead of duplicating the full policy table in code.
- **Disposition:** Logged only

### os/model-routing.js
- **Severity:** LOW
- **Finding:** The lightweight, research_web, social_drafting, spec_large, and default route arrays are currently identical, so the routing table carries redundant categories without behavior differences.
- **Suggestion:** Consolidate identical route lists into shared constants or make category-specific differences explicit.
- **Disposition:** Logged only

### os/models_check.js
- **Severity:** MEDIUM
- **Finding:** models_check.js recommends openrouter/google/gemini-2.5-flash-lite and openrouter/openai/o3-mini-high, but those model IDs are not present in the local allowlist/inventory path, so its routing recommendations can drift from actual routable models.
- **Suggestion:** Generate recommendations only from models that also exist in model-routing inventory or a shared allowlist source.
- **Disposition:** PR attempted

### os/state_report.js
- **Severity:** LOW
- **Finding:** state_report.js derives hats from memory/hats.md while the rest of the OS now reads hat-profiles.json, making the report inconsistent with the established canonical policy source.
- **Suggestion:** Load the hat list from hat-profiles.json or from preflight exports so the report matches runtime enforcement.
- **Disposition:** Logged only

### os/models_inventory.js
- **Severity:** LOW
- **Finding:** The heading "OpenAI fallback chain" is misleading because inv.routes.default includes OpenRouter entries as well, which can produce inaccurate operator output.
- **Suggestion:** Rename the section to something like "default routing chain" or print provider-specific chains separately.
- **Disposition:** Logged only

### os/x_daily_watchdog.js
- **Severity:** MEDIUM
- **Finding:** notify() calls execFileSync without error handling, so a Telegram send failure can crash the watchdog instead of surfacing a clean local warning.
- **Suggestion:** Wrap the notification call in try/catch and degrade to stderr logging on messaging failures.
- **Disposition:** PR attempted

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** DEFAULT_CONFIG.bio is loaded and exposed through loadConfig() but never used because the script always skips the bio update step, leaving dead configuration data in the file.
- **Suggestion:** Remove the unused bio config or implement the skipped bio-update path behind an explicit supported flow.
- **Disposition:** Logged only

### os/x_auth.js
- **Severity:** MEDIUM
- **Finding:** x_auth.js depends on ./openclaw_env_vars, but this helper is outside the reviewed OS file set and not part of the visible established pattern used elsewhere where modules edit openclaw.json directly or via shared constants, increasing the risk of runtime breakage from an implicit dependency.
- **Suggestion:** Use a clearly shared in-repo helper already referenced elsewhere or inline a small validated config-read/write path in this module.
- **Disposition:** PR attempted

### os/x_call.js
- **Severity:** LOW
- **Finding:** x_call.js duplicates OpenClaw config read/write helpers that overlap conceptually with x_auth.js’s env-var persistence path, increasing maintenance risk around token/config handling.
- **Suggestion:** Consolidate X env-var loading/updating into one shared helper used by both x_call.js and x_auth.js.
- **Disposition:** Logged only

### os/x_pricing.json
- **Severity:** LOW
- **Finding:** x_pricing.json records parse_status=manual_required and source URLs only, so cost routing still depends on hardcoded fallback estimates in code instead of a real config-driven pricing table.
- **Suggestion:** Populate this file with the normalized route cost data the code expects or remove the partial config until authoritative pricing data is available.
- **Disposition:** Logged only

### os/memory/x_budget_ledger.json
- **Severity:** LOW
- **Finding:** This committed ledger file is explicitly marked template-only runtime state, which is inconsistent with the stated pattern that live X budget state should live outside version control.
- **Suggestion:** Remove the tracked runtime-state artifact and keep only a documented template/example in a non-runtime location.
- **Disposition:** Logged only

### os/memory/x_digest.log.md
- **Severity:** LOW
- **Finding:** A runtime digest log placeholder is committed in-repo even though the file says active digest logs should live in an ignored runtime path outside version control.
- **Suggestion:** Stop tracking the runtime log artifact and replace it with documentation of the external runtime log location.
- **Disposition:** Logged only

### os/memory/x_digest_state.json
- **Severity:** LOW
- **Finding:** This tracked digest cursor file is another template-only runtime state artifact that conflicts with the documented runtime-path convention.
- **Suggestion:** Remove the committed state file and keep only a non-runtime template or schema reference.
- **Disposition:** Logged only

### os/memory/x_engagement.log.md
- **Severity:** LOW
- **Finding:** The engagement log placeholder is stored in-repo despite stating that real engagement output belongs in an ignored runtime path, which is inconsistent with the intended storage model.
- **Suggestion:** Keep only documentation or a sample format in-repo and move real engagement logs fully to runtime storage.
- **Disposition:** Logged only

### os/memory/x_queue.md
- **Severity:** LOW
- **Finding:** x_queue.md is checked in as a placeholder even though it represents active append-only queue state in the running system, making it inconsistent with the external runtime-state guidance used by the neighboring files.
- **Suggestion:** Replace the tracked queue file with schema documentation and write the live queue to an ignored runtime state path instead.
- **Disposition:** Logged only

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** This file is still a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **Suggestion:** Either implement the promised dry-run integration path or remove/replace the stub with documentation until real behavior exists.
- **Disposition:** Logged only

### skills/inbox_daily_triage_wrapper.js
- **Severity:** LOW
- **Finding:** This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying work, creating redundant no-op logic.
- **Suggestion:** Consolidate the wrapper and the triage skeleton into a single entrypoint or make the wrapper actually invoke the target path behind an explicit flag.
- **Disposition:** Logged only

### skills/web_repo_audit.js
- **Severity:** LOW
- **Finding:** Repo audit failures print stderr and exit without appending a failure log entry, which is inconsistent with the other skeletons’ memory-log pattern.
- **Suggestion:** Log the failure outcome to memory/logs.md and the hat log before exiting on command errors.
- **Disposition:** Logged only

### skills/weekly_healthcheck.js
- **Severity:** LOW
- **Finding:** The script shells out to hardcoded commands free, df, and uptime directly, which makes the health snapshot behavior less configurable than the rest of the workspace tooling.
- **Suggestion:** Move the command set into config/constants or a small helper so the snapshot inputs can be adjusted without code edits.
- **Disposition:** Logged only

### skills/README.md
- **Severity:** LOW
- **Finding:** The README promises lightweight read-only skill scripts in general terms, but the current directory mostly contains placeholders/no-op skeletons, which can mislead operators about implementation maturity.
- **Suggestion:** Clarify in the README which entries are skeletons/placeholders versus actually implemented skills.
- **Disposition:** Logged only

### skills/pr-review/SKILL.md
- **Severity:** LOW
- **Finding:** The command path is hardcoded to /root/.openclaw/workspace/scripts/pr-review-job.mjs, which is inconsistent with more portable workspace-relative patterns used elsewhere.
- **Suggestion:** Resolve the script path from the workspace root or a shared constant instead of embedding an absolute path in the skill.
- **Disposition:** Logged only
