# Self-Review Findings — 2026-03-25

Generated by `self-review-job.mjs` on 2026-03-25T08:10:32.193Z

## Findings

### scripts/autonomous_execute_queue.mjs
- **Severity:** MEDIUM
- **Finding:** runCritical() contains an unreachable status check because run() already throws on any non-zero exit code before returning.
- **Suggestion:** Remove the redundant status branch and either call run() directly or make run() return status without throwing so only one layer handles failures.
- **Disposition:** PR opened

### scripts/autonomous_execute_queue.mjs
- **Severity:** HIGH
- **Finding:** execBortOsTask() stages SECURITY.md even though this task only writes SECRETS.md, which can fail the workflow when SECURITY.md is absent or unchanged.
- **Suggestion:** Stage only files actually created or modified, or check optional companion files exist before adding them.
- **Disposition:** PR opened

### scripts/autonomous_execute_queue.mjs
- **Severity:** MEDIUM
- **Finding:** PR_BODY_PATH is hardcoded to /tmp/autonomy_pr_body.md, creating cross-run collisions if multiple queue executions overlap.
- **Suggestion:** Use a unique temp file per run or task via fs.mkdtempSync() or a filename including pid/timestamp.
- **Disposition:** PR opened

### scripts/autonomous_generate_queue.mjs
- **Severity:** LOW
- **Finding:** The queue generator is fully hardcoded and ignores autonomous_policy.json, so the generate step is a static seed file rather than policy-driven queue generation.
- **Suggestion:** Load queue candidates from config or discovery logic and keep only defaults/templates hardcoded.
- **Disposition:** Logged only

### scripts/autonomous_guard.mjs
- **Severity:** MEDIUM
- **Finding:** stopAutonomous() ignores the exit status of autonomous_stop.mjs, so stop failures are silently swallowed.
- **Suggestion:** Capture the spawnSync result and log or exit non-zero when the stop script fails.
- **Disposition:** PR opened

### scripts/autonomous_report.mjs
- **Severity:** MEDIUM
- **Finding:** The Telegram send result is never checked, so reporting can fail silently after the markdown report is generated.
- **Suggestion:** Inspect the openclaw message send exit code and log or report failures explicitly.
- **Disposition:** PR opened

### scripts/autonomous_runner.mjs
- **Severity:** LOW
- **Finding:** The comment says the queue is generated at the start of each autonomous window but runOnce() regenerates it on every 10-minute tick, which is inconsistent with documented behavior.
- **Suggestion:** Either generate once at startup or update the comment and naming to reflect per-tick regeneration.
- **Disposition:** Logged only

### scripts/autonomous_runner.mjs
- **Severity:** MEDIUM
- **Finding:** Child script paths are hardcoded to /root/.openclaw/workspace instead of using the shared constants or workspace pattern used elsewhere in the repo.
- **Suggestion:** Import the shared workspace constant or derive script paths from one config source rather than embedding absolute paths.
- **Disposition:** PR opened

### scripts/autonomous_start.mjs
- **Severity:** MEDIUM
- **Finding:** The hard-stop timer computes delta minutes with Math.max(0, stopMin - nowMin), so starting before midnight for a next-day stop time results in an immediate or skipped stop instead of overnight scheduling.
- **Suggestion:** Handle wraparound across midnight by adding 24 hours when stopMin is earlier than nowMin.
- **Disposition:** PR opened

### scripts/branch-cleanup.sh
- **Severity:** MEDIUM
- **Finding:** TELEGRAM_TARGET is resolved with an inline node command that reads /root/.openclaw/openclaw.json directly, duplicating config access logic and hardcoding runtime paths.
- **Suggestion:** Centralize config lookup in one shared helper or use exported environment or constants instead of embedding JSON parsing in shell scripts.
- **Disposition:** PR opened

### scripts/deploy.mjs
- **Severity:** LOW
- **Finding:** run() and runReadOnly() are identical wrappers around execSync(), which is redundant and diverges from the codebase’s usual single-command-helper pattern.
- **Suggestion:** Consolidate them into one helper and express intent via naming or comments instead of duplicate implementations.
- **Disposition:** Logged only

### scripts/deploy.mjs
- **Severity:** HIGH
- **Finding:** The rollback path performs git reset --hard to the previous commit without preserving local uncommitted work, which can destroy unrelated workspace changes if the repo is dirty.
- **Suggestion:** Refuse to deploy on a dirty worktree or stash and restore unrelated changes before any hard reset.
- **Disposition:** PR opened

### scripts/export-bort-source.sh
- **Severity:** LOW
- **Finding:** The script hardcodes /root/.openclaw/workspace instead of using the same BORT_WORKSPACE or constants pattern used by other scripts.
- **Suggestion:** Read the workspace root from environment or a shared constants source for consistency.
- **Disposition:** Logged only

### scripts/export-project-source.mjs
- **Severity:** LOW
- **Finding:** buildTgzBundle() returns a boolean but the caller ignores failures, so export can appear successful even when the bundle is not created.
- **Suggestion:** Check the return value and fail loudly or log a warning when tar creation fails.
- **Disposition:** Logged only

### scripts/generate-bort-capabilities-md.mjs
- **Severity:** MEDIUM
- **Finding:** readJson() does not handle missing or malformed snapshot files, so a bad source file crashes the generator with no actionable context.
- **Suggestion:** Wrap JSON parsing in try/catch and emit a clear error that identifies the failing snapshot path.
- **Disposition:** PR opened

### scripts/generate-project-source-core.mjs
- **Severity:** LOW
- **Finding:** extractRoutes() and similar regex-based parsers duplicate parsing logic already present in other generators and checkers, increasing maintenance drift when route syntax changes.
- **Suggestion:** Move shared route or hat extraction helpers into a common module and reuse them across scripts.
- **Disposition:** Logged only

### scripts/generate-state-of-bort.mjs
- **Severity:** MEDIUM
- **Finding:** The script shells out to openclaw skills list --json without surfacing command failure details, so skill enumeration silently degrades with little diagnostic signal.
- **Suggestion:** Surface command failures in the generated notes or stderr so partial state generation is observable.
- **Disposition:** PR opened

### scripts/pinchtab-session.sh
- **Severity:** MEDIUM
- **Finding:** cmd_start() leaves a stale PID file behind when startup times out, causing later status and start behavior to treat a failed boot as an active session.
- **Suggestion:** Remove the PID file and optionally kill the child process on health-check timeout.
- **Disposition:** PR opened

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** ghJson() is dead code and never called anywhere in the script.
- **Suggestion:** Remove the unused helper or replace callers with it if a generic JSON helper is actually desired.
- **Disposition:** Logged only

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** hasProjectSourceMd() and hasArchDrift() are defined but never used, leaving dead code and fragmented policy logic.
- **Suggestion:** Delete the unused helpers or integrate them into the review policy checks.
- **Disposition:** Logged only

### scripts/pr-review-job.mjs
- **Severity:** HIGH
- **Finding:** reviewPR() fetches full PR JSON and full diff for every open PR sequentially, which is expensive and can become slow or rate-limit prone as PR count grows.
- **Suggestion:** Filter with lighter metadata first and only fetch full diff and details for PRs that pass cheap prechecks.
- **Disposition:** PR opened

### scripts/repo_hygiene_check.sh
- **Severity:** HIGH
- **Finding:** The script can auto-commit and push allowlisted changes to main without first confirming the repo is on main, so it may push hygiene commits from an unexpected branch context.
- **Suggestion:** Verify the current branch is main before auto-committing and pushing or explicitly check out main in a safe way.
- **Disposition:** PR opened

### scripts/repo_hygiene_check.sh
- **Severity:** MEDIUM
- **Finding:** TELEGRAM_CHAT_ID is loaded via another hardcoded inline node parser against /root/.openclaw/openclaw.json, duplicating config lookup logic used in multiple shell scripts.
- **Suggestion:** Replace ad hoc JSON parsing with a shared helper or env or config abstraction.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** HIGH
- **Finding:** createFixPRs() runs fixer prompts without constraining the agent to the target file, then cleans up unexpected edits after the fact, which is brittle and can still create unintended side effects during execution.
- **Suggestion:** Pass explicit file constraints and abort immediately on multi-file edits before any further git operations.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** MEDIUM
- **Finding:** createFixPRs() writes PR bodies to predictable /tmp/pr-body-<slug>.md paths, which can collide across concurrent runs on the same host.
- **Suggestion:** Use a per-run temp directory or include pid or timestamp randomness in temp filenames.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** MEDIUM
- **Finding:** sendTelegram() assumes TELEGRAM_CHAT_ID is configured and does not guard against an empty target, unlike newer notification code elsewhere in the repo.
- **Suggestion:** Skip notification cleanly when no Telegram target is configured and log that decision consistently.
- **Disposition:** PR opened

### scripts/self-review-job.mjs
- **Severity:** LOW
- **Finding:** runWebResearch() creates /tmp/self-review-research-*.txt files but never uses them for the agent call, making the temp-file write dead work.
- **Suggestion:** Remove the unused temp files or actually feed them into the analysis workflow.
- **Disposition:** Logged only

### scripts/site-improvement-job.mjs
- **Severity:** LOW
- **Finding:** PINCHTAB_PID and PINCHTAB_BASE are declared but never used, which is dead code.
- **Suggestion:** Remove the unused constants or wire them into the Pinchtab lifecycle logic.
- **Disposition:** Logged only

### scripts/site-improvement-job.mjs
- **Severity:** MEDIUM
- **Finding:** createPRs() captures changeResult from the openclaw agent but never uses it, missing an opportunity to validate the claimed edited paths against the worktree.
- **Suggestion:** Parse and verify the returned file list before staging to ensure only intended files changed.
- **Disposition:** PR opened

### scripts/site-improvement-job.mjs
- **Severity:** HIGH
- **Finding:** createPRs() stages all changes with git add -A, so any accidental extra files from the agent run are included in the PR.
- **Suggestion:** Restrict staging to validated expected paths from suggested_change or the agent’s reported edit list.
- **Disposition:** PR opened

### scripts/update-bort-bundle.sh
- **Severity:** LOW
- **Finding:** The script hardcodes the workspace root and public output path instead of following the config or constants pattern used in several Node scripts.
- **Suggestion:** Source the root path from environment or a shared config helper to reduce path drift.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.log
- **Severity:** LOW
- **Finding:** This tracked file is effectively placeholder text stating runtime logs should not live in-repo, so keeping it in the reviewed file set is dead weight and inconsistent with its own guidance.
- **Suggestion:** Remove the committed log file and create the runtime log path on demand in an ignored location instead.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.js
- **Severity:** MEDIUM
- **Finding:** loadJson() and saveJson() perform unguarded JSON parse and write operations, so a corrupted state or prefs file will crash the sweep without a targeted recovery path.
- **Suggestion:** Wrap state and prefs parsing and writes with explicit error messages and fallback or reset handling for malformed runtime files.
- **Disposition:** PR opened

### integrations/gmail/backlog-sweep.js
- **Severity:** MEDIUM
- **Finding:** The script defaults to committed per-account runtime files like prefs-gobuffs10.json and backlog-state-gobuffs10.json, which conflicts with the repo’s own guidance that live state should live outside version control.
- **Suggestion:** Move default runtime state and prefs paths to ignored external locations and keep only template or example files in-repo.
- **Disposition:** PR opened

### integrations/gmail/count-unread.js
- **Severity:** LOW
- **Finding:** This duplicates the same paginated unread-count logic already implemented in count-threads.js, creating redundant code paths for the same operation.
- **Suggestion:** Reuse count-threads.js or extract a shared count helper so unread counting has one implementation.
- **Disposition:** Logged only

### integrations/gmail/daily-review.js
- **Severity:** HIGH
- **Finding:** The implementation does not remove UNREAD for SpamReview threads even though the file comment says SpamReview threads are marked read, making behavior inconsistent with the documented action.
- **Suggestion:** Add removeLabelIds:['UNREAD'] for the spamReview bucket or update the header comment if the intended behavior changed.
- **Disposition:** PR opened

### integrations/gmail/daily-review.js
- **Severity:** MEDIUM
- **Finding:** loadPrefs() parses JSON without error handling, so a malformed prefs file aborts the whole daily review with no clear remediation beyond a raw exception.
- **Suggestion:** Catch JSON parse failures and emit a targeted config error or fall back to safe defaults.
- **Disposition:** PR opened

### integrations/gmail/daily-review.js
- **Severity:** MEDIUM
- **Finding:** The Gmail query is hardcoded to in:inbox is:unread with no CLI override, unlike backlog-sweep.js and other scripts that make the query configurable.
- **Suggestion:** Add a --q argument or shared config option so the daily review query is configurable and consistent with the rest of the integration.
- **Disposition:** PR opened

### integrations/gmail/daily-summary.sh
- **Severity:** MEDIUM
- **Finding:** The script hardcodes the gobuffs10 secret paths, workspace path, and account name in defaults and message text instead of deriving them from a shared account config.
- **Suggestion:** Make account slug, secret paths, and display label config-driven through env vars or a per-account config file.
- **Disposition:** PR opened

### integrations/gmail/inbox-audit.js
- **Severity:** MEDIUM
- **Finding:** withBackoff() duplicates retry logic already provided by lib.js, creating inconsistent retry behavior and another backoff implementation to maintain.
- **Suggestion:** Reuse the shared withBackoff helper from lib.js or move the richer logging variant into lib.js and standardize callers on it.
- **Disposition:** PR opened

### integrations/gmail/inbox-audit.js
- **Severity:** LOW
- **Finding:** The documented default for --senderPages says 20, but the actual code default is 5, which is inconsistent and can mislead operators about scan coverage.
- **Suggestion:** Align the code and help text so the documented default matches the implemented default.
- **Disposition:** Logged only

### integrations/gmail/lib.js
- **Severity:** MEDIUM
- **Finding:** getOrCreateLabel() lists all labels on every call, so setup-labels.js and other callers repeat the same labels.list request unnecessarily.
- **Suggestion:** Cache label lookups per process or provide a bulk ensureLabels helper that fetches labels once and creates missing ones in one pass.
- **Disposition:** PR opened

### integrations/gmail/other-prune.js
- **Severity:** MEDIUM
- **Finding:** listAllThreads() loads every matching thread into memory before processing, which is less scalable than the resumable paged approach already implemented in other-prune2.js.
- **Suggestion:** Retire this script or refactor it to page and process incrementally using the same checkpointed pattern as other-prune2.js.
- **Disposition:** PR opened

### integrations/gmail/other-prune2.js
- **Severity:** MEDIUM
- **Finding:** loadJson() parses the state file without error handling, so a corrupted resume file crashes the resumable prune instead of resetting or surfacing a clear state error.
- **Suggestion:** Catch malformed-state parse failures and either rebuild a fresh state file or exit with an explicit recovery message.
- **Disposition:** PR opened

### integrations/gmail/package.json
- **Severity:** LOW
- **Finding:** package.json contains only dependencies and omits basic metadata or scripts, which is inconsistent with established package structure and makes common operations less discoverable.
- **Suggestion:** Add minimal package metadata and npm scripts for auth, test, daily-review, backlog-sweep, and audit commands.
- **Disposition:** Logged only

### integrations/gmail/prefs-gobuffs10.json
- **Severity:** MEDIUM
- **Finding:** A real account-specific prefs file is committed in-repo even though nearby templates and notes say real per-account config should come from explicit runtime config paths.
- **Suggestion:** Move live account prefs to an ignored runtime path and keep only a sanitized template in version control.
- **Disposition:** PR opened

### integrations/gmail/setup-labels.js
- **Severity:** LOW
- **Finding:** The label list is hardcoded in-script, which duplicates label configuration already described in RUNBOOK.md and other scripts.
- **Suggestion:** Centralize managed label names in shared config or constants so all scripts consume the same label set.
- **Disposition:** Logged only

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** INITIAL defaults to a hardcoded backlog size of 13888, which makes progress calculations environment-specific and stale as soon as the baseline changes.
- **Suggestion:** Read the initial baseline from state or config or require it as an explicit input rather than hardcoding it in the script.
- **Disposition:** PR opened

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** The script tails a log file path that may not exist yet, and although tail failure is masked with || true, the missing-file condition is silently swallowed instead of being reported clearly.
- **Suggestion:** Check for log existence explicitly and report no log yet so missing runtime artifacts are distinguishable from clean runs.
- **Disposition:** PR opened

### integrations/gmail/sweep-runner.sh
- **Severity:** MEDIUM
- **Finding:** The script defaults LOG and STATE to committed in-repo files even though the repo includes placeholder files saying active logs and state should live outside version control.
- **Suggestion:** Move default state and log outputs to ignored runtime paths and reserve in-repo files for templates or documentation only.
- **Disposition:** PR opened

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** test.js duplicates OAuth client loading logic that already exists in lib.js, creating unnecessary parallel auth code.
- **Suggestion:** Reuse loadOAuthClient() from lib.js and keep the test script focused on Gmail sanity checks only.
- **Disposition:** Logged only

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** The required --email argument is only used as a CLI gate and never validated against the authenticated account, making it effectively dead input.
- **Suggestion:** Either remove --email entirely or compare it against profile.data.emailAddress and fail on mismatch.
- **Disposition:** Logged only

### integrations/gmail/unsubscribe.js
- **Severity:** LOW
- **Finding:** normalizeFrom is imported from lib.js but never used, leaving dead code in the module imports.
- **Suggestion:** Remove the unused import to keep dependencies and intent clean.
- **Disposition:** Logged only

### integrations/gmail/unsubscribe.js
- **Severity:** MEDIUM
- **Finding:** For each sender the script performs message get calls sequentially until completion, which is slower than necessary for multi-sender unsubscribe batches.
- **Suggestion:** Add bounded concurrency for per-message metadata fetches or short-circuit more aggressively after the first successful unsubscribe path.
- **Disposition:** PR opened

### os/constants.js
- **Severity:** LOW
- **Finding:** OPENCLAW_CONFIG_PATH and BORT_WORKSPACE still default to hardcoded absolute paths, which makes the module less portable despite being positioned as a shared constants source.
- **Suggestion:** Resolve these from a single runtime root or env configuration layer and keep hardcoded paths only as last-resort fallbacks.
- **Disposition:** Logged only

### os/help.js
- **Severity:** MEDIUM
- **Finding:** deriveFromHatsMd() and state_report.js both scrape memory/hats.md independently, duplicating fragile parsing logic instead of using hat-profiles.json as the canonical source.
- **Suggestion:** Centralize hat metadata loading in one shared helper that reads os/hat-profiles.json first and uses markdown only as a fallback.
- **Disposition:** PR opened

### os/memory-log.js
- **Severity:** MEDIUM
- **Finding:** appendBlock() writes directly to target files without ensuring the parent directory exists, so first-run logging can fail if memory or the hat file path has not been created yet.
- **Suggestion:** Create parent directories before appendFileSync and keep the writer resilient on clean workspaces.
- **Disposition:** PR opened

### os/model-routing.js
- **Severity:** HIGH
- **Finding:** routeModel() falls back to hardcoded openai/gpt-5.2-chat-latest even though that model is outside the verified allowlist and not present in the configured chain.
- **Suggestion:** Return null or an explicit no-route result, or fall back only to a model from the verified configured allowlist.
- **Disposition:** PR opened

### os/model-routing.js
- **Severity:** LOW
- **Finding:** The ROUTES categories are all identical arrays, so the routing table currently adds repetition without encoding any category-specific behavior.
- **Suggestion:** Consolidate to a shared default chain until categories genuinely diverge, or generate per-category routes from a smaller config object.
- **Disposition:** Logged only

### os/model-routing.js
- **Severity:** LOW
- **Finding:** isModelAvailable() accepts an envelope parameter but never uses it, which is dead API surface and hints at unfinished sensitivity-based gating.
- **Suggestion:** Remove the unused parameter or implement the intended envelope-aware policy checks in one place.
- **Disposition:** Logged only

### os/model-routing_selftest.js
- **Severity:** HIGH
- **Finding:** The self-test expectations are stale and still assert gpt-5.3-codex as the routed default even though model-routing.js now routes to gpt-5.4 via the hat default chain.
- **Suggestion:** Update expected values to the current routing policy and make the test derive expectations from a shared fixture when possible.
- **Disposition:** PR opened

### os/models_check.js
- **Severity:** MEDIUM
- **Finding:** appendModelsCheckLog() appends unbounded full OpenRouter catalog snapshots into memory/logs.md on every run, which can grow quickly and degrade parsing performance over time.
- **Suggestion:** Store snapshots in a dedicated bounded state file or retain only the latest or last-N snapshots instead of appending full catalogs to logs.md indefinitely.
- **Disposition:** PR opened

### os/models_check.js
- **Severity:** MEDIUM
- **Finding:** readJson() parses /root/.openclaw/openclaw.json without error handling, so a malformed config crashes the watcher instead of surfacing a targeted diagnostic.
- **Suggestion:** Wrap config parsing in try/catch and emit a clear config-read failure with a safe fallback path.
- **Disposition:** PR opened

### os/models_inventory.js
- **Severity:** LOW
- **Finding:** The section label OpenAI fallback chain is inconsistent with the actual default route, which now includes an OpenRouter Claude fallback as well.
- **Suggestion:** Rename the section to Default fallback chain or similar so the output matches the configured model mix.
- **Disposition:** Logged only

### os/preflight.js
- **Severity:** MEDIUM
- **Finding:** loadHatProfiles() embeds a full fallback hat policy object that duplicates os/hat-profiles.json, increasing drift risk whenever policy changes.
- **Suggestion:** Move fallback defaults into a single shared data file or module and keep preflight’s loader thin.
- **Disposition:** PR opened

### os/preflight.js
- **Severity:** LOW
- **Finding:** envelopeTemplate() still advertises hat: inbox|web|resale|ops-core and omits autonomous, which is inconsistent with the actual hat registry.
- **Suggestion:** Generate the template hat list from the loaded hat profiles instead of hardcoding it.
- **Disposition:** Logged only

### os/preflight.test.js
- **Severity:** LOW
- **Finding:** path is imported but never used, leaving dead code in the test harness.
- **Suggestion:** Remove the unused import to keep the test file tidy.
- **Disposition:** Logged only

### os/run_model_tests.js
- **Severity:** HIGH
- **Finding:** The assertions are stale and still expect gpt-5.3-codex for multiple routes even though current routing now resolves to gpt-5.4 first.
- **Suggestion:** Update the expected models to current routing behavior and keep the test vectors synchronized with model-routing.js changes.
- **Disposition:** PR opened

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** BIO is declared but never used because the bio-update step is always skipped, leaving dead code at the top of the script.
- **Suggestion:** Remove BIO or implement the supported profile-update path behind an explicit capability check.
- **Disposition:** Logged only

### os/social_ops_execute_x.js
- **Severity:** MEDIUM
- **Finding:** The follow target list and tweet text are hardcoded in-script, which makes social-ops behavior static rather than policy or config driven.
- **Suggestion:** Move targets and seed copy into a config or content file that can be updated without editing executable code.
- **Disposition:** PR opened

### os/social_ops_follow_remaining.js
- **Severity:** MEDIUM
- **Finding:** The target follow list and hourly cap are hardcoded values, which makes operator tuning require code edits rather than configuration changes.
- **Suggestion:** Read targets and follow-rate limits from config or state so the script behavior can be changed without patching code.
- **Disposition:** PR opened

### os/state_report.js
- **Severity:** MEDIUM
- **Finding:** preflightTestStatus() shells out to node os/preflight.test.js every time the report runs, which is expensive and can mutate memory logs because the test harness writes to memory files.
- **Suggestion:** Separate pure validation tests from logging tests or use a non-mutating smoke check for report generation.
- **Disposition:** PR opened

### os/state_report.js
- **Severity:** MEDIUM
- **Finding:** The per-task routing section prints hardcoded fallback strings like openai/gpt-5.2-chat-latest and openai/gpt-5.2-pro that are not derived from the actual configured routing tables.
- **Suggestion:** Generate displayed fallback information from model-routing or openclaw config so the report cannot drift from reality.
- **Disposition:** PR opened

### os/x_auth.js
- **Severity:** HIGH
- **Finding:** updateOpenClawEnvVars() rewrites /root/.openclaw/openclaw.json directly with ad hoc file locking instead of using the established gateway or config tooling pattern.
- **Suggestion:** Route config mutations through the shared config or gateway mechanism or a single reusable config writer to avoid divergent lock and write semantics.
- **Disposition:** PR attempted

### os/x_auth.js
- **Severity:** LOW
- **Finding:** redact() always returns [REDACTED], so printing tokens_printed: [REDACTED] adds noise without conveying useful state and functions like dead ceremony.
- **Suggestion:** Remove the token print line entirely and just report that credentials were stored successfully.
- **Disposition:** Logged only

### os/x_budget.js
- **Severity:** MEDIUM
- **Finding:** ensureLedgerFile() comments that invalid JSON should not be overwritten, but the implementation silently swallows parse failure and leaves a broken ledger with no surfaced error.
- **Suggestion:** Detect malformed ledger JSON explicitly and return or throw a clear corruption error so callers know the budget state is unreliable.
- **Disposition:** PR opened

### os/x_budget.js
- **Severity:** LOW
- **Finding:** estimateCost() accepts actionType but never uses it, leaving dead parameter surface in the budget API.
- **Suggestion:** Remove the unused parameter or incorporate it into endpoint-cost fallback logic.
- **Disposition:** Logged only

### os/x_budget_checks.js
- **Severity:** MEDIUM
- **Finding:** The test writes real spend records and queue entries into the live memory ledger and queue files, so a test run mutates production budgeting state.
- **Suggestion:** Run the checks against isolated temp paths or injectable file locations instead of the live ledger and queue.
- **Disposition:** PR opened

### os/x_call.js
- **Severity:** HIGH
- **Finding:** A 401-triggered retry records spend twice for a single logical action because doRequest() records spend on both the failed attempt and the post-refresh retry.
- **Suggestion:** Record spend once per logical xCall or make retry accounting explicit so automatic refresh retries do not double-charge the budget ledger.
- **Disposition:** PR attempted

### os/x_call.js
- **Severity:** HIGH
- **Finding:** updateOpenClawEnvVars() duplicates the same direct config rewrite logic found in x_auth.js, creating multiple independent writers for the same sensitive config file.
- **Suggestion:** Extract one shared config-update helper or switch both call sites to the standard gateway or config tooling path.
- **Disposition:** PR attempted

### os/x_call.js
- **Severity:** MEDIUM
- **Finding:** loadPricingIfExists() resolves pricing from process.cwd()/os/x_pricing.json, making behavior dependent on the caller’s working directory instead of the module location.
- **Suggestion:** Resolve x_pricing.json relative to __dirname so pricing lookup is stable regardless of cwd.
- **Disposition:** PR attempted

### os/x_daily_post.js
- **Severity:** MEDIUM
- **Finding:** notify() uses execFileSync with stdio inherit and no error handling, so Telegram notification failures can crash the posting workflow unexpectedly.
- **Suggestion:** Wrap notification sends in try/catch and treat them as best-effort side effects.
- **Disposition:** PR opened

### os/x_daily_post.js
- **Severity:** LOW
- **Finding:** execSync is imported but never used, leaving dead code in the module imports.
- **Suggestion:** Remove the unused import.
- **Disposition:** Logged only

### os/x_daily_watchdog.js
- **Severity:** HIGH
- **Finding:** notify() sends unconditionally even when TELEGRAM_CHAT_ID is empty, which can make the watchdog fail while trying to report a missing-post condition.
- **Suggestion:** Guard notification sends the same way other scripts do and skip cleanly when Telegram is not configured.
- **Disposition:** PR opened

### os/x_digest_job.js
- **Severity:** MEDIUM
- **Finding:** The curated account allowlist is hardcoded in the job and duplicated again in appendDigest(), creating two places that must stay synchronized.
- **Suggestion:** Move the curated account list to shared config or state and render the log line from that same source.
- **Disposition:** PR attempted

### os/x_digest_job.js
- **Severity:** MEDIUM
- **Finding:** The job stores scored excerpts in both x_digest.log.md and x_digest_scored.md, duplicating persistence for overlapping data and increasing maintenance and log growth.
- **Suggestion:** Keep one canonical scored store and derive human-readable summaries from it instead of persisting similar data twice.
- **Disposition:** PR attempted

### os/x_engagement_job.js
- **Severity:** MEDIUM
- **Finding:** parsePostLog() reparses the markdown post log to recover tweet IDs, duplicating log-scraping behavior that already exists in other X scripts and depending on fragile markdown structure.
- **Suggestion:** Store post results in a structured state file or shared parser helper rather than repeatedly scraping markdown logs.
- **Disposition:** PR opened

### os/x_post_job.js
- **Severity:** MEDIUM
- **Finding:** notify() mixes best-effort notification with inherited stdio and no error handling, so a Telegram send failure can abort the post job after the X API action has already happened.
- **Suggestion:** Catch notification failures and log them separately so post execution and alert delivery are decoupled.
- **Disposition:** PR opened

### os/x_post_job.js
- **Severity:** LOW
- **Finding:** buildTweet() is dead code because the final posting path constructs tweet text inline and never calls it.
- **Suggestion:** Remove the unused helper or route the final tweet construction through it.
- **Disposition:** Logged only

### os/memory/x_budget_ledger.json
- **Severity:** MEDIUM
- **Finding:** This committed file is a template placeholder for runtime ledger state, which is inconsistent with its own note that real budget state should live outside version control.
- **Suggestion:** Remove the committed runtime-state placeholder from the active memory path and generate it on demand in an ignored runtime location.
- **Disposition:** PR attempted

### os/memory/x_digest.log.md
- **Severity:** LOW
- **Finding:** This file contains only a placeholder note and no executable or reporting logic, so keeping it under the active runtime log path is dead weight and inconsistent with the stated out-of-repo logging pattern.
- **Suggestion:** Replace it with an ignored runtime-created log file and keep any schema or documentation in a docs or template location instead.
- **Disposition:** Logged only

### os/memory/x_digest_state.json
- **Severity:** MEDIUM
- **Finding:** A live-looking state file with nextIndex is committed in-repo, which conflicts with the surrounding convention that runtime X state should not be version-controlled.
- **Suggestion:** Move digest cursor state to an ignored runtime state path and keep only documented defaults or templates in version control.
- **Disposition:** PR attempted

### os/memory/x_engagement.log.md
- **Severity:** LOW
- **Finding:** This tracked file is only a placeholder saying engagement output should not be stored in-repo, making it effectively dead content in the active runtime output path.
- **Suggestion:** Delete the committed placeholder and create the engagement log dynamically in an ignored runtime directory when needed.
- **Disposition:** Logged only

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** This file is a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **Suggestion:** Either implement the promised dry-run integration path or remove or replace the stub with documentation until real behavior exists.
- **Disposition:** Logged only

### skills/inbox_daily_triage_wrapper.js
- **Severity:** LOW
- **Finding:** This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying action, which is redundant placeholder logic.
- **Suggestion:** Consolidate the wrapper and skeleton into one entrypoint or implement the actual delegated call path behind an explicit flag.
- **Disposition:** Logged only

### skills/web_repo_audit.js
- **Severity:** MEDIUM
- **Finding:** repoPath validation uses a restrictive regex on the entire path string, which can reject valid absolute paths with spaces and duplicates path-safety logic instead of relying on execFile argument separation.
- **Suggestion:** Validate existence and directory type with fs APIs and keep git invocation argumentized rather than enforcing a custom path-character allowlist.
- **Disposition:** PR opened

### skills/web_repo_audit.js
- **Severity:** LOW
- **Finding:** On failure the script prints raw stderr or message directly, which is inconsistent with the surrounding minimal, structured output style used by these skill skeletons.
- **Suggestion:** Normalize failure output to a short structured message and log detailed diagnostics separately if needed.
- **Disposition:** Logged only
