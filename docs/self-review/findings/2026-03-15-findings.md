# Self-Review Findings — 2026-03-15

Generated by `self-review-job.mjs` on 2026-03-15T07:25:06.285Z

## Findings

### scripts/autonomous_guard.mjs
- **Severity:** HIGH
- **Finding:** ESM script uses CommonJS `require('child_process')` inside `stopAutonomous`, which can throw at runtime.
- **Suggestion:** Replace `require` with a top-level ESM import (`import { spawnSync } from 'child_process'`) and use it directly.
- **Disposition:** PR attempted

### scripts/autonomous_stop.mjs
- **Severity:** MEDIUM
- **Finding:** `JSON.parse` on `autonomous_state.json` is unguarded and can crash stop flow if the state file is corrupted.
- **Suggestion:** Wrap state parsing in try/catch and fall back to a minimal stop/report path when parsing fails.
- **Disposition:** PR opened

### scripts/autonomous_start.mjs
- **Severity:** MEDIUM
- **Finding:** `until` input is only presence-checked and not validated as `HH:MM`, allowing invalid stop times into state.
- **Suggestion:** Validate `until` with a strict `HH:MM` regex and exit with usage error when invalid.
- **Disposition:** PR attempted

### scripts/autonomous_start.mjs
- **Severity:** LOW
- **Finding:** Stop behavior is duplicated across guard loop and detached sleep timer, creating redundant control paths.
- **Suggestion:** Keep a single stop mechanism (prefer guard or timer) and remove the duplicate path.
- **Disposition:** Logged only

### scripts/autonomous_execute_queue.mjs
- **Severity:** MEDIUM
- **Finding:** Personal website repo path is hardcoded to `external/personal-website`, which is inconsistent with current `tmp/site-review/repo` workflow.
- **Suggestion:** Read target repo path from config/env and validate existence before branch/commit operations.
- **Disposition:** PR attempted

### scripts/branch-cleanup.sh
- **Severity:** HIGH
- **Finding:** `node -p "require('${REPO}/os/constants').TELEGRAM_CHAT_ID"` relies on CommonJS require for a `.js` module that follows ESM patterns elsewhere.
- **Suggestion:** Resolve Telegram target via `openclaw`/env/config or use an ESM-safe node invocation instead of `require`.
- **Disposition:** PR opened

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** `ghJson` helper is dead code and never called.
- **Suggestion:** Remove the unused function to reduce maintenance noise.
- **Disposition:** Logged only

### scripts/generate-state-of-bort.mjs
- **Severity:** MEDIUM
- **Finding:** Recursive `countFiles(WORKSPACE)` walks the entire workspace on every run, which can become expensive as data grows.
- **Suggestion:** Make file counting optional or scope it to relevant directories with a configurable cap/depth.
- **Disposition:** PR attempted

### scripts/pinchtab-session.sh
- **Severity:** MEDIUM
- **Finding:** `curl` health checks have no timeout flags, so health probes can hang and delay start/status flows.
- **Suggestion:** Add conservative curl timeouts (`--connect-timeout`/`--max-time`) to health_check.
- **Disposition:** PR opened

### scripts/export-project-source.mjs
- **Severity:** LOW
- **Finding:** Canonical file list is hardcoded here and duplicated in `update-bort-bundle.sh`, increasing drift risk.
- **Suggestion:** Move canonical filenames to a shared config/source and import/read it in both scripts.
- **Disposition:** Logged only

### scripts/update-bort-bundle.sh
- **Severity:** MEDIUM
- **Finding:** Script assumes `$PUB` exists and writable, but never creates/validates it before copy/zip operations.
- **Suggestion:** Add `mkdir -p "$PUB"` and explicit preflight checks before file copy/zip steps.
- **Disposition:** PR opened

### scripts/site-improvement-job.mjs
- **Severity:** MEDIUM
- **Finding:** Script hardcodes multiple operational paths/endpoints (`/tmp/pinchtab.pid`, `127.0.0.1:9867`, review dirs) instead of config-driven values.
- **Suggestion:** Centralize these values in config/env/constants and read them once at startup with validation.
- **Disposition:** PR attempted

### integrations/gmail/daily-summary.sh
- **Severity:** HIGH
- **Finding:** Script resolves TELEGRAM_CHAT_ID via `node -p require('/root/.openclaw/workspace/os/constants')`, which is brittle in ESM environments and hardcodes host-specific paths.
- **Suggestion:** Read chat target from env/config (or `openclaw` config query) and remove CommonJS `require` path coupling.
- **Disposition:** PR attempted

### integrations/gmail/status-ping.sh
- **Severity:** HIGH
- **Finding:** Script hardcodes credentials/token/state/log absolute paths and TELEGRAM_CHAT_ID resolution, making it non-portable and fragile across hosts.
- **Suggestion:** Move all paths/targets to env or a small config file with validation and defaults.
- **Disposition:** PR opened

### integrations/gmail/sweep-runner.sh
- **Severity:** MEDIUM
- **Finding:** Runtime knobs and secret paths are hardcoded (batch/query/creds/token/state/log), preventing safe per-environment tuning.
- **Suggestion:** Parameterize via env/flags and add sanity checks for required files before execution.
- **Disposition:** PR opened

### integrations/gmail/count-threads.js
- **Severity:** LOW
- **Finding:** OAuth loading and backoff logic duplicate functionality already centralized in `lib.js`, creating pattern drift.
- **Suggestion:** Reuse `loadOAuthClient` and shared `withBackoff` from `lib.js` to consolidate behavior.
- **Disposition:** Logged only

### integrations/gmail/other-prune.js
- **Severity:** MEDIUM
- **Finding:** Gmail list/get/modify calls run without retry/backoff handling, so transient 429/5xx errors can abort long runs mid-process.
- **Suggestion:** Wrap Gmail API calls with shared `withBackoff` and persist progress state for resumability.
- **Disposition:** PR opened

### integrations/gmail/daily-review.js
- **Severity:** LOW
- **Finding:** Archive-after-rule performs a second `threads.modify` call per matching thread, adding avoidable API overhead.
- **Suggestion:** Combine label/star/read/archive changes into one `threads.modify` request body per thread.
- **Disposition:** Logged only

### integrations/gmail/unsubscribe.js
- **Severity:** MEDIUM
- **Finding:** `httpRequest` has no timeout handling, so unsubscribe calls can hang indefinitely and stall the whole run.
- **Suggestion:** Add request/connect timeouts and bounded retries for HTTP unsubscribe attempts.
- **Disposition:** PR opened

### integrations/gmail/unsubscribe.js
- **Severity:** MEDIUM
- **Finding:** Gmail API calls in unsubscribe flow are not wrapped with backoff, increasing failure risk on rate limits.
- **Suggestion:** Use shared `withBackoff` for `messages.list/get/send` operations.
- **Disposition:** PR opened

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** `--email` is required but never used after validation, which is dead argument logic.
- **Suggestion:** Either verify `profile.data.emailAddress` against `--email` or remove the unused argument requirement.
- **Disposition:** Logged only

### integrations/gmail/backlog-sweep.log
- **Severity:** LOW
- **Finding:** Repository-tracked operational log is extremely large and contains volatile runtime data, which hurts reviewability and performance.
- **Suggestion:** Stop tracking this log in git and rotate/write it under tmp or ignored runtime paths.
- **Disposition:** Logged only

### integrations/gmail/backlog-state-gobuffs10.json
- **Severity:** MEDIUM
- **Finding:** Runtime state file stores massive offender/thread maps in-repo, causing heavy I/O and noisy diffs for operational churn.
- **Suggestion:** Move state to an ignored runtime directory and cap/prune historical map growth.
- **Disposition:** PR opened

### os/models_inventory.js
- **Severity:** HIGH
- **Finding:** Script iterates `inv.openaiFallbackChain` even though `inventory()` does not return that property, causing a runtime crash.
- **Suggestion:** Remove that loop or add `openaiFallbackChain` to `inventory()` with a safe default empty array.
- **Disposition:** PR attempted

### os/run_model_tests.js
- **Severity:** HIGH
- **Finding:** Self-test expectations reference models not present in router allowlist/routes (e.g., `openrouter/google/gemini-2.5-flash-lite`, `openai/gpt-5.2-pro`), making tests inconsistent and failing by design.
- **Suggestion:** Align expected outputs with `os/model-routing.js` or update routing table/health entries to match the test contract.
- **Disposition:** PR opened

### os/preflight.js
- **Severity:** MEDIUM
- **Finding:** `runProjectSourceCheck()` executes at module load time, creating hidden side effects and avoidable overhead for any importer/test.
- **Suggestion:** Move source-check invocation to an explicit CLI entrypoint or guarded function call instead of top-level import execution.
- **Disposition:** PR attempted

### os/preflight.js
- **Severity:** MEDIUM
- **Finding:** `runProjectSourceCheck()` swallows all failures silently, masking diagnostics when preflight drift checks break.
- **Suggestion:** Log a concise warning/error code on failure while keeping non-blocking behavior.
- **Disposition:** PR attempted

### os/state_report.js
- **Severity:** MEDIUM
- **Finding:** Report hardcodes `preflight tests: PASS (14 passed / 0 failed)` instead of reading actual latest test status, risking stale or false health reporting.
- **Suggestion:** Parse real test output/artifact at runtime and print the computed pass/fail summary.
- **Disposition:** PR attempted

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** `BIO` constant is dead code because bio update is always skipped and the variable is never used.
- **Suggestion:** Remove the unused constant or implement a supported bio-update flow behind a feature flag.
- **Disposition:** Logged only

### os/constants.js
- **Severity:** MEDIUM
- **Finding:** `TELEGRAM_CHAT_ID` is hardcoded in source, reducing portability and violating config-driven pattern used elsewhere.
- **Suggestion:** Resolve chat ID from environment/OpenClaw config with validation and fallback handling.
- **Disposition:** PR opened

### os/x_daily_post.js
- **Severity:** MEDIUM
- **Finding:** Notifications are sent via `execSync` shell command construction, which is brittle and can fail on message-size/escaping edge cases.
- **Suggestion:** Use the OpenClaw message API/tooling interface directly (or spawn with argv array) instead of shell-string command assembly.
- **Disposition:** PR opened

### os/x_post_job.js
- **Severity:** MEDIUM
- **Finding:** Notification path also relies on `execSync` with interpolated shell command text, creating the same reliability/escaping risk pattern.
- **Suggestion:** Replace shell command construction with argument-safe process invocation or native messaging integration.
- **Disposition:** PR opened

### os/x_auth.js
- **Severity:** MEDIUM
- **Finding:** Token/config file writes are direct read-modify-write with no lock/merge guard, so concurrent writers can clobber env vars.
- **Suggestion:** Add atomic merge protection (file lock/version check) before persisting to `/root/.openclaw/openclaw.json`.
- **Disposition:** PR opened

### os/x_call.js
- **Severity:** MEDIUM
- **Finding:** `updateOpenClawEnvVars()` in refresh flow also performs unguarded full-file rewrites, risking lost updates under concurrent processes.
- **Suggestion:** Centralize config writes behind a locked/serialized writer helper shared with `os/x_auth.js`.
- **Disposition:** PR attempted

### os/model-routing.js
- **Severity:** LOW
- **Finding:** Fallback return model is hardcoded to `openai/gpt-5.2-chat-latest`, which is outside the declared allowlist/health table and can break deterministic routing guarantees.
- **Suggestion:** Use an allowlisted default fallback from `ROUTES.default` or fail closed with explicit “no available model” handling.
- **Disposition:** Logged only

### os/memory/x_digest.log.md
- **Severity:** LOW
- **Finding:** Logged endpoint format is inconsistent (`/2/users/me`) with status-only conventions used elsewhere (`GET /2/...`), reducing cross-log parsing consistency.
- **Suggestion:** Normalize endpoint logging to include method + path consistently (e.g., `GET /2/users/me`) in digest writers.
- **Disposition:** Logged only

### os/memory/x_engagement.log.md
- **Severity:** LOW
- **Finding:** File is empty but committed as runtime output, creating churn/noise without diagnostic value.
- **Suggestion:** Keep an explicit header/placeholder schema in the file or ignore runtime logs from git and generate on demand.
- **Disposition:** Logged only

### os/memory/x_budget_ledger.json
- **Severity:** LOW
- **Finding:** Ledger stores only one day and lacks rollover/retention metadata, making trend checks and anomaly debugging harder.
- **Suggestion:** Add bounded retention metadata (e.g., last_pruned_at, days_kept) and keep a small rolling window for deterministic audits.
- **Disposition:** Logged only

### skills/inbox_daily_triage.js
- **Severity:** HIGH
- **Finding:** Envelope omits required `taskType` and `taskSize`, so preflight validation fails and main script body is unreachable.
- **Suggestion:** Add valid `taskType`/`taskSize` fields to the envelope (or relax preflight requirements for skeleton mode).
- **Disposition:** PR opened

### skills/inbox_daily_triage_wrapper.js
- **Severity:** HIGH
- **Finding:** Envelope omits required `taskType` and `taskSize`, causing deterministic preflight rejection before wrapper logic runs.
- **Suggestion:** Include `taskType` and `taskSize` in the wrapper envelope to match current preflight contract.
- **Disposition:** PR opened

### skills/web_repo_audit.js
- **Severity:** HIGH
- **Finding:** Envelope omits required `taskType` and `taskSize`, making the audit path unreachable under current preflight rules.
- **Suggestion:** Populate `taskType` and `taskSize` in the envelope so the script can execute after validation.
- **Disposition:** PR opened

### skills/weekly_healthcheck.js
- **Severity:** HIGH
- **Finding:** Envelope omits required `taskType` and `taskSize`, so the healthcheck never reaches command execution.
- **Suggestion:** Add valid `taskType` and `taskSize` to the envelope to satisfy preflight.
- **Disposition:** PR opened

### skills/web_repo_audit.js
- **Severity:** MEDIUM
- **Finding:** `repoPath` is interpolated directly into shell commands, creating command-injection risk from environment input.
- **Suggestion:** Use `spawnSync`/argv arrays (or strict path validation/escaping) instead of string-interpolated shell commands.
- **Disposition:** PR opened

### skills/weekly_healthcheck.js
- **Severity:** MEDIUM
- **Finding:** `execSync` calls are not wrapped in error handling, so transient command failures crash the script without controlled output.
- **Suggestion:** Wrap command collection in try/catch and emit a structured failure summary with non-zero exit.
- **Disposition:** PR opened

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** Logging and envelope boilerplate duplicates patterns in other skill skeletons, increasing maintenance drift risk.
- **Suggestion:** Factor shared preflight/log scaffolding into a small helper used by all local skill skeleton scripts.
- **Disposition:** Logged only

### skills/pr-review/SKILL.md
- **Severity:** LOW
- **Finding:** Command examples hardcode an absolute workspace path, which is brittle across non-standard workspace locations and inconsistent with portable skill patterns.
- **Suggestion:** Use a workspace-relative invocation (or derive path from the current workspace root) instead of `/root/.openclaw/workspace/...`.
- **Disposition:** Logged only
