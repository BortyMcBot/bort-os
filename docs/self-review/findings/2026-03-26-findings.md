# Self-Review Findings — 2026-03-26

Generated by `self-review-job.mjs` on 2026-03-26T07:36:23.634Z

## Findings

### scripts/autonomous_execute_queue.mjs
- **Severity:** HIGH
- **Finding:** PR_BODY_PATH is still a single hardcoded process-wide file (`/tmp/autonomy_pr_body.md`), so multiple PR creations in the same run can overwrite each other and concurrent runs can collide.
- **Suggestion:** Generate a unique PR body temp path per task or per PR using `fs.mkdtempSync()` or a pid/timestamp-based filename.
- **Disposition:** PR attempted

### scripts/autonomous_execute_queue.mjs
- **Severity:** MEDIUM
- **Finding:** runCritical() still contains a redundant non-zero status branch even though run() already throws on any failing exit code before returning.
- **Suggestion:** Remove the unreachable status check and handle failures in only one layer.
- **Disposition:** PR attempted

### scripts/autonomous_execute_queue.mjs
- **Severity:** HIGH
- **Finding:** execBortOsTask() still stages `SECURITY.md` even though this task only writes `SECRETS.md`, which can fail when `SECURITY.md` is absent or unchanged.
- **Suggestion:** Stage only the files actually created or modified for that task.
- **Disposition:** PR attempted

### scripts/autonomous_generate_queue.mjs
- **Severity:** LOW
- **Finding:** The queue generator is fully hardcoded and ignores autonomous_policy.json, so the generate step remains a static seed file rather than policy-driven queue generation.
- **Suggestion:** Load queue candidates from config or discovery logic and keep only defaults/templates hardcoded.
- **Disposition:** Logged only

### scripts/autonomous_guard.mjs
- **Severity:** MEDIUM
- **Finding:** stopAutonomous() still ignores the exit status of `autonomous_stop.mjs`, so stop failures are silently swallowed.
- **Suggestion:** Capture the `spawnSync()` result and exit non-zero or log explicitly when the stop script fails.
- **Disposition:** PR attempted

### scripts/autonomous_report.mjs
- **Severity:** MEDIUM
- **Finding:** The Telegram send result is never checked, so report delivery can still fail silently after the markdown report is generated.
- **Suggestion:** Inspect the `openclaw message send` result and surface non-zero exit status explicitly.
- **Disposition:** PR attempted

### scripts/autonomous_runner.mjs
- **Severity:** LOW
- **Finding:** The comment says the queue is generated at the start of each autonomous window, but runOnce() regenerates it on every 10-minute tick, which is inconsistent with the documented behavior.
- **Suggestion:** Either generate once at startup or update the comment/naming to reflect per-tick regeneration.
- **Disposition:** Logged only

### scripts/autonomous_runner.mjs
- **Severity:** MEDIUM
- **Finding:** Child script paths are still hardcoded to `/root/.openclaw/workspace/scripts/...` instead of using a shared workspace/config source.
- **Suggestion:** Build child script paths from `BORT_WORKSPACE` or a shared constants module instead of embedding absolute paths.
- **Disposition:** PR attempted

### scripts/autonomous_start.mjs
- **Severity:** MEDIUM
- **Finding:** The hard-stop timer still uses `Math.max(0, stopMin - nowMin)`, so starting before midnight for a next-day stop time can result in an immediate or skipped stop instead of overnight scheduling.
- **Suggestion:** Handle wraparound across midnight by adding 24 hours when `stopMin` is earlier than `nowMin`.
- **Disposition:** PR attempted

### scripts/branch-cleanup.sh
- **Severity:** MEDIUM
- **Finding:** TELEGRAM_TARGET is still resolved with an inline node command that reads `/root/.openclaw/openclaw.json` directly, duplicating config access logic and hardcoding runtime paths.
- **Suggestion:** Centralize config lookup in a shared helper or use exported environment/constants instead of embedding JSON parsing in shell scripts.
- **Disposition:** PR attempted

### scripts/deploy.mjs
- **Severity:** LOW
- **Finding:** run() and runReadOnly() are identical wrappers around execSync(), which is redundant and diverges from the codebase’s usual single-command-helper pattern.
- **Suggestion:** Consolidate them into one helper and express intent via naming/comments instead of duplicate implementations.
- **Disposition:** Logged only

### scripts/export-bort-source.sh
- **Severity:** LOW
- **Finding:** The script hardcodes `/root/.openclaw/workspace` instead of using the same `BORT_WORKSPACE`/constants pattern used elsewhere.
- **Suggestion:** Read the workspace root from environment or a shared constants source for consistency.
- **Disposition:** Logged only

### scripts/export-project-source.mjs
- **Severity:** LOW
- **Finding:** buildTgzBundle() returns a boolean but the caller ignores failures, so export can appear successful even when the bundle is not created.
- **Suggestion:** Check the return value and fail loudly or log a warning when tar creation fails.
- **Disposition:** Logged only

### scripts/generate-bort-capabilities-md.mjs
- **Severity:** MEDIUM
- **Finding:** If readJson() fails it will now throw a clearer error, but the generator still does not catch and contextualize that failure at the top level, so one bad snapshot aborts the whole run.
- **Suggestion:** Catch snapshot read errors in the main flow and report which generated section failed before exiting.
- **Disposition:** PR attempted

### scripts/generate-project-source-core.mjs
- **Severity:** LOW
- **Finding:** extractRoutes() and similar regex-based parsers duplicate parsing logic already present in other generators/checkers, increasing maintenance drift when route syntax changes.
- **Suggestion:** Move shared route/hat extraction helpers into a common module and reuse them across scripts.
- **Disposition:** Logged only

### scripts/pinchtab-session.sh
- **Severity:** MEDIUM
- **Finding:** cmd_start() now removes stale PID files on timeout but does not wait for the killed child to actually exit, so a slow-to-die process can still linger briefly after the script reports failure.
- **Suggestion:** After sending kill, wait or poll briefly before exiting so cleanup is deterministic.
- **Disposition:** PR attempted

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** ghJson() is dead code and never called anywhere in the script.
- **Suggestion:** Remove the unused helper or replace callers with it if a generic JSON helper is actually desired.
- **Disposition:** Logged only

### scripts/pr-review-job.mjs
- **Severity:** LOW
- **Finding:** hasProjectSourceMd() and hasArchDrift() are defined but never used, leaving dead code and fragmented policy logic.
- **Suggestion:** Delete the unused helpers or integrate them into the review policy checks.
- **Disposition:** Logged only

### scripts/pr-review-job.mjs
- **Severity:** HIGH
- **Finding:** reviewPR() now skips some obvious rejects cheaply, but it still fetches full PR JSON and full diffs sequentially for all remaining open PRs, which can be slow and rate-limit prone as PR count grows.
- **Suggestion:** Batch or parallelize bounded metadata/diff fetches after cheap prechecks, or add more low-cost filters before full diff retrieval.
- **Disposition:** PR attempted

### scripts/repo_hygiene_check.sh
- **Severity:** MEDIUM
- **Finding:** TELEGRAM_CHAT_ID is still loaded via a hardcoded inline node parser against `/root/.openclaw/openclaw.json`, duplicating config lookup logic used in multiple shell scripts.
- **Suggestion:** Replace ad hoc JSON parsing with a shared helper or env/config abstraction.
- **Disposition:** PR attempted

### scripts/run-project-source-check.mjs
- **Severity:** MEDIUM
- **Finding:** The wrapper uses a hardcoded `/root/.openclaw/workspace` path for child execution instead of the shared workspace/constants pattern used elsewhere.
- **Suggestion:** Build the child script path from `BORT_WORKSPACE` or shared constants rather than embedding an absolute path.
- **Disposition:** PR attempted

### scripts/self-review-job.mjs
- **Severity:** LOW
- **Finding:** runWebResearch() creates `/tmp/self-review-research-*.txt` files but never uses them for the agent call, making the temp-file write dead work.
- **Suggestion:** Remove the unused temp files or actually feed them into the analysis workflow.
- **Disposition:** Logged only

### scripts/site-improvement-job.mjs
- **Severity:** LOW
- **Finding:** PINCHTAB_PID and PINCHTAB_BASE are declared but never used, which is dead code.
- **Suggestion:** Remove the unused constants or wire them into the Pinchtab lifecycle logic.
- **Disposition:** Logged only

### scripts/site-improvement-job.mjs
- **Severity:** MEDIUM
- **Finding:** The new changeResult validation assumes changed file paths appear as standalone whitespace-free lines, which is brittle and may misparse valid agent output formats or filenames with spaces.
- **Suggestion:** Parse a structured response format from the agent or validate only against explicit expected paths from suggested_change.
- **Disposition:** PR attempted

### integrations/gmail/daily-review.js
- **Severity:** MEDIUM
- **Finding:** The default prefs path still points at a committed in-repo file (`prefs-gobuffs10.json`), which conflicts with the newer runtime-path pattern used elsewhere for live account state.
- **Suggestion:** Change the default `--prefs` path to an ignored runtime location such as `~/.openclaw/state/gmail` and keep the committed file template-only.
- **Disposition:** PR attempted

### integrations/gmail/daily-summary.sh
- **Severity:** MEDIUM
- **Finding:** The script still defaults `PREFS` to an in-repo file even though nearby sweep tooling has moved live runtime state and config toward external runtime paths.
- **Suggestion:** Point the default prefs path at a runtime config location and keep the committed prefs file as a template/example only.
- **Disposition:** PR attempted

### integrations/gmail/inbox-audit.js
- **Severity:** LOW
- **Finding:** The help text still documents `--senderPages` default as 20 while the code default is 5, which is inconsistent and can mislead operators about scan coverage.
- **Suggestion:** Align the documented default with the implemented value or change the code default to match the help text.
- **Disposition:** Logged only

### integrations/gmail/other-prune.js
- **Severity:** LOW
- **Finding:** The compatibility wrapper silently forwards to `other-prune2.js`, so legacy callers get behavior changes without any explicit deprecation or migration signal.
- **Suggestion:** Print a short deprecation notice to stderr before forwarding so operators know they are using the resumable replacement.
- **Disposition:** Logged only

### integrations/gmail/other-prune2.js
- **Severity:** MEDIUM
- **Finding:** The default `statePath` still points at the committed in-repo `other-prune-state-gobuffs10.json`, which conflicts with the repo’s template-only convention for live runtime state.
- **Suggestion:** Move the default resumable state path to an ignored runtime location outside the repo and keep the committed JSON file template-only.
- **Disposition:** PR attempted

### integrations/gmail/package.json
- **Severity:** LOW
- **Finding:** package.json contains only dependencies and omits basic metadata/scripts, which is inconsistent with established package structure and makes common Gmail operations less discoverable.
- **Suggestion:** Add minimal package metadata and npm scripts for auth, test, daily-review, backlog-sweep, and audit commands.
- **Disposition:** Logged only

### integrations/gmail/setup-labels.js
- **Severity:** LOW
- **Finding:** The label list is still hardcoded in-script, which duplicates label configuration already described in RUNBOOK.md and other scripts.
- **Suggestion:** Centralize managed label names in shared config/constants so all Gmail scripts consume the same label set.
- **Disposition:** Logged only

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** `STATE` and `LOG` still default to committed in-repo paths even though active runtime state/logging has been moved to ignored runtime locations elsewhere in the Gmail tooling.
- **Suggestion:** Change the default `STATE` and `LOG` paths to the same runtime directory pattern used by `sweep-runner.sh`.
- **Disposition:** PR attempted

### integrations/gmail/status-ping.sh
- **Severity:** MEDIUM
- **Finding:** When no baseline is available the script silently sets `INITIAL` to the current unread count, which hides that progress is unknown and makes the reported percentage misleadingly look like 0.0%.
- **Suggestion:** Surface an explicit “baseline unknown” state instead of silently deriving `INITIAL` from the current unread count.
- **Disposition:** PR attempted

### integrations/gmail/sweep-runner.sh
- **Severity:** MEDIUM
- **Finding:** `PREFS` still defaults to an in-repo file even though the repo now treats committed account-specific prefs as template-only rather than live runtime config.
- **Suggestion:** Move the default prefs path to an ignored runtime config location to match the new state/log path convention.
- **Disposition:** PR attempted

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** test.js duplicates OAuth client loading logic that already exists in lib.js, creating unnecessary parallel auth code.
- **Suggestion:** Reuse `loadOAuthClient()` from lib.js and keep the test script focused on Gmail sanity checks only.
- **Disposition:** Logged only

### integrations/gmail/test.js
- **Severity:** LOW
- **Finding:** The required `--email` argument is only used as a CLI gate and never validated against the authenticated account, making it effectively dead input.
- **Suggestion:** Either remove `--email` entirely or compare it against `profile.data.emailAddress` and fail on mismatch.
- **Disposition:** Logged only

### os/constants.js
- **Severity:** LOW
- **Finding:** OPENCLAW_CONFIG_PATH and BORT_WORKSPACE still default to hardcoded absolute paths, which makes the module less portable despite being positioned as a shared constants source.
- **Suggestion:** Resolve these from a single runtime root or env configuration layer and keep hardcoded paths only as last-resort fallbacks.
- **Disposition:** Logged only

### os/help.js
- **Severity:** MEDIUM
- **Finding:** deriveFromHatsMd() now prefers hat-profiles.json but still hardcodes a canonical required-fields list locally, so envelope field policy is split between code and data instead of coming from one source of truth.
- **Suggestion:** Move the canonical required envelope fields into shared config/data alongside hat profiles and load them from there.
- **Disposition:** PR attempted

### os/model-routing.js
- **Severity:** LOW
- **Finding:** The ROUTES categories are all identical arrays, so the routing table still adds repetition without encoding any category-specific behavior.
- **Suggestion:** Consolidate to a shared default chain until categories genuinely diverge, or generate per-category routes from a smaller config object.
- **Disposition:** Logged only

### os/model-routing.js
- **Severity:** LOW
- **Finding:** isModelAvailable() accepts an envelope parameter but never uses it, which is dead API surface and hints at unfinished sensitivity-based gating.
- **Suggestion:** Remove the unused parameter or implement the intended envelope-aware policy checks in one place.
- **Disposition:** Logged only

### os/model-routing_selftest.js
- **Severity:** MEDIUM
- **Finding:** The self-test now carries expectedModel values inline for every case, which still duplicates the active routing policy instead of deriving expectations from a shared fixture/source.
- **Suggestion:** Load expected routing from shared config/test fixtures so the self-test cannot drift when model-routing.js changes.
- **Disposition:** PR attempted

### os/models_check.js
- **Severity:** MEDIUM
- **Finding:** The new dedicated snapshot file keeps only the latest catalog state, so historical comparison beyond one prior snapshot is still lost even though the original issue called for bounded retention rather than complete history removal.
- **Suggestion:** Retain the last N snapshots in a bounded sidecar state file instead of overwriting a single latest snapshot.
- **Disposition:** PR attempted

### os/models_inventory.js
- **Severity:** LOW
- **Finding:** The section label OpenAI fallback chain is inconsistent with the actual default route, which now includes an OpenRouter Claude fallback as well.
- **Suggestion:** Rename the section to Default fallback chain or similar so the output matches the configured model mix.
- **Disposition:** Logged only

### os/preflight.js
- **Severity:** MEDIUM
- **Finding:** runProjectSourceCheck() still ignores non-zero child exit status from spawnSync and only catches thrown exceptions, so child-process failures can silently disappear.
- **Suggestion:** Inspect the returned status/error fields from spawnSync and log a minimal warning when the source check exits non-zero.
- **Disposition:** PR attempted

### os/preflight.js
- **Severity:** LOW
- **Finding:** envelopeTemplate() still advertises hat: inbox|web|resale|ops-core and omits autonomous, which is inconsistent with the actual hat registry.
- **Suggestion:** Generate the template hat list from the loaded hat profiles instead of hardcoding it.
- **Disposition:** Logged only

### os/preflight.test.js
- **Severity:** LOW
- **Finding:** path is imported but never used, leaving dead code in the test harness.
- **Suggestion:** Remove the unused import to keep the test file tidy.
- **Disposition:** Logged only

### os/social_ops_execute_x.js
- **Severity:** LOW
- **Finding:** BIO is still loaded from config but never used because the bio-update step remains skipped, leaving dead config surface and dead variable state.
- **Suggestion:** Remove bio from this job’s config contract or implement the supported profile-update path behind an explicit capability check.
- **Disposition:** Logged only

### os/social_ops_follow_remaining.js
- **Severity:** MEDIUM
- **Finding:** The target list and hourly cap are now externalized but there is no validation for negative/zero/oversized values, so malformed config can silently disable or distort the job.
- **Suggestion:** Clamp and validate loaded config values before use and fall back to safe defaults when invalid.
- **Disposition:** PR attempted

### os/state_report.js
- **Severity:** MEDIUM
- **Finding:** preflightTestStatus() now uses a syntax check only, which no longer mutates logs but also no longer exercises any real runtime validation, so the reported status is weaker than the old intent.
- **Suggestion:** Replace it with a dedicated non-mutating preflight smoke check rather than a bare syntax-only command.
- **Disposition:** PR attempted

### os/x_budget.js
- **Severity:** LOW
- **Finding:** estimateCost() accepts actionType but never uses it, leaving dead parameter surface in the budget API.
- **Suggestion:** Remove the unused parameter or incorporate it into endpoint-cost fallback logic.
- **Disposition:** Logged only

### os/x_auth.js
- **Severity:** MEDIUM
- **Finding:** The new shared config writer still rewrites openclaw.json directly and is now duplicated as project-local logic rather than using the established gateway/config tooling path.
- **Suggestion:** Route env var mutations through the gateway/config mechanism or one truly canonical config writer shared across all callers.
- **Disposition:** PR attempted

### os/x_budget_checks.js
- **Severity:** MEDIUM
- **Finding:** The test isolates writes via process.chdir(tmpDir), which is fragile because it changes global process cwd and can still affect any future relative-path assumptions in the same process.
- **Suggestion:** Prefer injectable file paths or env-configured temp paths in x_budget rather than relying on a global cwd swap.
- **Disposition:** PR attempted

### os/x_call.js
- **Severity:** MEDIUM
- **Finding:** The shared openclaw env helper still rewrites the config file directly, so x_call.js no longer duplicates the writer but still follows a custom direct-write path instead of the standard gateway/config tooling.
- **Suggestion:** Migrate the shared env-var helper to the canonical gateway/config update path so all callers converge on one supported mechanism.
- **Disposition:** PR attempted

### os/x_daily_post.js
- **Severity:** LOW
- **Finding:** execSync is imported but never used, leaving dead code in the module imports.
- **Suggestion:** Remove the unused import.
- **Disposition:** Logged only

### os/x_digest_job.js
- **Severity:** MEDIUM
- **Finding:** appendDigest() now reloads the curated account file independently instead of reusing the already-loaded curated list from main(), so the allowlist source is centralized but still read redundantly.
- **Suggestion:** Pass the already-loaded curated list into appendDigest() to avoid duplicate file reads and split loading logic.
- **Disposition:** PR attempted

### os/x_digest_job.js
- **Severity:** MEDIUM
- **Finding:** The new curated sidecar file is another hardcoded config path with no validation, so malformed usernames or empty lists can silently break digest selection.
- **Suggestion:** Validate the loaded curated file shape and fall back safely when invalid or empty.
- **Disposition:** PR attempted

### os/x_engagement_job.js
- **Severity:** MEDIUM
- **Finding:** parsePostLog() prefers a structured state file first, but no writer in the reviewed files is shown maintaining x_post_results.json, so the new path may remain unused and the fragile markdown fallback still carries the real load.
- **Suggestion:** Ensure the posting flow writes the structured post-results state file or remove the dead preferred path until it exists.
- **Disposition:** PR attempted

### os/memory/x_digest.log.md
- **Severity:** LOW
- **Finding:** This committed placeholder still occupies the same active runtime log path used by the job code, so the repo continues to mix template documentation with live-output locations.
- **Suggestion:** Move the template note to docs/templates and reserve `os/memory/x_digest.log.md` exclusively for ignored runtime output.
- **Disposition:** Logged only

### os/memory/x_engagement.log.md
- **Severity:** LOW
- **Finding:** This committed placeholder still lives at the active runtime output path, which is inconsistent with the stated convention that live engagement logs should not exist in-repo.
- **Suggestion:** Move the placeholder text to a docs/template location and keep the runtime log path ignored and generated on demand.
- **Disposition:** Logged only

### skills/inbox_daily_triage.js
- **Severity:** LOW
- **Finding:** This file is still a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **Suggestion:** Either implement the promised dry-run integration path or remove/replace the stub with documentation until real behavior exists.
- **Disposition:** Logged only

### skills/inbox_daily_triage_wrapper.js
- **Severity:** LOW
- **Finding:** This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying action, which is redundant placeholder logic.
- **Suggestion:** Consolidate the wrapper and skeleton into one entrypoint or implement the actual delegated call path behind an explicit flag.
- **Disposition:** Logged only

### skills/web_repo_audit.js
- **Severity:** LOW
- **Finding:** On failure the script still prints raw stderr/message directly, which is inconsistent with the surrounding minimal structured output style used by these skill skeletons.
- **Suggestion:** Normalize failure output to a short structured message and log detailed diagnostics separately if needed.
- **Disposition:** Logged only
