# Self-Review Plan — 2026-03-29

Generated by `self-review-job.mjs` on 2026-03-29T13:11:44.615Z
Previous plan: plans/2026-03-28-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 41 total (1 HIGH, 1 MEDIUM, 39 LOW)
- **PRs opened:** 0

- **Staleness notifications sent:** 0

## Open Items

- **scripts/self-review-job.mjs** (LOW): runWebResearch() still writes /tmp/self-review-research-*.txt for every topic but never uses those files in the downstream call, leaving dead temp-file work in the flow.
- **scripts/autonomous_execute_queue.mjs** (LOW): execBortOsTask() and execPersonalWebsiteTask() still duplicate large chunks of branch/commit/push/PR workflow logic, which is redundant and will get harder to maintain as more task types are added.
- **scripts/autonomous_generate_queue.mjs** (LOW): seedQueue() is hardcoded to two specific repos and tasks, so changing autonomous work selection still requires editing code instead of adjusting config or data.
- **scripts/deploy.mjs** (LOW): run() and runReadOnly() are functionally identical execSync wrappers, so the second helper is redundant and adds maintenance noise without changing behavior.
- **scripts/export-bort-source.sh** (LOW): The script hardcodes /root/.openclaw/workspace for invoked scripts and output paths, which is inconsistent with workspace-aware scripts that honor BORT_WORKSPACE or shared constants.
- **scripts/update-bort-bundle.sh** (LOW): The script hardcodes /root/.openclaw/workspace and public/bort-bundle paths, reducing portability relative to the rest of the workspace-rooted tooling.
- **scripts/generate-state-of-bort.mjs** (LOW): The installed-skill section arbitrarily truncates output to 30 entries, which can silently omit available skills from the generated state snapshot.
- **scripts/arch-drift-check.mjs** (LOW): WATCH_FILES is hardcoded to only os/preflight.js and os/model-routing.js, so other architecture-relevant enforcement files can drift without being classified by this check.
- **scripts/autonomous_start.mjs** (LOW): autonomous_start.mjs still hardcodes /root/.openclaw/workspace and absolute child script paths instead of using the shared workspace constant pattern used by other updated scripts.
- **scripts/autonomous_stop.mjs** (LOW): autonomous_stop.mjs also hardcodes /root/.openclaw/workspace and the autonomous_report.mjs path, which is inconsistent with the codebase’s newer workspace-aware pattern.
- **scripts/repo_hygiene_check.sh** (LOW): The dirty-worktree alert path still unconditionally calls openclaw message send even when TELEGRAM_CHAT_ID is empty, which is inconsistent with the guarded notification behavior elsewhere in the script.
- **integrations/gmail/backlog-sweep.js** (LOW): backlog-sweep.js contains its own copy of mapLimit(), which is now duplicated across multiple Gmail scripts instead of following a shared utility pattern.
- **integrations/gmail/daily-review.js** (LOW): daily-review.js also carries a local copy of mapLimit(), creating redundant concurrency utility logic alongside backlog-sweep.js and inbox-audit.js.
- **integrations/gmail/inbox-audit.js** (LOW): inbox-audit.js includes a third local copy of mapLimit(), so the concurrency improvement introduced duplicated helper code instead of a shared utility.
- **integrations/gmail/test.js** (LOW): test.js manually reimplements OAuth credential and token loading instead of using the shared loadOAuthClient() helper from lib.js, creating redundant auth logic to maintain.
- **integrations/gmail/backlog-state-gobuffs10.json** (LOW): A runtime backlog state file template is still committed in-repo even though the file itself says real per-account state should live outside version control, which is inconsistent with the runtime-state pattern.
- **integrations/gmail/other-prune-state-gobuffs10.json** (LOW): This committed prune state file is another runtime-state artifact checked into the repo despite its own note saying real state should live outside version control.
- **integrations/gmail/progress-gobuffs10.json** (LOW): progress-gobuffs10.json is tracked in-repo even though it is labeled as template-only runtime progress state, which conflicts with the intended ignored runtime-state layout.
- **integrations/gmail/backlog-sweep.log** (LOW): A runtime log placeholder is committed in-repo even though it explicitly says active logs should live outside version control, which is inconsistent with the rest of the runtime-path guidance.
- **integrations/gmail/daily-summary.sh** (LOW): daily-summary.sh hardcodes gobuffs10-specific defaults for account slug and secret/prefs paths, making reuse for another inbox dependent on environment overrides instead of config.
- **integrations/gmail/sweep-runner.sh** (LOW): sweep-runner.sh also hardcodes gobuffs10-specific credential, token, prefs, and state defaults, which makes the runner less portable across Gmail accounts.
- **integrations/gmail/README.md** (LOW): README.md still centers a local token.json/credentials.json workflow inside integrations/gmail, while RUNBOOK.md and the newer scripts assume secrets and runtime state live under ~/.openclaw paths.
- **os/preflight.js** (LOW): loadHatProfiles() still embeds a large fallback hat-policy object that duplicates hat-profiles.json, creating redundant policy definitions that can drift from the JSON source of truth.
- **os/model-routing.js** (LOW): the lightweight, research_web, social_drafting, spec_large, and default route arrays are currently identical, so the routing table carries redundant categories without behavior differences.
- **os/models_inventory.js** (LOW): The heading "OpenAI fallback chain" is misleading because inv.routes.default includes non-OpenAI entries like OpenRouter Claude, which can misstate the actual routing chain.
- **os/social_ops_execute_x.js** (LOW): DEFAULT_CONFIG.bio is still loaded and returned by loadConfig() but never used because the script always skips the bio update step, leaving dead configuration data in the flow.
- **os/x_budget_checks.js** (LOW): The blocked-action check depends on a fallback path that records DAILY_CAP_USD only when the first guardOrQueue() unexpectedly succeeds, making the test less deterministic than the rest of the harness.
- **os/x_pricing.json** (LOW): x_pricing.json still contains only manual_required metadata and source URLs, so pricing remains effectively hardcoded in code instead of being truly config-driven.
- **os/memory/x_budget_ledger.json** (LOW): This committed ledger file is explicitly marked template-only runtime state, which is inconsistent with the stated pattern that live X budget state should live outside version control.
- **os/memory/x_digest.log.md** (LOW): A runtime digest log placeholder is committed in-repo even though the file says active digest logs should live in an ignored runtime path outside version control.
- **os/memory/x_digest_state.json** (LOW): This tracked digest cursor file is another template-only runtime state artifact that conflicts with the documented runtime-path convention.
- **os/memory/x_engagement.log.md** (LOW): The engagement log placeholder is stored in-repo despite stating that real engagement output belongs in an ignored runtime path, which is inconsistent with the intended storage model.
- **os/memory/x_queue.md** (LOW): x_queue.md is checked in as a placeholder even though it represents active append-only queue state in the running system, making it inconsistent with the external runtime-state guidance used by the neighboring files.
- **skills/inbox_daily_triage.js** (LOW): This file is still a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **skills/inbox_daily_triage_wrapper.js** (LOW): This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying work, creating redundant no-op logic.
- **skills/web_repo_audit.js** (LOW): Repo audit failures print stderr and exit without appending a failure log entry, which is inconsistent with the other skeletons’ memory-log pattern.
- **skills/weekly_healthcheck.js** (LOW): The script shells out to hardcoded commands free, df, and uptime directly, which makes the health snapshot behavior less configurable than the rest of the workspace tooling.
- **skills/README.md** (LOW): The README promises lightweight read-only skill scripts in general terms, but the current directory mostly contains placeholders/no-op skeletons, which can mislead operators about implementation maturity.
- **skills/pr-review/SKILL.md** (LOW): The command path is hardcoded to /root/.openclaw/workspace/scripts/pr-review-job.mjs, which is inconsistent with more portable workspace-relative patterns used elsewhere.



## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
