# Self-Review Plan — 2026-03-27

Generated by `self-review-job.mjs` on 2026-03-27T07:25:41.432Z
Previous plan: plans/2026-03-26-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 27 total (0 HIGH, 8 MEDIUM, 19 LOW)
- **PRs opened:** 0

- **Staleness notifications sent:** 0

## Open Items

- **scripts/deploy.mjs** (LOW): run() and runReadOnly() are identical wrappers around execSync(), which is redundant and diverges from the codebase’s usual single-command-helper pattern.
- **scripts/export-bort-source.sh** (LOW): The script hardcodes /root/.openclaw/workspace instead of using the same BORT_WORKSPACE/constants pattern used by other scripts.
- **scripts/export-project-source.mjs** (LOW): buildTgzBundle() returns a boolean but the caller ignores failures, so export can appear successful even when the bundle is not created.
- **scripts/generate-project-source-core.mjs** (LOW): extractRoutes() and similar regex-based parsers duplicate parsing logic already present in other generators/checkers, increasing maintenance drift when route syntax changes.
- **scripts/pr-review-job.mjs** (LOW): ghJson() is dead code and never called anywhere in the script.
- **scripts/pr-review-job.mjs** (LOW): hasProjectSourceMd() and hasArchDrift() are defined but never used, leaving dead code and fragmented policy logic.
- **scripts/self-review-job.mjs** (LOW): runWebResearch() creates /tmp/self-review-research-*.txt files but never uses them for the agent call, making the temp-file write dead work.
- **scripts/site-improvement-job.mjs** (LOW): PINCHTAB_PID and PINCHTAB_BASE are declared but never used, which is dead code.
- **integrations/gmail/inbox-audit.js** (LOW): The help text still documents `--senderPages` default as 20 while the code default is 5, which is inconsistent and can mislead operators about scan coverage.
- **integrations/gmail/other-prune.js** (LOW): The compatibility wrapper silently forwards to `other-prune2.js`, so legacy callers get behavior changes without any explicit deprecation or migration signal.
- **integrations/gmail/package.json** (LOW): package.json contains only dependencies and omits basic metadata/scripts, which is inconsistent with established package structure and makes common Gmail operations less discoverable.
- **integrations/gmail/setup-labels.js** (LOW): The label list is still hardcoded in-script, which duplicates label configuration already described in RUNBOOK.md and other scripts.
- **integrations/gmail/test.js** (LOW): test.js duplicates OAuth client loading logic that already exists in lib.js, creating unnecessary parallel auth code.
- **integrations/gmail/test.js** (LOW): The required `--email` argument is only used as a CLI gate and never validated against the authenticated account, making it effectively dead input.
- **os/memory/x_digest.log.md** (LOW): This committed placeholder still occupies the same active runtime log path used by the digest job, so the repo continues to mix template documentation with live-output locations.
- **os/memory/x_engagement.log.md** (LOW): This committed placeholder still lives at the active runtime output path, which is inconsistent with the stated convention that live engagement logs should not exist in-repo.
- **skills/inbox_daily_triage.js** (LOW): This file is still a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **skills/inbox_daily_triage_wrapper.js** (LOW): This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying action, which is redundant placeholder logic.
- **skills/web_repo_audit.js** (LOW): On failure the script still prints raw stderr/message directly, which is inconsistent with the surrounding minimal structured output style used by these skill skeletons.



## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
