# Self-Review Plan — 2026-04-02

Generated by `self-review-job.mjs` on 2026-04-02T07:11:16.240Z
Previous plan: plans/2026-04-01-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 59 total (0 HIGH, 1 MEDIUM, 58 LOW)
- **PRs opened:** 0

- **Staleness notifications sent:** 1

## Open Items

- **scripts/arch-drift-check.mjs** (LOW): WATCH_FILES is hardcoded to only os/preflight.js and os/model-routing.js, so other architecture-relevant enforcement files can drift without being classified by this check.
- **scripts/autonomous_execute_queue.mjs** (LOW): execBortOsTask() and execPersonalWebsiteTask() duplicate large chunks of branch/commit/push/PR workflow logic, which is redundant and will get harder to maintain as more task types are added.
- **scripts/autonomous_generate_queue.mjs** (LOW): seedQueue() is hardcoded to two specific repos and tasks, so changing autonomous work selection still requires editing code instead of adjusting config or data.
- **scripts/autonomous_report.mjs** (LOW): autonomous_report.mjs always attempts a Telegram send even when TELEGRAM_CHAT_ID is unset, which is inconsistent with the guarded notification pattern used elsewhere.
- **scripts/autonomous_runner.mjs** (LOW): autonomous_runner.mjs runs immediately and then every 10 minutes without any lock or singleton guard, so duplicate starts can cause overlapping queue executions.
- **scripts/autonomous_start.mjs** (LOW): autonomous_start.mjs still hardcodes /root/.openclaw/workspace and absolute child script paths instead of using the shared workspace constant pattern used by other scripts.
- **scripts/autonomous_stop.mjs** (LOW): autonomous_stop.mjs also hardcodes /root/.openclaw/workspace and the autonomous_report.mjs path, which is inconsistent with the codebase’s newer workspace-aware pattern.
- **scripts/deploy.mjs** (LOW): run() and runReadOnly() are functionally identical execSync wrappers, so the second helper is redundant and adds maintenance noise without changing behavior.
- **scripts/export-bort-source.sh** (LOW): The script hardcodes /root/.openclaw/workspace for invoked scripts and output paths, which is inconsistent with workspace-aware scripts that honor BORT_WORKSPACE or shared constants.
- **scripts/export-project-source.mjs** (LOW): export-project-source.mjs hardcodes workspace, dist, and upload paths instead of following the shared constants/workspace-aware pattern used elsewhere.
- **scripts/generate-bort-capabilities-md.mjs** (LOW): generate-bort-capabilities-md.mjs hardcodes snapshot and output paths under /root/.openclaw/workspace, which is inconsistent with workspace-aware script patterns elsewhere.
- **scripts/generate-project-source-core.mjs** (LOW): generate-project-source-core.mjs hardcodes workspace and config file locations, reducing portability relative to scripts that import shared constants.
- **scripts/generate-state-of-bort.mjs** (LOW): The installed-skill section arbitrarily truncates output to 30 entries, which can silently omit available skills from the generated state snapshot.
- **scripts/pr-review-job.mjs** (LOW): ghJson() is defined but never used, leaving dead helper code in the PR review job.
- **scripts/refresh-project-source.mjs** (LOW): refresh-project-source.mjs hardcodes absolute child script paths instead of using a shared workspace constant, making it inconsistent with workspace-aware patterns elsewhere.
- **scripts/repo_hygiene_check.sh** (LOW): The dirty-worktree alert path still unconditionally calls openclaw message send even when TELEGRAM_CHAT_ID is empty, which is inconsistent with the guarded notification behavior elsewhere in the script.
- **scripts/self-review-job.mjs** (LOW): runWebResearch() still writes /tmp/self-review-research-*.txt for every topic but never uses those files in the downstream call, leaving dead temp-file work in the flow.
- **scripts/site-improvement-job.mjs** (LOW): PINCHTAB_PID and PINCHTAB_BASE are declared but never used, leaving dead configuration constants in the site improvement job.
- **scripts/update-bort-bundle.sh** (LOW): The script hardcodes /root/.openclaw/workspace and public/bort-bundle paths, reducing portability relative to the rest of the workspace-rooted tooling.
- **integrations/gmail/backlog-sweep.js** (LOW): backlog-sweep.js contains its own copy of mapLimit(), which duplicates the same bounded-concurrency helper logic also present in daily-review.js, inbox-audit.js, and unsubscribe.js.
- **integrations/gmail/daily-review.js** (LOW): daily-review.js also carries a local mapLimit() implementation, creating redundant concurrency utility code instead of following a shared helper pattern.
- **integrations/gmail/inbox-audit.js** (LOW): inbox-audit.js includes another local copy of mapLimit(), so the same worker-pool logic is repeated across multiple Gmail scripts.
- **integrations/gmail/unsubscribe.js** (LOW): unsubscribe.js defines yet another local mapLimit() helper instead of using a shared concurrency utility, adding more duplicate support code to maintain.
- **integrations/gmail/test.js** (LOW): test.js manually reimplements OAuth credential and token loading instead of using the shared loadOAuthClient() helper from lib.js, creating redundant auth logic.
- **integrations/gmail/other-prune.js** (LOW): other-prune.js is now only a legacy passthrough wrapper around other-prune2.js, leaving a compatibility shim that adds another entrypoint without functional value.
- **integrations/gmail/backlog-state-gobuffs10.json** (LOW): A runtime backlog state template is still committed in-repo even though the file says real per-account state should live outside version control, which is inconsistent with the runtime-state pattern.
- **integrations/gmail/other-prune-state-gobuffs10.json** (LOW): This committed prune state file is another runtime-state artifact checked into the repo despite its own note saying real state should live outside version control.
- **integrations/gmail/progress-gobuffs10.json** (LOW): progress-gobuffs10.json is tracked in-repo even though it is labeled as template-only runtime progress state, which conflicts with the intended ignored runtime-state layout.
- **integrations/gmail/backlog-sweep.log** (LOW): A runtime log placeholder is committed in-repo even though it explicitly says active logs should live outside version control, which is inconsistent with the runtime-path guidance elsewhere.
- **integrations/gmail/prefs-gobuffs10.json** (LOW): prefs-gobuffs10.json is committed in-repo despite its own note saying live account prefs should be provided via an explicit runtime prefs path instead of being stored in version control.
- **integrations/gmail/daily-summary.sh** (LOW): daily-summary.sh hardcodes gobuffs10-specific defaults for account slug and secret or prefs paths, making reuse for another inbox depend on environment overrides instead of shared config.
- **integrations/gmail/status-ping.sh** (LOW): status-ping.sh also hardcodes gobuffs10-specific credential, token, and state defaults, which makes the operational status flow less portable across Gmail accounts.
- **integrations/gmail/sweep-runner.sh** (LOW): sweep-runner.sh hardcodes gobuffs10-specific credential, token, prefs, and state defaults, which is inconsistent with a config-driven multi-account integration pattern.
- **integrations/gmail/README.md** (LOW): README.md still centers a local credentials.json or token.json workflow inside integrations/gmail, while RUNBOOK.md and the newer scripts assume secrets and runtime state live under ~/.openclaw paths.
- **os/x_call.js** (LOW): x_call.js duplicates the same OpenClaw env-var load/update file-lock logic found in x_auth.js instead of sharing one credential/config helper.
- **os/model-routing.js** (LOW): ROUTES repeats nearly the same Codex-first fallback chain in every category, creating redundant routing tables that are harder to keep aligned.
- **os/model-routing_selftest.js** (LOW): model-routing_selftest.js reports expectedModel from inventory().routes.default[0] for every case even when category-specific routes are being tested, which weakens the value of the self-test output.
- **os/social_ops_execute_x.js** (LOW): social_ops_execute_x.js loads a configurable bio value but then always skips the bio-update step, leaving dead config and unused data in the flow.
- **os/social_ops_follow_remaining.js** (LOW): social_ops_follow_remaining.js hardcodes a default target list in code, which makes follow policy changes require edits instead of configuration.
- **os/state_report.js** (LOW): state_report.js still falls back to parsing legacy memory/hats.md even though preflight.js and hat-profiles.json are now the clearer source-of-truth path.
- **os/help.js** (LOW): help.js also includes a legacy memory/hats.md parsing fallback, which is inconsistent with the newer hat-profiles.json and preflight-driven source-of-truth pattern.
- **os/preflight.js** (LOW): loadHatProfiles() embeds a full hardcoded fallback copy of the hat definitions, duplicating policy data that already lives in os/hat-profiles.json.
- **os/memory-log.js** (LOW): hatFilePath() hardcodes a partial hat-to-file map and throws on unknown hats, which will require code edits whenever a new hat is added in hat-profiles.json.
- **os/models_check.js** (LOW): recommendRouting() defines recResearch using openrouter/openai/o3-mini-high, but model-routing.js intentionally filters OpenRouter OpenAI models unless explicitly requested, making this recommendation inconsistent with actual routing policy.
- **os/x_daily_post.js** (LOW): x_daily_post.js imports fs, execSync, and RESULTS_PATH but only uses fs and RESULTS_PATH, leaving at least one unused child_process import in the script.
- **os/x_digest_job.js** (LOW): x_digest_job.js hardcodes the curated allowlist in code and separately hardcodes the same list again in digest output text, creating duplicated policy data that can drift.
- **os/x_pricing.json** (LOW): x_pricing.json is a manual-placeholder status file rather than machine-usable pricing data, so x_call.js falls back to defaults instead of having a real config-driven pricing source.
- **os/memory/x_budget_ledger.json** (LOW): This committed ledger file is explicitly marked template-only runtime state, which is inconsistent with the intended pattern of keeping live X budget state outside version control.
- **os/memory/x_digest.log.md** (LOW): The digest log placeholder is committed in-repo even though it says active runtime digest logs should live outside version control, which conflicts with the runtime-state pattern.
- **os/memory/x_digest_state.json** (LOW): This digest cursor file is another tracked template-only runtime state artifact despite its own note saying real state should live outside version control.
- **os/memory/x_engagement.log.md** (LOW): The engagement log placeholder is committed in-repo even though the file says active runtime output should live in an ignored runtime path, which is inconsistent with the codebase’s runtime-state guidance.
- **os/memory/x_queue.md** (LOW): x_queue.md is tracked in-repo even though it is labeled as a schema/template placeholder and says active runtime queue state should live outside version control.
- **skills/inbox_daily_triage.js** (LOW): inbox_daily_triage.js is a placeholder that hardcodes a fixed skeleton envelope and never calls the underlying Gmail review flow, leaving dead-end skill logic in place.
- **skills/inbox_daily_triage_wrapper.js** (LOW): inbox_daily_triage_wrapper.js is another no-op placeholder that duplicates the same envelope, logging, and messaging pattern as inbox_daily_triage.js without adding functional behavior.
- **skills/web_repo_audit.js** (LOW): web_repo_audit.js manually duplicates the same preflight, executionHeader, appendLog, appendHatLog, and timestamp scaffolding pattern used across the local skill scripts.
- **skills/weekly_healthcheck.js** (LOW): weekly_healthcheck.js repeats the same preflight and memory-log scaffolding used by the other local skills, which creates redundant boilerplate across the skill directory.
- **skills/README.md** (LOW): README.md states these scripts must be read-only by default, but the directory still contains multiple skeleton/no-op entrypoints that are documented like real skills, which is inconsistent with a production-ready skill catalog.
- **skills/pr-review/SKILL.md** (LOW): SKILL.md hardcodes the absolute workspace path to scripts/pr-review-job.mjs instead of following the codebase’s newer workspace-aware or relative-path pattern.

- PR #163 'fix: [SELF-REVIEW] loadHatProfiles() embeds a full fallback hat policy object' — 7 days old (notification sent)

## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
