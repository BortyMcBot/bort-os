# Self-Review Plan — 2026-03-15

Generated by `self-review-job.mjs` on 2026-03-15T07:25:06.299Z
Previous plan: plans/2026-03-11-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 46 total (10 HIGH, 22 MEDIUM, 14 LOW)
- **PRs opened:** 18
  - fix: [SELF-REVIEW] `JSON.parse` on `autonomous_state.json` is unguarded and can — https://github.com/BortyMcBot/bort-os/pull/27
  - fix: [SELF-REVIEW] `node -p "require('${REPO}/os/constants').TELEGRAM_CHAT_ID"` — https://github.com/BortyMcBot/bort-os/pull/28
  - fix: [SELF-REVIEW] `curl` health checks have no timeout flags, so health probes — https://github.com/BortyMcBot/bort-os/pull/29
  - fix: [SELF-REVIEW] Script assumes `$PUB` exists and writable, but never creates — https://github.com/BortyMcBot/bort-os/pull/30
  - fix: [SELF-REVIEW] Script hardcodes credentials/token/state/log absolute paths  — https://github.com/BortyMcBot/bort-os/pull/31
  - fix: [SELF-REVIEW] Runtime knobs and secret paths are hardcoded (batch/query/cr — https://github.com/BortyMcBot/bort-os/pull/32
  - fix: [SELF-REVIEW] Gmail list/get/modify calls run without retry/backoff handli — https://github.com/BortyMcBot/bort-os/pull/33
  - fix: [SELF-REVIEW] `httpRequest` has no timeout handling, so unsubscribe calls  — https://github.com/BortyMcBot/bort-os/pull/34
  - fix: [SELF-REVIEW] Runtime state file stores massive offender/thread maps in-re — https://github.com/BortyMcBot/bort-os/pull/35
  - fix: [SELF-REVIEW] Self-test expectations reference models not present in route — https://github.com/BortyMcBot/bort-os/pull/36
  - fix: [SELF-REVIEW] `TELEGRAM_CHAT_ID` is hardcoded in source, reducing portabil — https://github.com/BortyMcBot/bort-os/pull/37
  - fix: [SELF-REVIEW] Notifications are sent via `execSync` shell command construc — https://github.com/BortyMcBot/bort-os/pull/38
  - fix: [SELF-REVIEW] Notification path also relies on `execSync` with interpolate — https://github.com/BortyMcBot/bort-os/pull/39
  - fix: [SELF-REVIEW] Token/config file writes are direct read-modify-write with n — https://github.com/BortyMcBot/bort-os/pull/40
  - fix: [SELF-REVIEW] Envelope omits required `taskType` and `taskSize`, so prefli — https://github.com/BortyMcBot/bort-os/pull/41
  - fix: [SELF-REVIEW] Envelope omits required `taskType` and `taskSize`, causing d — https://github.com/BortyMcBot/bort-os/pull/42
  - fix: [SELF-REVIEW] Envelope omits required `taskType` and `taskSize`, making th — https://github.com/BortyMcBot/bort-os/pull/43
  - fix: [SELF-REVIEW] Envelope omits required `taskType` and `taskSize`, so the he — https://github.com/BortyMcBot/bort-os/pull/44
- **Staleness notifications sent:** 0

## Open Items

- **scripts/autonomous_start.mjs** (LOW): Stop behavior is duplicated across guard loop and detached sleep timer, creating redundant control paths.
- **scripts/pr-review-job.mjs** (LOW): `ghJson` helper is dead code and never called.
- **scripts/export-project-source.mjs** (LOW): Canonical file list is hardcoded here and duplicated in `update-bort-bundle.sh`, increasing drift risk.
- **integrations/gmail/count-threads.js** (LOW): OAuth loading and backoff logic duplicate functionality already centralized in `lib.js`, creating pattern drift.
- **integrations/gmail/daily-review.js** (LOW): Archive-after-rule performs a second `threads.modify` call per matching thread, adding avoidable API overhead.
- **integrations/gmail/test.js** (LOW): `--email` is required but never used after validation, which is dead argument logic.
- **integrations/gmail/backlog-sweep.log** (LOW): Repository-tracked operational log is extremely large and contains volatile runtime data, which hurts reviewability and performance.
- **os/social_ops_execute_x.js** (LOW): `BIO` constant is dead code because bio update is always skipped and the variable is never used.
- **os/model-routing.js** (LOW): Fallback return model is hardcoded to `openai/gpt-5.2-chat-latest`, which is outside the declared allowlist/health table and can break deterministic routing guarantees.
- **os/memory/x_digest.log.md** (LOW): Logged endpoint format is inconsistent (`/2/users/me`) with status-only conventions used elsewhere (`GET /2/...`), reducing cross-log parsing consistency.
- **os/memory/x_engagement.log.md** (LOW): File is empty but committed as runtime output, creating churn/noise without diagnostic value.
- **os/memory/x_budget_ledger.json** (LOW): Ledger stores only one day and lacks rollover/retention metadata, making trend checks and anomaly debugging harder.
- **skills/inbox_daily_triage.js** (LOW): Logging and envelope boilerplate duplicates patterns in other skill skeletons, increasing maintenance drift risk.
- **skills/pr-review/SKILL.md** (LOW): Command examples hardcode an absolute workspace path, which is brittle across non-standard workspace locations and inconsistent with portable skill patterns.



## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
