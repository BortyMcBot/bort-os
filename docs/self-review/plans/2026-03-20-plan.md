# Self-Review Plan — 2026-03-20

Generated by `self-review-job.mjs` on 2026-03-20T07:43:22.062Z
Previous plan: plans/2026-03-19-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 77 total (13 HIGH, 40 MEDIUM, 24 LOW)
- **PRs opened:** 36
  - fix: [SELF-REVIEW] The script uses require('child_process') inside an ES — https://github.com/BortyMcBot/bort-os/pull/97
  - fix: [SELF-REVIEW] PR collection ignores gh command failures and silently — https://github.com/BortyMcBot/bort-os/pull/98
  - fix: [SELF-REVIEW] All git/gh command exit codes are ignored, so branch — https://github.com/BortyMcBot/bort-os/pull/99
  - fix: [SELF-REVIEW] The until argument is not validated beyond presence, so — https://github.com/BortyMcBot/bort-os/pull/100
  - fix: [SELF-REVIEW] Deployment decides whether to restart by diffing only — https://github.com/BortyMcBot/bort-os/pull/101
  - fix: [SELF-REVIEW] countFiles(WORKSPACE) recursively walks the entire — https://github.com/BortyMcBot/bort-os/pull/102
  - fix: [SELF-REVIEW] runDeploy() executes during --dry-run after an APPROVE — https://github.com/BortyMcBot/bort-os/pull/103
  - fix: [SELF-REVIEW] The script auto-adds and commits all workspace changes with — https://github.com/BortyMcBot/bort-os/pull/104
  - fix: [SELF-REVIEW] STALENESS_STATE_PATH is hardcoded to — https://github.com/BortyMcBot/bort-os/pull/105
  - fix: [SELF-REVIEW] writeTodo() emits Markdown tables containing unescaped — https://github.com/BortyMcBot/bort-os/pull/106
  - fix: [SELF-REVIEW] The script copies a hardcoded file list into the public — https://github.com/BortyMcBot/bort-os/pull/107
  - fix: [SELF-REVIEW] Runtime sweep state including detailed offender maps is — https://github.com/BortyMcBot/bort-os/pull/108
  - fix: [SELF-REVIEW] The tracked state file contains thread IDs, sender — https://github.com/BortyMcBot/bort-os/pull/109
  - fix: [SELF-REVIEW] Account-specific progress state is committed to the repo — https://github.com/BortyMcBot/bort-os/pull/110
  - fix: [SELF-REVIEW] The integration hardcodes a single account-specific prefs — https://github.com/BortyMcBot/bort-os/pull/111
  - fix: [SELF-REVIEW] loadOAuthClient() does not validate parsed credential — https://github.com/BortyMcBot/bort-os/pull/112
  - fix: [SELF-REVIEW] The code computes bucket but never uses it after — https://github.com/BortyMcBot/bort-os/pull/113
  - fix: [SELF-REVIEW] This script duplicates OAuth loading and backoff logic that — https://github.com/BortyMcBot/bort-os/pull/114
  - fix: [SELF-REVIEW] The implementation removes UNREAD for spamReview even — https://github.com/BortyMcBot/bort-os/pull/115
  - fix: [SELF-REVIEW] The script hardcodes /tmp/gmail-daily.json inside the — https://github.com/BortyMcBot/bort-os/pull/116
  - fix: [SELF-REVIEW] scanTopSenders() makes one threads.get call per thread for — https://github.com/BortyMcBot/bort-os/pull/117
  - fix: [SELF-REVIEW] The prune flow does a full two-pass scan with one metadata — https://github.com/BortyMcBot/bort-os/pull/118
  - fix: [SELF-REVIEW] Gmail API calls in listThreadsPage(), threads.get, and — https://github.com/BortyMcBot/bort-os/pull/119
  - fix: [SELF-REVIEW] The script parses JSON with console.log(...) inside node — https://github.com/BortyMcBot/bort-os/pull/120
  - fix: [SELF-REVIEW] parseMailto() uses new URL(mailtoUrl) without guarding — https://github.com/BortyMcBot/bort-os/pull/121
  - fix: [SELF-REVIEW] The script iterates inv.openaiFallbackChain, but — https://github.com/BortyMcBot/bort-os/pull/122
  - fix: [SELF-REVIEW] latestCommitSubjects() calls execSync(...) but only — https://github.com/BortyMcBot/bort-os/pull/123
  - fix: [SELF-REVIEW] The post-auth if (args.dryRun) branch is unreachable — https://github.com/BortyMcBot/bort-os/pull/124
  - fix: [SELF-REVIEW] updateOpenClawEnvVars() rewrites — https://github.com/BortyMcBot/bort-os/pull/125
  - fix: [SELF-REVIEW] loadHatProfiles() synchronously reads and parses [HIGH RISK] — https://github.com/BortyMcBot/bort-os/pull/126
  - fix: [SELF-REVIEW] Several test comments and expectations describe models like — https://github.com/BortyMcBot/bort-os/pull/127
  - fix: [SELF-REVIEW] runProjectSourceCheck() ignores the child process exit [HIGH RISK] — https://github.com/BortyMcBot/bort-os/pull/128
  - fix: [SELF-REVIEW] The report hardcodes preflight tests: PASS (14 passed / 0 — https://github.com/BortyMcBot/bort-os/pull/129
  - fix: [SELF-REVIEW] notify() shells out via execSync and interpolates — https://github.com/BortyMcBot/bort-os/pull/130
  - fix: [SELF-REVIEW] lastDigestEntry() relies on brittle string slicing around — https://github.com/BortyMcBot/bort-os/pull/131
  - fix: [SELF-REVIEW] The script exits early on missing/unsafe WEB_REPO_PATH — https://github.com/BortyMcBot/bort-os/pull/132
- **Staleness notifications sent:** 0

## Open Items

- **scripts/autonomous_runner.mjs** (LOW): `RUN_QUEUE` points to `memory/autonomous_queue.md` while the generator/executor use `memory/autonomous_queue.json`, leaving the seeded markdown queue unused.
- **scripts/autonomous_execute_queue.mjs** (LOW): `task` is accepted by `execBortOsTask()` and `execPersonalWebsiteTask()` but only a single hardcoded task ID is handled in each function, making the queue format more general than the implementation.
- **scripts/export-project-source.mjs** (LOW): `buildTgzBundle()` discards tar stderr and its boolean result is ignored, so bundle creation failures are silently swallowed while the export continues.
- **scripts/generate-bort-capabilities-md.mjs** (LOW): Input and output paths are hardcoded to `/root/.openclaw/workspace/source/...`, which breaks the workspace/path pattern used by the rest of the scripts.
- **scripts/pr-review-job.mjs** (LOW): `ghJson()` is dead code and is never called anywhere in the script.
- **scripts/site-improvement-job.mjs** (LOW): `PINCHTAB_PID` and `PINCHTAB_BASE` are defined but never used.
- **integrations/gmail/lib.js** (LOW): `getOrCreateLabel()` lists all labels on every call, so scripts creating five labels perform five redundant label-list API requests.
- **integrations/gmail/classify.js** (LOW): `looksImportant()` rebuilds lowercased allow/block/keyword arrays on every message classification, creating avoidable repeated work in large sweeps.
- **integrations/gmail/backlog-sweep.js** (LOW): The default prefs and state filenames are hardcoded to the gobuffs10 account, reducing portability despite the script already taking creds and token paths.
- **integrations/gmail/count-unread.js** (LOW): The script imports `googleapis` only to instantiate Gmail after `loadOAuthClient()`, while every other Gmail utility follows a mixed but more centralized helper approach.
- **integrations/gmail/test.js** (LOW): The `--email` argument is required only for OAuth account selection messaging but is never validated against the loaded token or used in API calls.
- **os/preflight.js** (LOW): The fallback hat profile embedded in `loadHatProfiles()` duplicates config already stored in `os/hat-profiles.json`, creating two sources of truth that can drift.
- **os/preflight.test.js** (LOW): `path` is imported but never used.
- **os/social_ops_execute_x.js** (LOW): The `BIO` constant is dead code because bio updates are always skipped and the value is never read.
- **os/state_report.js** (LOW): Per-task routing fallback labels mention models like `openai/gpt-5.2-pro`, which are not part of the current routing allowlist and may not match real fallback behavior.
- **os/constants.js** (LOW): `resolveTelegramChatId()` hardcodes `/root/.openclaw/openclaw.json`, which makes the helper host-specific despite already exposing `BORT_WORKSPACE` as configurable.
- **os/x_budget_checks.js** (LOW): The “blocked” check is not deterministic because `guardOrQueue()` usually tests a small real endpoint cost and may never hit the cap, leaving the test to fake success by appending a synthetic queue entry.
- **os/x_pricing.json** (LOW): The file is treated as pricing input by `os/x_call.js`, but it currently contains only a manual-required note and no usable pricing schema, making its role ambiguous.
- **os/memory/x_digest_state.json** (LOW): The file contains only a mutable cursor (`nextIndex`) and is runtime state rather than reusable project source.
- **skills/inbox_daily_triage.js** (LOW): This file duplicates the same preflight/header/logging skeleton behavior already implemented by `skills/inbox_daily_triage_wrapper.js`, leaving two near-identical no-op entrypoints to maintain.
- **skills/inbox_daily_triage_wrapper.js** (LOW): The wrapper is effectively dead code because it never invokes any underlying triage logic and only repeats the common skeleton pattern.
- **skills/web_repo_audit.js** (LOW): `repoPath` validation is handled after preflight/output setup instead of through the same envelope/config validation pattern used elsewhere, creating split validation paths.
- **skills/weekly_healthcheck.js** (LOW): The healthcheck shells out with hardcoded commands (`free -h`, `df -h /`, `uptime`) rather than using config-driven targets or a shared command wrapper, which limits portability.
- **skills/pr-review/SKILL.md** (LOW): The command path is hardcoded to `/root/.openclaw/workspace/scripts/pr-review-job.mjs`, which makes the skill less portable across workspaces and hosts.



## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
