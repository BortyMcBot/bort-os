# Self-Review Plan — 2026-03-28

Generated by `self-review-job.mjs` on 2026-03-28T07:19:12.808Z
Previous plan: plans/2026-03-27-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 50 total (3 HIGH, 12 MEDIUM, 35 LOW)
- **PRs opened:** 0

- **Staleness notifications sent:** 0

## Open Items

- **scripts/self-review-job.mjs** (LOW): runWebResearch() writes /tmp/self-review-research-*.txt for every topic but never uses those files for prompting or later processing, making the temp-file work dead code.
- **scripts/deploy.mjs** (LOW): run() and runReadOnly() are functionally identical wrappers around execSync, which duplicates logic without changing behavior.
- **scripts/repo_hygiene_check.sh** (LOW): The script unconditionally calls openclaw message send in the dirty/non-allowlist branch even when TELEGRAM_CHAT_ID is empty, unlike its guarded notification behavior elsewhere.
- **scripts/export-bort-source.sh** (LOW): The script hardcodes /root/.openclaw/workspace for both invoked commands and output paths, which is inconsistent with workspace-aware scripts that honor BORT_WORKSPACE/constants.
- **scripts/update-bort-bundle.sh** (LOW): The script hardcodes /root/.openclaw/workspace and public/bort-bundle paths, making it less portable than other workspace-rooted tooling.
- **scripts/autonomous_generate_queue.mjs** (LOW): seedQueue() is entirely hardcoded to two specific tasks and repos, so queue contents require code edits instead of policy/config updates.
- **scripts/autonomous_execute_queue.mjs** (LOW): execBortOsTask() and execPersonalWebsiteTask() contain large inlined task-specific implementations, which duplicates branch/commit/PR flow and does not scale as more autonomous tasks are added.
- **scripts/generate-state-of-bort.mjs** (LOW): The installed-skill listing arbitrarily truncates to 30 entries, which can silently omit available skills from the generated snapshot.
- **integrations/gmail/test.js** (LOW): test.js manually reimplements OAuth credential loading instead of using the shared loadOAuthClient() helper from lib.js, creating redundant logic and a second parsing path to maintain.
- **integrations/gmail/inbox-audit.js** (LOW): countThreads() and scanTopSenders() pass a string as the second argument to withBackoff(), but lib.js expects an options object, so the extra context is silently ignored.
- **integrations/gmail/backlog-state-gobuffs10.json** (LOW): A runtime backlog state file template is committed in-repo even though the file itself says real per-account state should live outside version control, which is inconsistent with the stated runtime-state pattern.
- **integrations/gmail/other-prune-state-gobuffs10.json** (LOW): This committed prune state file is another runtime-state artifact checked into the repo despite its own note saying real state should live outside version control.
- **integrations/gmail/progress-gobuffs10.json** (LOW): progress-gobuffs10.json is tracked in-repo even though it is labeled as template-only runtime progress state, which conflicts with the intended ignored runtime-state layout.
- **integrations/gmail/backlog-sweep.log** (LOW): A runtime log placeholder is committed in-repo even though it explicitly says active logs should live outside version control, which is inconsistent with the rest of the runtime-path guidance.
- **integrations/gmail/daily-summary.sh** (LOW): daily-summary.sh hardcodes the default account slug gobuffs10 and matching secret/prefs paths, so reusing it for another inbox requires script-level environment overrides everywhere.
- **integrations/gmail/sweep-runner.sh** (LOW): sweep-runner.sh also hardcodes gobuffs10-specific credential, token, prefs, and state defaults, which makes the runner less portable across Gmail accounts.
- **integrations/gmail/README.md** (LOW): README.md still instructs users to create token.json and credentials.json directly inside integrations/gmail, while RUNBOOK.md and the newer scripts assume secrets and runtime state live under ~/.openclaw paths.
- **os/preflight.js** (LOW): loadHatProfiles() embeds a full fallback hat-policy object that duplicates hat-profiles.json, creating redundant policy definitions that can drift from the JSON source.
- **os/model-routing.js** (LOW): The lightweight, research_web, social_drafting, spec_large, and default route arrays are currently identical, so the routing table carries redundant categories without behavior differences.
- **os/state_report.js** (LOW): state_report.js derives hats from memory/hats.md while the rest of the OS now reads hat-profiles.json, making the report inconsistent with the established canonical policy source.
- **os/models_inventory.js** (LOW): The heading "OpenAI fallback chain" is misleading because inv.routes.default includes OpenRouter entries as well, which can produce inaccurate operator output.
- **os/social_ops_execute_x.js** (LOW): DEFAULT_CONFIG.bio is loaded and exposed through loadConfig() but never used because the script always skips the bio update step, leaving dead configuration data in the file.
- **os/x_call.js** (LOW): x_call.js duplicates OpenClaw config read/write helpers that overlap conceptually with x_auth.js’s env-var persistence path, increasing maintenance risk around token/config handling.
- **os/x_pricing.json** (LOW): x_pricing.json records parse_status=manual_required and source URLs only, so cost routing still depends on hardcoded fallback estimates in code instead of a real config-driven pricing table.
- **os/memory/x_budget_ledger.json** (LOW): This committed ledger file is explicitly marked template-only runtime state, which is inconsistent with the stated pattern that live X budget state should live outside version control.
- **os/memory/x_digest.log.md** (LOW): A runtime digest log placeholder is committed in-repo even though the file says active digest logs should live in an ignored runtime path outside version control.
- **os/memory/x_digest_state.json** (LOW): This tracked digest cursor file is another template-only runtime state artifact that conflicts with the documented runtime-path convention.
- **os/memory/x_engagement.log.md** (LOW): The engagement log placeholder is stored in-repo despite stating that real engagement output belongs in an ignored runtime path, which is inconsistent with the intended storage model.
- **os/memory/x_queue.md** (LOW): x_queue.md is checked in as a placeholder even though it represents active append-only queue state in the running system, making it inconsistent with the external runtime-state guidance used by the neighboring files.
- **skills/inbox_daily_triage.js** (LOW): This file is still a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **skills/inbox_daily_triage_wrapper.js** (LOW): This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying work, creating redundant no-op logic.
- **skills/web_repo_audit.js** (LOW): Repo audit failures print stderr and exit without appending a failure log entry, which is inconsistent with the other skeletons’ memory-log pattern.
- **skills/weekly_healthcheck.js** (LOW): The script shells out to hardcoded commands free, df, and uptime directly, which makes the health snapshot behavior less configurable than the rest of the workspace tooling.
- **skills/README.md** (LOW): The README promises lightweight read-only skill scripts in general terms, but the current directory mostly contains placeholders/no-op skeletons, which can mislead operators about implementation maturity.
- **skills/pr-review/SKILL.md** (LOW): The command path is hardcoded to /root/.openclaw/workspace/scripts/pr-review-job.mjs, which is inconsistent with more portable workspace-relative patterns used elsewhere.



## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
