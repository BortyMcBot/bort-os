# Self-Review Plan — 2026-03-25

Generated by `self-review-job.mjs` on 2026-03-25T08:10:32.216Z
Previous plan: plans/2026-03-24-plan.md

## This Run Summary

- **Files reviewed:** (see findings log)
- **Findings:** 97 total (14 HIGH, 48 MEDIUM, 35 LOW)
- **PRs opened:** 42
  - fix: [SELF-REVIEW] runCritical() contains an unreachable status check because — https://github.com/BortyMcBot/bort-os/pull/133
  - fix: [SELF-REVIEW] stopAutonomous() ignores the exit status of — https://github.com/BortyMcBot/bort-os/pull/134
  - fix: [SELF-REVIEW] The Telegram send result is never checked, so reporting can — https://github.com/BortyMcBot/bort-os/pull/135
  - fix: [SELF-REVIEW] Child script paths are hardcoded to — https://github.com/BortyMcBot/bort-os/pull/136
  - fix: [SELF-REVIEW] The hard-stop timer computes delta minutes with Math.max(0, — https://github.com/BortyMcBot/bort-os/pull/137
  - fix: [SELF-REVIEW] TELEGRAM_TARGET is resolved with an inline node command — https://github.com/BortyMcBot/bort-os/pull/138
  - fix: [SELF-REVIEW] The rollback path performs git reset --hard to the previous — https://github.com/BortyMcBot/bort-os/pull/139
  - fix: [SELF-REVIEW] readJson() does not handle missing or malformed snapshot — https://github.com/BortyMcBot/bort-os/pull/140
  - fix: [SELF-REVIEW] The script shells out to openclaw skills list --json — https://github.com/BortyMcBot/bort-os/pull/141
  - fix: [SELF-REVIEW] cmd_start() leaves a stale PID file behind when startup — https://github.com/BortyMcBot/bort-os/pull/142
  - fix: [SELF-REVIEW] reviewPR() fetches full PR JSON and full diff for every — https://github.com/BortyMcBot/bort-os/pull/143
  - fix: [SELF-REVIEW] The script can auto-commit and push allowlisted changes to — https://github.com/BortyMcBot/bort-os/pull/144
  - fix: [SELF-REVIEW] createFixPRs() runs fixer prompts without constraining the — https://github.com/BortyMcBot/bort-os/pull/145
  - fix: [SELF-REVIEW] createPRs() captures changeResult from the openclaw agent — https://github.com/BortyMcBot/bort-os/pull/146
  - fix: [SELF-REVIEW] loadJson() and saveJson() perform unguarded JSON parse and — https://github.com/BortyMcBot/bort-os/pull/147
  - fix: [SELF-REVIEW] The implementation does not remove UNREAD for SpamReview — https://github.com/BortyMcBot/bort-os/pull/148
  - fix: [SELF-REVIEW] The script hardcodes the gobuffs10 secret paths, workspace — https://github.com/BortyMcBot/bort-os/pull/149
  - fix: [SELF-REVIEW] withBackoff() duplicates retry logic already provided by — https://github.com/BortyMcBot/bort-os/pull/150
  - fix: [SELF-REVIEW] getOrCreateLabel() lists all labels on every call, so — https://github.com/BortyMcBot/bort-os/pull/151
  - fix: [SELF-REVIEW] listAllThreads() loads every matching thread into memory — https://github.com/BortyMcBot/bort-os/pull/152
  - fix: [SELF-REVIEW] loadJson() parses the state file without error handling, so — https://github.com/BortyMcBot/bort-os/pull/153
  - fix: [SELF-REVIEW] A real account-specific prefs file is committed in-repo — https://github.com/BortyMcBot/bort-os/pull/154
  - fix: [SELF-REVIEW] INITIAL defaults to a hardcoded backlog size of 13888, — https://github.com/BortyMcBot/bort-os/pull/155
  - fix: [SELF-REVIEW] The script defaults LOG and STATE to committed in-repo — https://github.com/BortyMcBot/bort-os/pull/156
  - fix: [SELF-REVIEW] For each sender the script performs message get calls — https://github.com/BortyMcBot/bort-os/pull/157
  - fix: [SELF-REVIEW] deriveFromHatsMd() and state_report.js both scrape — https://github.com/BortyMcBot/bort-os/pull/158
  - fix: [SELF-REVIEW] appendBlock() writes directly to target files without — https://github.com/BortyMcBot/bort-os/pull/159
  - fix: [SELF-REVIEW] routeModel() falls back to hardcoded [HIGH RISK] — https://github.com/BortyMcBot/bort-os/pull/160
  - fix: [SELF-REVIEW] The self-test expectations are stale and still assert — https://github.com/BortyMcBot/bort-os/pull/161
  - fix: [SELF-REVIEW] appendModelsCheckLog() appends unbounded full OpenRouter — https://github.com/BortyMcBot/bort-os/pull/162
  - fix: [SELF-REVIEW] loadHatProfiles() embeds a full fallback hat policy object [HIGH RISK] — https://github.com/BortyMcBot/bort-os/pull/163
  - fix: [SELF-REVIEW] The assertions are stale and still expect gpt-5.3-codex for — https://github.com/BortyMcBot/bort-os/pull/164
  - fix: [SELF-REVIEW] The follow target list and tweet text are hardcoded — https://github.com/BortyMcBot/bort-os/pull/165
  - fix: [SELF-REVIEW] The target follow list and hourly cap are hardcoded values, — https://github.com/BortyMcBot/bort-os/pull/166
  - fix: [SELF-REVIEW] preflightTestStatus() shells out to node — https://github.com/BortyMcBot/bort-os/pull/167
  - fix: [SELF-REVIEW] ensureLedgerFile() comments that invalid JSON should not be — https://github.com/BortyMcBot/bort-os/pull/168
  - fix: [SELF-REVIEW] The test writes real spend records and queue entries into — https://github.com/BortyMcBot/bort-os/pull/169
  - fix: [SELF-REVIEW] notify() uses execFileSync with stdio inherit and no error — https://github.com/BortyMcBot/bort-os/pull/170
  - fix: [SELF-REVIEW] notify() sends unconditionally even when TELEGRAM_CHAT_ID — https://github.com/BortyMcBot/bort-os/pull/171
  - fix: [SELF-REVIEW] parsePostLog() reparses the markdown post log to recover — https://github.com/BortyMcBot/bort-os/pull/172
  - fix: [SELF-REVIEW] notify() mixes best-effort notification with inherited — https://github.com/BortyMcBot/bort-os/pull/173
  - fix: [SELF-REVIEW] repoPath validation uses a restrictive regex on the entire — https://github.com/BortyMcBot/bort-os/pull/174
- **Staleness notifications sent:** 0

## Open Items

- **scripts/autonomous_generate_queue.mjs** (LOW): The queue generator is fully hardcoded and ignores autonomous_policy.json, so the generate step is a static seed file rather than policy-driven queue generation.
- **scripts/autonomous_runner.mjs** (LOW): The comment says the queue is generated at the start of each autonomous window but runOnce() regenerates it on every 10-minute tick, which is inconsistent with documented behavior.
- **scripts/deploy.mjs** (LOW): run() and runReadOnly() are identical wrappers around execSync(), which is redundant and diverges from the codebase’s usual single-command-helper pattern.
- **scripts/export-bort-source.sh** (LOW): The script hardcodes /root/.openclaw/workspace instead of using the same BORT_WORKSPACE or constants pattern used by other scripts.
- **scripts/export-project-source.mjs** (LOW): buildTgzBundle() returns a boolean but the caller ignores failures, so export can appear successful even when the bundle is not created.
- **scripts/generate-project-source-core.mjs** (LOW): extractRoutes() and similar regex-based parsers duplicate parsing logic already present in other generators and checkers, increasing maintenance drift when route syntax changes.
- **scripts/pr-review-job.mjs** (LOW): ghJson() is dead code and never called anywhere in the script.
- **scripts/pr-review-job.mjs** (LOW): hasProjectSourceMd() and hasArchDrift() are defined but never used, leaving dead code and fragmented policy logic.
- **scripts/self-review-job.mjs** (LOW): runWebResearch() creates /tmp/self-review-research-*.txt files but never uses them for the agent call, making the temp-file write dead work.
- **scripts/site-improvement-job.mjs** (LOW): PINCHTAB_PID and PINCHTAB_BASE are declared but never used, which is dead code.
- **scripts/update-bort-bundle.sh** (LOW): The script hardcodes the workspace root and public output path instead of following the config or constants pattern used in several Node scripts.
- **integrations/gmail/backlog-sweep.log** (LOW): This tracked file is effectively placeholder text stating runtime logs should not live in-repo, so keeping it in the reviewed file set is dead weight and inconsistent with its own guidance.
- **integrations/gmail/count-unread.js** (LOW): This duplicates the same paginated unread-count logic already implemented in count-threads.js, creating redundant code paths for the same operation.
- **integrations/gmail/inbox-audit.js** (LOW): The documented default for --senderPages says 20, but the actual code default is 5, which is inconsistent and can mislead operators about scan coverage.
- **integrations/gmail/package.json** (LOW): package.json contains only dependencies and omits basic metadata or scripts, which is inconsistent with established package structure and makes common operations less discoverable.
- **integrations/gmail/setup-labels.js** (LOW): The label list is hardcoded in-script, which duplicates label configuration already described in RUNBOOK.md and other scripts.
- **integrations/gmail/test.js** (LOW): test.js duplicates OAuth client loading logic that already exists in lib.js, creating unnecessary parallel auth code.
- **integrations/gmail/test.js** (LOW): The required --email argument is only used as a CLI gate and never validated against the authenticated account, making it effectively dead input.
- **integrations/gmail/unsubscribe.js** (LOW): normalizeFrom is imported from lib.js but never used, leaving dead code in the module imports.
- **os/constants.js** (LOW): OPENCLAW_CONFIG_PATH and BORT_WORKSPACE still default to hardcoded absolute paths, which makes the module less portable despite being positioned as a shared constants source.
- **os/model-routing.js** (LOW): The ROUTES categories are all identical arrays, so the routing table currently adds repetition without encoding any category-specific behavior.
- **os/model-routing.js** (LOW): isModelAvailable() accepts an envelope parameter but never uses it, which is dead API surface and hints at unfinished sensitivity-based gating.
- **os/models_inventory.js** (LOW): The section label OpenAI fallback chain is inconsistent with the actual default route, which now includes an OpenRouter Claude fallback as well.
- **os/preflight.js** (LOW): envelopeTemplate() still advertises hat: inbox|web|resale|ops-core and omits autonomous, which is inconsistent with the actual hat registry.
- **os/preflight.test.js** (LOW): path is imported but never used, leaving dead code in the test harness.
- **os/social_ops_execute_x.js** (LOW): BIO is declared but never used because the bio-update step is always skipped, leaving dead code at the top of the script.
- **os/x_auth.js** (LOW): redact() always returns [REDACTED], so printing tokens_printed: [REDACTED] adds noise without conveying useful state and functions like dead ceremony.
- **os/x_budget.js** (LOW): estimateCost() accepts actionType but never uses it, leaving dead parameter surface in the budget API.
- **os/x_daily_post.js** (LOW): execSync is imported but never used, leaving dead code in the module imports.
- **os/x_post_job.js** (LOW): buildTweet() is dead code because the final posting path constructs tweet text inline and never calls it.
- **os/memory/x_digest.log.md** (LOW): This file contains only a placeholder note and no executable or reporting logic, so keeping it under the active runtime log path is dead weight and inconsistent with the stated out-of-repo logging pattern.
- **os/memory/x_engagement.log.md** (LOW): This tracked file is only a placeholder saying engagement output should not be stored in-repo, making it effectively dead content in the active runtime output path.
- **skills/inbox_daily_triage.js** (LOW): This file is a pure skeleton that only prints placeholder text and logs a no-op, making it effectively dead code unless something external depends on its stub behavior.
- **skills/inbox_daily_triage_wrapper.js** (LOW): This wrapper duplicates the same envelope validation and logging flow as inbox_daily_triage.js but intentionally performs no underlying action, which is redundant placeholder logic.
- **skills/web_repo_audit.js** (LOW): On failure the script prints raw stderr or message directly, which is inconsistent with the surrounding minimal, structured output style used by these skill skeletons.



## Proposed Next Actions

1. Review and merge any self-review PRs opened this run
2. Address any [ACTION] items from web research digest
3. Revisit deferred LOW findings if patterns recur

## Questions for Bryan

- (none this run — will populate when findings require a decision)
