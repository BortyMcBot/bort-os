# Web Research Digest — 2026-03-30

Generated by `self-review-job.mjs` on 2026-03-30T13:12:41.648Z

## AI/Agent Tooling Updates

Gemini skill doesn’t appear to be available here, and live search isn’t working in this environment, so this is a careful high-signal summary based on durable recent agent-tooling patterns rather than a fresh scrape.

- Codex-style agent workflows keep moving toward tight, verified loops rather than big one-shot autonomous runs. The strong pattern is small-step execution, explicit verification after each tool call, and retry only when the state is known.
- Agentic frameworks are converging on durable execution primitives: checkpoints, resumable jobs, explicit state files, and bounded subagents. Partial failure recovery now matters more than flashy autonomy demos.
- Tool-use best practice keeps shifting toward typed/internal tools over shell-heavy behavior. Teams want narrow, auditable tools for read/edit/search/browse/message/deploy because they reduce side-effect risk and make failures easier to trace.
- Evaluation has moved from “can it solve the task once?” to “can it avoid duplicate actions, recover cleanly, and leave an audit trail?” Operational correctness is becoming the real benchmark.
- Multi-agent setups still help, but bounded specialization beats swarm chaos. One orchestrator plus a few tightly scoped workers usually beats a pile of overlapping agents freelancing in the dark.

[ACTION] Add verify gates after every mutating step: changed-files checks, expected-path validation, cleanup confirmation, and explicit partial-failure status output.

[ACTION] Prefer first-class tools over generic shell commands whenever there is already a typed tool for the job.

[ACTION] Persist state/checkpoints for long-running jobs so reruns resume cleanly instead of repeating side effects.

[ACTION] Evaluate agent workflows on recovery behavior and auditability, not just happy-path success.

[ACTION] Keep multi-agent orchestration narrow and role-based instead of letting lots of agents overlap on the same surface area.

## eBay/Resale Market Trends

Gemini skill doesn’t appear to be available here, and live search isn’t working in this environment, so this is a cautious resale-market read based on durable recent patterns rather than a fresh scrape.

- Demand continues to favor practical, replacement-driven categories over purely speculative ones. Parts, tools, electronics accessories, shoes, and branded everyday goods usually move more consistently than hype-heavy collectible niches.
- Pricing strategy has shifted toward faster sell-through discipline. Sellers are generally winning more with realistic comps, quicker markdowns on stale inventory, and shipping-aware pricing instead of listing high and waiting forever.
- Listing quality matters more as marketplaces get denser. Better titles, cleaner item specifics, accurate condition notes, and stronger first photos often improve conversion more than tiny price changes.
- Repeatable inventory is becoming more valuable than one-off treasure hunting. Replenishable or semi-repeatable SKUs make sourcing, repricing, and operational automation much easier.
- Marketplace automation remains a risk surface. APIs and seller tooling tend to get stricter over time on auth, rate limits, and compliance, so unattended integrations need monitoring and fallback workflows.

[ACTION] Bias sourcing toward practical, repeatable categories with stable sell-through, not just sexy theoretical margins.

[ACTION] Reprice stale inventory sooner — especially where active comps are crowded and watcher activity is weak.

[ACTION] Track sell-through by category and inventory age, not just gross profit per sale.

[ACTION] Improve listing inputs first: title structure, item specifics, photos, and condition language usually beat fancy pricing logic.

[ACTION] Audit any marketplace/API automation for auth, rate limits, and manual fallback paths before it breaks at the worst possible time.

## OpenClaw/Skill Ecosystem

Gemini skill doesn’t appear to be available here, and I don’t have live ecosystem pull right now, so this is a careful OpenClaw-focused summary based on framework patterns visible in this environment.

- OpenClaw is clearly leaning more on first-class tools plus narrowly scoped skills. The good pattern is: use a built-in tool when it exists, and use skills to encode procedure, guardrails, and task selection logic.
- Skill design is trending toward smaller, sharper scopes. The strongest skills are explicit about when to use them, when not to use them, and what exact workflow they own instead of pretending to be a universal Swiss-army blob.
- Portability matters more because skills can exist both globally and in the workspace. Absolute paths, hidden local dependencies, and machine-specific assumptions stand out fast and make skills brittle.
- ClawHub-style distribution pushes skills toward package hygiene: clearer metadata, more precise descriptions, and cleaner structure. That pressure is good — it makes skills easier to discover, install, and trust.
- Development patterns increasingly favor isolated sessions/subagents for longer work and typed tools for side effects. That matches the broader agent trend: explicit orchestration beats vague autonomous wandering.

[ACTION] Audit skills for absolute paths and machine-local assumptions before treating them as portable or publishable.

[ACTION] Keep new skills highly specific: one obvious job, clear trigger language, explicit exclusions, and minimal hidden dependencies.

[ACTION] Prefer first-class OpenClaw tools inside skills over shell wrappers whenever the equivalent tool already exists.

[ACTION] If publishing through ClawHub, treat each skill like a distributable package: clean metadata, predictable structure, and documented runtime assumptions.

[ACTION] Centralize shared helper behavior instead of copying the same logic across multiple skills or wrapper scripts.

## General Tech/Stack News

Gemini skill doesn’t appear to be available here, and live search isn’t working in this environment, so this is a cautious ecosystem summary based on durable recent trends rather than a fresh scrape.

- Node.js tooling keeps drifting toward newer runtimes, so older LTS versions increasingly cause friction in package installs, local dev, lint/test stacks, and CI behavior. “Still boots” is not the same thing as “the toolchain is healthy.”
- GitHub platform changes continue to push toward stricter automation guardrails: tighter token scopes, better provenance expectations, and more scrutiny on Actions permissions and write-capable workflows.
- VPS security risk still mostly comes from neglected basics, not movie-hacker fantasy. Patch cadence, SSH exposure, stale dependencies, weak firewall posture, and overly broad automation permissions are still the usual self-own pattern.
- Developer tooling keeps getting more agent-integrated, but the setups that actually work have verification, rollback, and observability built in. Fancy autonomy without safeguards is still how teams manufacture regret.
- Supply-chain hygiene remains a major theme: fewer dependencies, current lockfiles, cautious update flow, and auditable build/deploy paths matter more as automation touches more of the stack.

[ACTION] Keep Node on a current supported LTS across dev, CI, and server environments so the stack doesn’t drift into compatibility weirdness.

[ACTION] Audit GitHub tokens, Actions permissions, and branch protections — especially anything that can write, deploy, merge, or publish.

[ACTION] Review VPS basics on a schedule: SSH hardening, firewall exposure, patching cadence, and dependency update posture.

[ACTION] Add explicit verification and rollback steps to deployment and agent automation before expanding what it’s allowed to touch.

[ACTION] Trim dependency sprawl and keep lockfiles current; supply-chain hygiene is now normal ops, not optional cleanup theater.
