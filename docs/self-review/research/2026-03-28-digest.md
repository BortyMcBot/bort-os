# Web Research Digest — 2026-03-28

Generated by `self-review-job.mjs` on 2026-03-28T13:10:45.183Z

## AI/Agent Tooling Updates

Gemini skill isn’t available here, so I’m using web search to get a current snapshot instead.
Gemini skill isn’t available here, and live web search failed due to API credits, so this is a careful high-signal summary based on durable recent agent-tooling patterns rather than a fresh scrape.

- OpenAI/Codex-style workflows are converging on tight verification loops instead of long one-shot autonomy. The strong pattern is now: plan a small step, use a narrow tool, verify the result, then continue. Reliability is beating “big autonomous run” vibes.
- Agent frameworks are standardizing around durable execution: checkpoints, resumability, explicit state files, and isolated workers/subagents. The reason is simple: partial failure recovery matters more than flashy demos once agents touch real repos and ops workflows.
- Tool-use best practice is increasingly “prefer typed/internal tools over shell whenever possible.” Teams are pushing agents toward first-class browse/edit/search/message tools because they’re safer, more auditable, and easier to constrain than freeform command execution.
- Evaluation is shifting away from benchmark theater toward operational correctness: duplicate-action resistance, recovery after a failed step, expected-path validation, and clean partial-failure signaling. In practice, these are now the traits that make an agent usable.
- Multi-agent setups are still useful, but the winning pattern is bounded specialization, not swarm chaos. One orchestrator plus a few tightly scoped specialists tends to outperform a bunch of overlapping agents stepping on each other.

[ACTION] Add explicit verify gates after every mutating step: changed-files check, expected-path check, and exit-status signaling for partial cleanup/failure.

[ACTION] Prefer first-class tools over shell wrappers for common actions like editing, search, messaging, browser work, and config changes.

[ACTION] For longer workflows, persist state/checkpoints so reruns resume cleanly instead of repeating side effects.

[ACTION] Judge agent changes on failure recovery and auditability, not just whether the happy path worked once.

[ACTION] Keep multi-agent orchestration narrow: use specialists with clear boundaries instead of open-ended agent swarms.

## eBay/Resale Market Trends

Gemini skill isn’t available here, and live search is unavailable right now, so this is a cautious resale-market read based on durable recent patterns rather than a fresh scrape.

- Demand has stayed strongest in practical, replacement-driven categories: parts, tools, electronics accessories, shoes, and branded everyday goods tend to move more consistently than speculative collectible niches. Utility keeps beating hype when buyers get more price-sensitive.
- Pricing strategy has shifted toward faster inventory turns over ambitious list prices. Sellers are doing better with realistic comps, quicker markdowns on stale listings, and shipping-aware pricing instead of waiting forever for a top-of-market buyer.
- Listing quality matters more as marketplaces get more saturated: clean titles, good first photos, accurate condition notes, and strong item specifics often improve conversion more than tiny price tweaks.
- Operationally, repeatable inventory is getting more valuable than one-off treasure hunting. Multi-quantity or repeat-sourcable SKUs make automation, repricing, and sell-through management way easier.
- Seller/platform automation still carries risk: APIs and marketplace tooling tend to move toward stricter auth, rate limits, and compliance expectations, so “set it and forget it” integrations age badly without monitoring.

[ACTION] Bias sourcing toward practical, repeatable categories with stable sell-through, not just the sexiest theoretical margins.

[ACTION] Reprice stale listings faster — especially when active comps are crowded and watcher activity is weak.

[ACTION] Track sell-through by category and age bucket, not just gross profit per sale.

[ACTION] Improve listing inputs first: title structure, photos, item specifics, and condition language usually beat fancy pricing logic.

[ACTION] If you rely on marketplace/API automation, audit auth, rate limits, and fallback manual workflows before they fail at the worst possible time.

## OpenClaw/Skill Ecosystem

Gemini skill isn’t available here, and I don’t have live ecosystem pull right now, so this is a careful OpenClaw-focused summary based on the framework patterns visible in this environment.

- OpenClaw is clearly leaning harder into first-class tools plus narrowly scoped skills. The healthy pattern is: use a built-in tool when it exists, and use skills mainly to encode task-specific procedure and guardrails.
- Skill design is trending toward sharper trigger conditions and smaller surface area. The better skills are specific about when to use them, when not to use them, and what exact workflow they own instead of acting like giant catch-all playbooks.
- Portability matters more now that skills can live both globally and in the workspace. Absolute paths, hidden local assumptions, and undocumented dependencies stand out fast and make skills brittle across installs.
- ClawHub-style distribution pushes skills toward package-like hygiene: clearer metadata, explicit descriptions, cleaner structure, and fewer machine-specific assumptions. That’s good — it forces authors to stop shipping mystery meat.
- Development patterns increasingly favor isolated sessions/subagents for longer work and typed tools for side effects. That matches the broader agent trend: explicit orchestration beats vague “go do stuff” autonomy.

[ACTION] Audit skills for absolute paths and machine-local assumptions before treating them as portable or publishable.

[ACTION] Keep new skills highly specific: one obvious job, clear trigger language, explicit exclusions, and minimal hidden dependencies.

[ACTION] Prefer first-class OpenClaw tools inside skills over shell wrappers whenever the equivalent tool already exists.

[ACTION] If publishing through ClawHub, treat each skill like a distributable package: clean metadata, predictable structure, and documented runtime assumptions.

[ACTION] For skill maintenance, centralize shared helper behavior instead of copying the same logic across multiple skills or wrappers.

## General Tech/Stack News

Gemini skill isn’t available here, and live search is unavailable right now, so this is a cautious ecosystem summary based on durable recent trends rather than a fresh scrape.

- Node.js tooling keeps drifting toward modern runtimes, so older LTS versions increasingly cause friction in package installs, dev servers, lint/test stacks, and CI behavior. “It still starts” is not the same as “the toolchain is happy.”
- GitHub platform changes continue to emphasize stronger automation guardrails: tighter token scopes, better provenance expectations, more scrutiny on Actions permissions, and more value in keeping branch/deploy protections boring and strict.
- VPS security risk still mostly comes from neglected basics rather than movie-hacker nonsense: patch cadence, SSH exposure, weak firewall defaults, stale dependencies, and overly broad automation permissions are still the usual foot-guns.
- Developer tooling is getting more agent-integrated, but the useful setups are the ones with verification, rollback, and observability built in. Fancy autonomy without safeguards is still how people turn a server into a regret machine.
- Supply-chain hygiene remains a major theme: fewer dependencies, current lockfiles, cautious updates, and auditable build/deploy paths matter more as automation touches more of the stack.

[ACTION] Keep Node on a current supported LTS across dev, CI, and server environments so the stack doesn’t drift into compatibility weirdness.

[ACTION] Audit GitHub tokens, Actions permissions, and branch protections — especially any workflow that can write, deploy, merge, or publish.

[ACTION] Review VPS basics on a schedule: SSH hardening, firewall exposure, unattended patch posture, and dependency update cadence.

[ACTION] Add explicit verification and rollback steps to deployment/agent automation before expanding what it’s allowed to touch.

[ACTION] Trim dependency sprawl and keep lockfiles current; supply-chain hygiene is now normal ops, not optional spring cleaning.
