# Web Research Digest — 2026-03-23

Generated by `self-review-job.mjs` on 2026-03-23T07:09:52.057Z

## AI/Agent Tooling Updates

I’m doing a quick source-grounded pass and I’ll keep it to the signal, with action items flagged.
Here’s the useful cut:

- **OpenAI is consolidating agent development around the Responses API, built-in tools, and the Agents SDK.** Their “new tools for building agents” launch makes the direction pretty explicit: Responses is the preferred primitive for multi-step, tool-using agents, with built-in web search, file search, computer use, and integrated tracing/observability. They also say the Assistants API is headed toward deprecation once parity lands, targeting **mid-2026**. [ACTION] If you still have Assistants-era abstractions in the stack, start planning a migration path now instead of investing deeper there.

- **Tooling is getting broader and more capability-aware, not just “function calling with extra steps.”** OpenAI’s tools guide now treats built-in tools, function calling, remote MCP servers, shell, skills, computer use, file search, and tool search as standard agent building blocks. One especially relevant detail: **tool_search is only supported on gpt-5.4 and later**. [ACTION] Make model routing tool-capability-aware, especially for any workflow that relies on dynamic tool loading or deferred tool definitions.

- **Reliability is increasingly an orchestration + observability problem.** OpenAI is leaning hard into tracing and agent workflow inspection, which is basically the mature-platform admission that production agents need visible state transitions, tool-call chains, and retry history. The best-practice pattern here is smaller prompts, explicit tool contracts, and stronger traces instead of giant magical prompts. [ACTION] Add structured logs around tool selection, tool failures, retries, and final decisions so agent failures are debuggable in production.

- **MCP keeps solidifying as the interoperability standard.** The MCP docs still frame it well: it’s the standard way to connect AI apps to tools, data, and workflows, with broad support across ChatGPT, Claude, VS Code, Cursor, and others. The practical consequence is that bespoke one-off integrations are getting harder to justify when MCP gives you reuse across multiple agent clients. [ACTION] Expose internal tools over MCP before writing provider-specific adapters whenever you can.

- **Framework best practice is still “single agent first, multi-agent only when boundaries are real.”** OpenAI’s SDK/orchestration direction supports multi-agent systems, but the real pattern underneath is not “more agents everywhere” — it’s “split agents only when permissions, context windows, or responsibilities genuinely differ.” Otherwise you mostly buy complexity and debugging pain. [ACTION] Default to one agent plus tools, and only introduce multiple agents when they measurably improve isolation, reliability, or latency.

If you want, I can turn this into:
1. a **bort-os-specific action list**, or
2. a **weekly agent-tooling digest template**.

## eBay/Resale Market Trends

Here’s the clean read:

- **eBay is pushing sellers toward native AI-assisted pricing and listing workflows.** Recent seller updates highlight AI background enhancement plus a **Product research tool** framed around helping sellers understand market value and price items more effectively. That’s a strong signal that better comps, better presentation, and smarter pricing are now baseline advantages, especially for used, collectible, and long-tail inventory. [ACTION] Build pricing/comps review into your standard listing workflow instead of treating it as optional cleanup.

- **Legacy category and metadata APIs are continuing to age out.** eBay’s deprecation and migration guidance keeps pushing developers away from older Trading/Product-era metadata flows like **GetCategories**, **GetCategoryFeatures**, and legacy product compatibility paths, with migration centered on **Taxonomy API** and **Metadata API**. [ACTION] Audit any listing/category/aspect logic that still depends on Trading/Product-era metadata calls before those deprecations turn into break-fix work.

- **Media handling is shifting to newer APIs too.** Older image-upload flows like **UploadSiteHostedPictures** are deprecated, with eBay steering developers toward the **Media API** (`createImageFromFile`, `createImageFromUrl`). Since photos are one of the biggest resale conversion levers, image-pipeline debt becomes sales-pipeline debt fast. [ACTION] Migrate any legacy listing image workflow to the Media API sooner rather than later.

- **Marketing automation is getting narrower in some API areas even while seller-facing tooling improves.** Some Marketing API methods like **setupQuickCampaign** and **launchCampaign** are deprecated/decommissioning, while eBay keeps adding seller-side UX for discounts, research, and merchandising. Translation: what sellers can do manually in-product is not always mirrored by stable automation surfaces. [ACTION] Separate “seller UI capabilities” from “API-supported automation” in your roadmap so you don’t assume platform features are scriptable.

- **Platform signals favor differentiated inventory over commodity flips.** The current seller-tool direction—better pricing intel, better media, better shipping/trust signals, and international selling improvements—tends to benefit categories where presentation and confidence drive margin: collectibles, niche apparel, specialty electronics, and higher-ASP used goods. [ACTION] Bias sourcing toward inventory where better photos, stronger comps, and clearer trust signals can materially widen margin.

If you want, I can turn this into:
1. a **resale sourcing strategy memo**, or
2. an **eBay API migration checklist** with endpoint-by-endpoint replacements.

## OpenClaw/Skill Ecosystem

Here’s the tight version:

- **OpenClaw is evolving from “chat agent” into a fuller control plane.** The docs and Control UI surface now emphasize sessions, cron, skills, nodes, approvals, logs, config schema/forms, updates, model failover, and multi-agent routing. The framework signal is pretty clear: the platform is leaning harder into orchestration and operations, not just prompt handling. [ACTION] Design new workflows around sessions + cron + config schema + Control UI instead of one-off script wrappers when possible.

- **Skills are becoming first-class managed runtime objects, not just folders on disk.** The Control UI exposes `skills.*` operations like status, enable/disable, install, and API key updates, which implies skills are expected to be installable, configurable, and lifecycle-managed by the platform. [ACTION] Build skills so they tolerate install/update/enable/disable flows cleanly, with minimal manual filesystem assumptions.

- **ClawHub is clearly the intended distribution layer, but it still looks early-stage.** ClawHub is pitching versioned, searchable, rollback-ready AgentSkills bundles installable with `npx clawhub@latest install ...`, but the public site currently looks sparse. That means the direction is real, but the ecosystem is still open territory. [ACTION] If you have polished internal skills, publishing early could give you outsized visibility because the registry still looks thin.

- **The winning skill pattern looks like: metadata-rich, portable, and UI-friendly.** Between ClawHub’s publish/install/version framing and OpenClaw’s managed skills surface, the framework wants skills that are portable bundles with clear metadata, versionability, and predictable config/API-key handling. Hardcoded paths and hidden env assumptions are going to age badly. [ACTION] Audit skills for path portability, explicit configuration, and install-time assumptions before treating them as reusable assets.

- **OpenClaw’s docs structure suggests skills should plug into automation patterns, not stand alone.** The surrounding docs now emphasize standing orders, cron vs heartbeat, hooks, webhooks, polling, retry, queue, and session tools. So the framework expectation is increasingly “skills as components in recurring workflows,” not isolated commands. [ACTION] When authoring or updating a skill, include guidance for cron/heartbeat/session usage so it fits OpenClaw’s automation model.

If you want, I can turn this into:
1. a **bort-os skill audit checklist**, or
2. a **ClawHub publishing readiness checklist**.

## General Tech/Stack News

Here’s the useful cut:

- **Node.js 24 is now the main LTS lane.** Node’s release track puts **v24 (Krypton)** in **Active LTS**, while **v22 (Jod)** and **v20 (Iron)** are in **Maintenance LTS**; **v18 is EOL** and **v25** is Current. For production defaults, 24 is now the safest forward-looking baseline. [ACTION] Start targeting Node 24 for new services and plan a controlled upgrade path if you’re still centered on Node 22.

- **GitHub is getting better at auditability for AI-generated work.** Recent platform changes add traceability from **Copilot coding agent commits back to session logs**, which is a strong signal that agent-authored code is becoming a first-class audited workflow. [ACTION] Require commit-to-session traceability, or an equivalent audit link, for any coding-agent changes you merge.

- **GitHub model governance is becoming less opaque.** Copilot usage reporting now resolves **auto model selection to actual underlying models** instead of leaving usage hidden behind a generic “Auto” bucket. That matters for spend analysis, compliance, and routing policy review. [ACTION] Update dashboards and policy checks to use resolved model metrics instead of treating “auto” as unanalyzable.

- **Ubuntu/VPS security advisories still show heavy kernel churn, and regressions remain part of patching reality.** Recent notices continue to include kernel issues plus follow-up regressions in adjacent packages/tools, which means “patch fast” is only half the job. [ACTION] Add a post-upgrade smoke test for SSH, remote access, and any critical services after security patch windows instead of relying on package install success alone.

- **Developer tooling direction is toward clearer install paths and stronger operational visibility.** Node is increasingly explicit about supported install methods, while GitHub is making AI activity more attributable and inspectable. The common pattern is less ambiguity in how tools are installed and how automated work is tracked. [ACTION] Standardize one blessed install/update path per host and one audit trail for agent-authored changes so your infra doesn’t drift into one-off snowflake setups.

If you want, I can turn this into:
1. a **VPS maintenance checklist for OpenClaw hosts**, or
2. a **weekly dev-infra digest template**.
