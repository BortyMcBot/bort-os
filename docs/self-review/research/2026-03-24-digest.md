# Web Research Digest — 2026-03-24

Generated by `self-review-job.mjs` on 2026-03-24T07:10:39.807Z

## AI/Agent Tooling Updates

I’m doing a quick source-grounded pass and keeping it to the signal, with action items flagged.
Here’s the useful cut:

- **OpenAI is consolidating agent development around the Responses API, built-in tools, and the Agents SDK.** Their current platform direction is pretty explicit: Responses is the preferred primitive for multi-step, tool-using agents, with built-in web search, file search, computer use, and integrated observability/tracing. They’ve also signaled the Assistants API is on a path toward deprecation once parity lands, targeting **mid-2026**. [ACTION] If you still have Assistants-era abstractions in your stack, start planning a migration path now instead of investing deeper there.

- **Tool use is broader and more capability-aware than old-school function calling.** OpenAI’s tools guidance now treats built-in tools, function calling, remote MCP servers, shell, skills, computer use, file search, and tool search as normal agent building blocks. One important implementation detail: **tool_search is only supported on gpt-5.4 and later**. [ACTION] Make model routing explicitly capability-aware, especially for workflows that rely on dynamic tool loading or deferred tool definitions.

- **Agent reliability is increasingly an orchestration and observability problem, not just a prompting problem.** OpenAI is leaning hard into tracing and workflow inspection, which is the mature-platform tell that production agents need visible state transitions, tool-call chains, and retry history. The emerging best practice is smaller prompts, clearer tool contracts, and stronger traces instead of giant “do everything” prompts. [ACTION] Add structured logs around tool selection, tool failures, retries, and final decisions so agent failures are debuggable in production.

- **MCP keeps solidifying as the interoperability standard for agent tooling.** MCP’s pitch remains strong: one standard way to connect AI apps to data sources, tools, and workflows, with broad support across ChatGPT, Claude, VS Code, Cursor, and others. The practical implication is that bespoke one-off integrations are getting harder to justify when MCP gives you reuse across multiple agent clients. [ACTION] Expose internal tools over MCP before writing provider-specific adapters whenever possible.

- **Framework best practice is still “single agent first, multi-agent only when boundaries are real.”** OpenAI’s SDK/orchestration direction supports multi-agent systems, but the underlying pattern is not “more agents everywhere” — it’s “split agents only when permissions, context windows, or responsibilities genuinely differ.” Otherwise you mostly buy complexity and debugging pain. [ACTION] Default to one agent plus tools, and only introduce multiple agents when they measurably improve isolation, reliability, or latency.

If you want, I can turn this into:
1. a **bort-os-specific action list**, or
2. a **weekly agent-tooling digest template**.

## eBay/Resale Market Trends

Here’s the clean read:

- **eBay is pushing sellers toward native AI-assisted pricing and listing workflows.** Recent seller updates highlight AI background enhancement plus a **Product research tool** framed around helping sellers understand market value and price items more effectively. That’s a strong signal that better comps, better presentation, and smarter pricing are now baseline advantages, especially for used, collectible, and long-tail inventory. [ACTION] Build pricing/comps review into your standard listing workflow instead of treating it as optional cleanup.

- **Legacy category and metadata APIs are continuing to age out.** eBay’s deprecation and migration guidance keeps pushing developers away from older Trading/Product-era metadata flows like **GetCategories**, **GetCategoryFeatures**, and legacy product compatibility paths, with migration centered on **Taxonomy API** and **Metadata API**. [ACTION] Audit any listing/category/aspect logic that still depends on Trading/Product-era metadata calls before those deprecations turn into break-fix work.

- **Media handling is shifting to newer APIs too.** Older image-upload flows like **UploadSiteHostedPictures** are deprecated, with eBay steering developers toward the **Media API** (`createImageFromFile`, `createImageFromUrl`). Since photos are one of the biggest resale conversion levers, image-pipeline debt becomes sales-pipeline debt fast. [ACTION] Migrate any legacy listing image workflow to the Media API sooner rather than later.

- **Marketing automation is getting narrower in some API areas even while seller-facing tooling improves.** Some Marketing API methods like **setupQuickCampaign** and **launchCampaign** are deprecated/decommissioning, while eBay keeps adding seller-side UX for discounts, research, and merchandising. Translation: what sellers can do manually in-product is not always mirrored by stable automation surfaces. [ACTION] Separate “seller UI capabilities” from “API-supported automation” in your roadmap so you don’t assume platform features are scriptable.

- **Platform signals favor differentiated inventory over commodity flips.** The current seller-tool direction—better pricing intel, better media, better shipping/trust signals, and international selling improvements—tends to benefit categories where presentation and confidence drive margin: collectibles, niche apparel, specialty electronics, and higher-ASP used goods. [ACTION] Bias sourcing toward inventory where better photos, stronger comps, and clearer trust signals can materially widen margin.

If you want, I can turn this into:
1. a **resale sourcing strategy memo**, or
2. an **eBay API migration checklist** with endpoint-by-endpoint replacements.

## OpenClaw/Skill Ecosystem

I’m checking the OpenClaw docs and ClawHub pages directly so this stays grounded in actual framework signal, not guesswork.
Here’s the tight version:

- **OpenClaw is becoming more of an operational control plane than “just an agent runtime.”** The docs index and Control UI page now emphasize sessions, cron, skills, nodes, approvals, logs, config schema/forms, updates, model failover, and multi-agent routing. That’s the clearest framework-level trend: the platform is investing hard in orchestration and day-2 operations, not just prompting. [ACTION] Design new workflows around sessions + cron + config schema + Control UI instead of piling up one-off wrapper scripts.

- **Skills are increasingly treated as managed runtime objects.** The Control UI exposes `skills.*` operations like status, enable/disable, install, and API key updates, which means skills are expected to be installable, configurable, and lifecycle-managed by the platform. That’s a different world from “copy a folder and hope.” [ACTION] Build skills so they tolerate install/update/enable/disable flows cleanly, with explicit config and minimal filesystem assumptions.

- **ClawHub is clearly the intended distribution layer, but the ecosystem still looks very early.** ClawHub is pitching versioned, searchable, rollback-ready AgentSkills bundles installable with `npx clawhub@latest install ...`, but the public front page still shows **no highlighted skills and no popular skills yet**. So the direction is obvious, but the market is still wide open. [ACTION] If you have polished internal skills, publishing early could give you disproportionate visibility because the registry is still sparse.

- **The emerging skill pattern is: portable, metadata-rich, and packaging-friendly.** Between OpenClaw’s managed skills surface and ClawHub’s publish/version/install model, the framework clearly wants skills that behave like reusable artifacts rather than ad hoc local scripts. Hardcoded absolute paths, hidden env assumptions, and manual install steps are going to age badly. [ACTION] Audit skills for path portability, explicit configuration, and install-time assumptions before treating them as reusable assets.

- **OpenClaw’s docs structure suggests skills should plug into automation patterns, not stand alone.** The docs now prominently cover cron jobs, cron vs heartbeat, hooks, webhooks, Gmail PubSub, retries, queueing, and session management. That means the “OpenClaw-native” way to build a skill is increasingly “make it fit recurring/event-driven workflows,” not “ship a lonely command.” [ACTION] When authoring or updating a skill, include guidance for cron/heartbeat/session usage so it fits OpenClaw’s automation model.

If you want, I can turn this into:
1. a **bort-os skill audit checklist**, or
2. a **ClawHub publishing readiness checklist**.

## General Tech/Stack News

I’m doing a quick primary-source sweep across Node, GitHub, and Ubuntu security pages and I’ll keep it to the signal.
Here’s the useful cut:

- **Node.js 24 is now the main LTS lane.** The current release table shows **v24 (Krypton)** as **Active LTS**, while **v22 (Jod)** and **v20 (Iron)** are both **Maintenance LTS**; **v18 is EOL** and **v25** is Current. If you want the safest default target for new production work, 24 is now the center of gravity. [ACTION] Start targeting Node 24 for new services and plan a controlled upgrade path if you’re still centered on Node 22.

- **Node is also getting clearer about supported installation paths.** The Node releases page now explicitly distinguishes **Official** versus **Community** installation methods and spells out criteria for each. That’s a quiet but important tooling trend: less ambiguity around how runtimes should be installed and maintained. [ACTION] Pick one blessed Node install/update path per host or team and document it, instead of letting machines drift across ad hoc package-manager setups.

- **GitHub platform changes are leaning into model choice and security controls.** The latest changelog feed shows **Gemini 3.1 Pro** expanding across JetBrains IDEs, Xcode, Eclipse, github.com, mobile, and VS Code via Copilot Chat model pickers, while another recent update adds **push protection exemptions from repository settings** instead of only org/enterprise scope. Translation: GitHub is broadening model availability while also making security control surfaces more granular. [ACTION] Revisit your GitHub governance defaults so model access and secret-scanning exceptions are intentional, not inherited accidents.

- **Ubuntu/VPS security advisories are still dominated by kernel and core-system churn.** The current notice stream includes multiple **Linux kernel**, **systemd**, **strongSwan**, and **pyOpenSSL** advisories all landing at once. That’s the usual lesson in neon lights: the risky stuff is still the boring infrastructure. [ACTION] Add a regular patch + smoke-test routine for kernel, systemd, VPN, TLS, and SSH-sensitive hosts instead of treating updates as a passive background task.

- **Developer tooling news overall is moving toward more explicit operational visibility.** Between Node’s clearer installation guidance and GitHub’s expanding model/security controls, the ecosystem trend is less “magic defaults” and more “make runtime and policy choices visible.” That’s healthy, but only if you actually wire it into your ops habits. [ACTION] Maintain a short host-level checklist covering runtime version, install method, upgrade cadence, and security-control ownership for each VPS or developer environment.

If you want, I can turn this into:
1. a **VPS maintenance checklist for OpenClaw hosts**, or
2. a **weekly dev-infra digest template**.
