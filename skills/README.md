# Local Skill Skeletons (Project 1)

These are lightweight, read-only-by-default scripts.

They MUST:
- run preflight (Task Envelope)
- emit an execution header
- log a 1–3 line entry to memory/logs.md
- avoid external state changes unless explicitly approved
