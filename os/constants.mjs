// Shared constants for bort-os scripts (ESM version).
// Single source of truth — import this instead of hardcoding values.

export const TELEGRAM_CHAT_ID = '8374853956';
export const BORT_WORKSPACE = process.env.BORT_WORKSPACE || '/root/.openclaw/workspace';
